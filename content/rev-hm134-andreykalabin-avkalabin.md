https://github.com/avkalabin/hangman  
[Andrey Kalabin]

Игра в процедурном стиле, выполнена в двух классах.

## НЕДОСТАТКИ РЕАЛИЗАЦИИ

1. Команды работают неправильно: если заявлено только две команды(Н/В), то другие команды должны игнорироваться
```
[Н]овая игра или [В]ыход?
не хочу играть далее в эту игру
  +---+
  |   |
      |
      |
=========
```

2. Но они мало того, что не игнорируются, но еще приводят к странным эффектам:
```
[Н]овая игра или [В]ыход?
не хочу играть далее в эту игру

Введите букву: 
Такой буквы нет
Использованы буквы: [х]

Введите букву: 
Такой буквы нет
Использованы буквы: [х, и]

Введите букву: 
Такой буквы нет
Использованы буквы: [д, х, и]

Введите букву: 
*******в*
Использованы буквы: [в, д, х, и]

Введите букву: 
Такой буквы нет
*******в*
Использованы буквы: [в, д, х, и, э]

Введите букву: 
Это букву уже вводили
```

3. Можно ввести не одну букву, а набор букв и это будет считаться допустимым вводом

4. Неправильная последовательность в порядке отображения введенных букв: букву 'ц' я ввел второй, но в распечатке она стала первой. 
При этом введенная третьей буква 'к' в распечатке стала на третью позицию
```
Введите букву: 
й
Использованы буквы: [й]

Введите букву: 
ц
Использованы буквы: [ц, й]

Введите букву: 
к
Использованы буквы: [ц, й, к]
```
Введенные буквы должны размещаться по алфавиту(й, к, ц) либо в порядке ввода(й, ц, к).

## ХОРОШО

+ 👍 Игра запускается
+ 👍 Можно ввести только буквы русского алфавита
+ 👍 Есть список введенных букв

## ЗАМЕЧАНИЯ

**1. Нейминг**

- Избыточно
```
Hangman.drawHangman(...)

//ПРАВИЛЬНО:
Hangman.draw(...)
```

- Название должно объяснять суть явления. Это класс для распечатки хангмана, то есть рендерер
```
class Hangman

//ПРАВИЛЬНО:
class HangmanRenderer
```
*Oracle Java code conventions, part."Naming conventions"*  
*Мартин, "Чистый код", гл.2*  
*Ютуб, Немчинский "Как называть переменные, методы и классы?"*

**2. Нарушение DRY**, магические буквы, числа, слова. Вводи константы 
```
System.out.println("[Н]овая игра или [В]ыход?");
if ("в".equalsIgnoreCase(String.valueOf(command))) {...}
if ("н".equalsIgnoreCase(String.valueOf(command))) {...}

//ПРАВИЛЬНО:
private final static String START = "Н";
private final static String QUIT = "В";

System.out.printf("[%s]овая игра или [%s]ыход? \n", START, QUIT);
if (QUIT.equalsIgnoreCase(String.valueOf(command))) {...}
if (START.equalsIgnoreCase(String.valueOf(command))) {...}
```
*Фаулер, "Рефакторинг", гл.8, "Замена магического числа символической константой"*  
*refactoring.guru "Замена магического числа символьной константой"*  

- В названии переменных не пиши тип данных, к которым они относится. И вообще не употребляй венгерскую ноттацию.  
Название переменной должно отвечать на вопрос что хранит переменная, а не как хранит
```
List<String> wordsList

//ПРАВИЛЬНО:
List<String> words
```

**3. Избавься от падежей**
```
11643 слов

//ПРАВИЛЬНО:
Количество слов: 11643
```

**4. Создавай вспомогательные методы**, делай программу более простой и понятной
```
if (hiddenWord.contains(String.valueOf(letter))) {...}

//ПРАВИЛЬНО:
if (isHidenWordLetter(letter)) {...}

private boolean isHidenWordLetter(char letter) {
  return hiddenWord.contains(String.valueOf(letter));
}
```

**5. Большой божественный метод**

Большой божественный метод состоящий из необозримого количества строк- 50
```
private static void playRound(List<String> wordsList, Scanner scanner)
```
Делает много всего сразу, разобраться в нем трудно, код читается плохо. 
Метод разделить на несколько маленьких, каждый из которых будет делать свое.  
*Мартин "ЧК", гл.3, "Компактность!"*  
*Фаулер "Рефакторинг", гл.6, "Извлечение метода"* 

**6. При прочих равных, бери данные из окружения класса**

Если одни и те же данные используются в разных методах класса, то эти данные нужно не перекидывать туда-сюда между методами, а сделать их полями класса
```
private static void updateMaskedWord(String hiddenWord, char letter, StringBuilder maskedWord) 

//ЗДЕСЬ ЛУЧШЕ:
private static final StringBuilder MASKED_WORD = new StringBuilder();
private static StringBuilder HIDDEN_WORD = new StringBuilder();

private static void updateMaskedWord(char letter) 
```
Да, вопрос о том, в каких случаях и когда поля должны браться из окружения, а когда передаваться между методами, не так прост и дискуссионен.  
Об этом нужно думать отдельно в каждом конкретном случае.
Поэтому особо на этом замечании не настаиваю.

В этой реализации программы неиспользование изменяемых полей класса приводит к предпосылкам для создания божественных методов.

Статические изменяемые поля не позволяют корректно работать двум одинаковым одновременно запущенным классам.  
Если это беспокоит, то выход есть- пиши в процедурном стиле, но создавай объекты, где изменяемые поля будут не статическими.  
Типа такого, это все еще не противоречит процедурному стилю:
```
public class Main {
  public static void main(String[] args) {
    Game game = new Game();
    game.start();
  }
}
```

**7. Если нужно печатать или создавать строку** с более, чем одним подстановочным значением или значение вставляется внутрь сообщения, используй форматированный вывод- тогда сразу будет виден весь шаблон
```
System.out.println("У вас осталось " + attemptsLeft + " попыток");

//ПРАВИЛЬНО:
System.out.printf("У вас осталось %d попыток \n", attemptsLeft);
```

**8. Экономия строчек- не приоритет**

Засунуть несколько инструкций в одну строчку это не круто, это непонятно. 
Вводи поясняющие переменные
```
return words.get(new Random().nextInt(words.size())).toLowerCase();
```
*Фаулер, "Рефакторинг", гл.6, "Введение поясняющей переменной".* 

**9. Приоритет- читаемость кода**

Смысл этой инструкции- напечатать сообщение, а после него сделать пустую строку до следующего сообщения
```
System.out.println("Использованы буквы: " + usedLetters + "\n");

//РЕЗУЛЬТАТ:
Использованы буквы: [у, е, ц, й, к, н]

Введите букву:
```
Не экономь строчки кода, изложи свою мысль более внятно:
```
System.out.println("Использованы буквы: " + usedLetters);
System.out.println();
```

**10. Код должен читаться, как открытая книга**, вводи вспомогательные методы
```
while (true) {
  //...
  if (attemptsLeft > 0) {
    System.out.println("У вас осталось " + attemptsLeft + " попыток");
   } else {
    System.out.println("Вы ПРОИГРАЛИ");
    //...
   }
}

//ЛУЧШЕ:
while (!isGameOver(...)) {
  //...
  if (isWin()) {
    printWinMessage();
   } else (isLose()){
    printLoseMessage();
   }
}

boolean isWon(...)
boolean isLose(...)

boolean isGameOver(...) {
  return  isWon(...) || isLose(...);
}
```

**11. Не пробрасывай НИКОГДА исключения из main в космос**

Если ты знаешь, что может возникнуть какое-то исключение, его нужно поймать и обработать
```
public static void main(String[] args) throws IOException
```

**12. Реакция на ошибки** 

Если по указанному адресу не будет найден файл со словами, то программа аварийно вылетит и напечатает непонятные(для юзера) сообщения в консоль
```
Exception in thread "main" java.nio.file.NoSuchFileException: src\main\resources\dictionary.txt_
	at java.base/sun.nio.fs.WindowsException.translateToIOException(WindowsException.java:85)
	at java.base/sun.nio.fs.WindowsException.rethrowAsIOException(WindowsException.java:103)
```
Здесь должна быть другая реакция на отсутствие файла со словами.  
Нужно сказать юзеру, что файл со словами по указанному пути открыть не удалось и поэтому работа программы будет завершена. 

Причем, путь нужно указать не относительный, как у тебя(`src\main\resources\dictionary.txt_`). 
А абсолютный, чтобы юзер знал, где конкретно ему нужно искать файл: `c:/..../dictionary.txt_`.

После этого корректно завершить работу программы без вываливания красных эксепшенов в консоль.

**13. Проверка соответствия буквы алфавиту сейчас- индусский код**

Сейчас соотвествие буквы русскому алфавиту происходит путем поиска буквы в строке. Это очень избыточный алгоритм
```
private static String alphabet = "абвгдеёжзийклмнопрстуфхцчшщъыьэюя";

private static boolean isValidLetter(char letter) {
  return RUSSIAN_LOWERCASE_LETTERS.contains(String.valueOf(letter));
}

//ПРАВИЛЬНО:
private static boolean isRussianLetter(char symbol) {
  symbol = Character.toLowerCase(symbol);
  return (symbol >= 'а' && symbol <= 'я') || symbol == 'ё';
}
```
Почитай про то, как символы хранятся в таблице ASCII/юникод.

**14. class Hangman**

+ 👍 Все идеально, кроме названия класса.

## ВЫВОД

Есть тенденция к созданию божественных методов.

Для развития навыков процедурной декомпозиции, посмотреть стрим Сергея [Крестики-нолики в процедурном стиле](https://www.youtube.com/watch?v=PPikj1qHxrA)

n.134(247)  
#ревью #виселица 