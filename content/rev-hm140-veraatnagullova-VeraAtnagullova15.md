https://github.com/VeraAtnagullova15/Hangman---Java  
[Вера Атнагуллова]

Игра в процедурном стиле, выполнена в нескольких классах.

## НЕДОСТАТКИ РЕАЛИЗАЦИИ

1. В начале игры печатается совершенно ацкое количество пустых строк
```
Введите 1, если да, или 0, если нет.

1

<34 пусты строки>

сагиттальная
```

2. "Сагиттальная", серьезно? Как я это должен угадать? 
Да и отгадывание прилагательных как таковых- очень сомнительное дело
```
сагиттальная
Введите только одну букву русского алфавита
```

3. Это буква русского алфавита
```
Введите только одну букву русского алфавита
ё
Вы ввели некорректный символ или ввели несколько букв подряд
```

4. Первая картинка существует, но никогда не печатается на экран:
```
  +---+
  |   |
      |
      |
      |
      |
=========
```

## ХОРОШО

+ 👍 Игра запускается
+ 👍 Можно ввести только одиночную букву русского алфавита
+ 👍 Есть список введенных букв

## ЗАМЕЧАНИЯ

**1. Нейминг**

- Название классов должно быть существительным. Этот класс назван глаголом:
```
class PrintHangman
```

- В названии переменных не пиши тип данных, к которым они относится. И вообще не употребляй венгерскую ноттацию.  
Название переменной должно отвечать на вопрос что хранит переменная, а не как хранит
```
String[] arrayWords 
char[] array

//ПРАВИЛЬНО:
String[] words 
char[] осмысленноеНазвание
```

- Не используй аббревиатуры, за исключением совершенно очевидных, вроде "http"
```
InputStream is

//ПРАВИЛЬНО:
InputStream inputStream 
```

- Не сокращай без необходимости
```
Random rand 
Scanner sc


//ПРАВИЛЬНО:
Random random 
Scanner scanner
```

- По конвенции, "is" пишется первым
```
boolean inputLetterIsCorrect(String s)

//ПРАВИЛЬНО:
boolean IsCorrectInputLetter(String s)
```

- Так логичнее
```
char[] wordWithStars = makeSecretWord(wordForGuess);

//ПРАВИЛЬНО:
char[] secretWord = makeSecretWord(wordForGuess);
```

- Этот метод не вводит букву снова. Он добавляет букву в список использованных букв в том случае, если этой буквы еще там нет
```
boolean enterLetterAgain(char symbol, ArrayList<String> list)
```

- При прочих равных, в названия не нужно вставлять частицы "Of", "The" и т.д. 
Если конечно там "Of" не используется в контексте valueOf()
```
int placeOfLetter;

//ПРАВИЛЬНО:
int letterIndex;
```

- Здесь "буква" должна быть в единственном числе
```
for (String letters : list) {
  System.out.print(letters + " ");
}

//ПРАВИЛЬНО:
for (String letter : list) {
  System.out.print(letter + " ");
}
```

- Названия входящих аргументов должны объяснять, что они хранят
```
boolean letterFound(char symbol, char[] array, char[] secretArray)

//ПРАВИЛЬНО:
boolean letterFound(char letter, char[] word, char[] secretWord)
```

*Oracle Java code conventions, part."Naming conventions"*  
*Мартин, "Чистый код", гл.2*  
*Ютуб, Немчинский "Как называть переменные, методы и классы?"*

**2. Нарушение DRY**, магические буквы, числа, слова. Вводи константы 
```
System.out.println("Введите 1, если да, или 0, если нет.\n");
case "1" -> //...
case "0" -> //...

//ПРАВИЛЬНО:
private final static String START = "1";
private final static String QUIT = "0";

System.out.printf("Введите '%s', если да, или '%s', если нет. \n", START, QUIT);
case START -> //...
case QUIT -> //...
```
*Фаулер, "Рефакторинг", гл.8, "Замена магического числа символической константой"*  
*refactoring.guru "Замена магического числа символьной константой"* 

**3. Если в блоке if есть return**(break, continue, throw, exit и т.д.), то else не пишется. 

В этом случае неважно, будет else или нет, так как программа будет работать одинаково, а код без `else` будет выглядеть читабельней
```
if (list.contains(String.valueOf(symbol))) {
  return true;
} else {
  list.add(String.valueOf(symbol));
  return false;
}

//ПРАВИЛЬНО:
if (list.contains(String.valueOf(symbol))) {
  return true;
} 
list.add(String.valueOf(symbol));
return false;
```

**4. Используй классы через их интерфейсы**
```
ArrayList<String> listOfWrongLetters = new ArrayList<>();
boolean enterLetterAgain(char symbol, ArrayList<String> list) {...}

//ПРАВИЛЬНО:
List<String> listOfWrongLetters = new ArrayList<>();
boolean enterLetterAgain(char symbol, List<String> list) {...}
```
Общее правило: ArrayList нужно использовать через List, HashMap- через Map и т.д.  
Это позволяет пользоваться преимуществами полиморфизма.

Да, бывают ситуации, когда, например, с LinkedList нужно работать именно как с LinkedList, а не с List. 
Но это уже нюансы
```
"Если вы выработаете привычку использовать в качестве типов интерфейсы, ваша программа будет гораздо более гибкой" - Блох.
```
*"Java. Эффективное программирование" гл.9.8*

**5. Нарушение инкапсуляции**. Публичным должен быть только метод `main()`. Остальные методы и поля здесь должны быть приватными.

**6. Приоритет- читаемость кода, а не экономия строк** 

Расположи печать в две строки, сразу будет видно, как в итоге распечатается текст
```
System.out.println("Вы проиграли.\nБыло загадано слово: " + wordForGuess);

//ПРАВИЛЬНО:
System.out.println("Вы проиграли);
System.out.println("Было загадано слово: " + wordForGuess);
```

**7. class ChoiceOfWord**

- Имя файла для чтения метод должен получать во входящие аргументы

Сейчас имя читаемого файла жестко прописано в классе- это делает класс неуниверсальным
```
public static String randomWord() {
  InputStream is = classLoader.getResourceAsStream("singular.txt");
  //...
}

//ПРАВИЛЬНО:
public static String randomWord(String filename) {
  InputStream is = classLoader.getResourceAsStream(filename);
  //...
}
```

- Не используй фиксированный размер для массива слов

Сейчас для хранения прочитанных из файла слов, создается массив с заранее заданным размером
```
String[] arrayWords = new String[67_600]; // number of word in the file
for (int i = 0; i < arrayWords.length; i++) {
  arrayWords[i] = scan.nextLine();
}
```
Это неправильно, потому что при добавлении/удалении слов из файла, этот размер перестанет быть актуальным и приведет к сбою работы программы.

Например, если сейчас из файла "singular.txt" удалить пару десятков слов, то программа из-за этого аварийно прекратит работу:
```
Exception in thread "main" java.util.NoSuchElementException: No line found
	at java.base/java.util.Scanner.nextLine(Scanner.java:1677)
```

Слова нужно считывать не в массив, а в `List`.  
У листа есть то преимущество, что ему не нужно устанавливать фиксированный размер- при добавлении новых записей он автоматически увеличивается в размере. 
Читай файл построчно, пока он не закончится, а его строки-слова записывай в `List`.

- Этот метод не знает, зачем у него запрашивают слово- для угадывания(ForGuess), или для чего-то еще. 
Он знает только то, что что должен вернуть случайное слово из файла 
```
public static String randomWord() {
  //...
  String wordForGuess = arrayWords[rand.nextInt(arrayWords.length)];
  return wordForGuess;
}

//ПРАВИЛЬНО:
String word = arrayWords[rand.nextInt(arrayWords.length)];
return word;

//ЕЩЕ ЛУЧШЕ- СРАЗУ ВЕРНУТЬ:
return arrayWords[rand.nextInt(arrayWords.length)];
```

- Реакция на ошибки

Если по указанному адресу не будет найден файл со словами, то программа аварийно вылетит и напечатает непонятные для юзера сообщения в консоль
```
Введите 1, если да, или 0, если нет.

1

<тут печатается тридцать четыре пустых строки (?!)>

Файл не найден!  <-- ЧТО ЮЗЕР ДОЛЖЕН СДЕЛАТЬ С ЭТОЙ ИНФОРМАЦИЕЙ?
Exception in thread "main" java.lang.NullPointerException  <-- ЧТО ЮЗЕР ДОЛЖЕН ЗДЕСЬ ПОНЯТЬ?
```
Здесь должна быть другая реакция на отсутствие файла со словами.  
Нужно сказать юзеру, что файл со словами по указанному пути открыть не удалось и поэтому работа программы будет завершена. 

Причем, нужно не просто написать, что какой-то неназванный файл не найден.  
А написать абсолютный путь к ненайденному файлу, чтобы юзер знал, где конкретно ему нужно искать этот файл: `c:/..../singular.txt`.

После этого корректно завершить работу программы без вываливания красных эксепшенов в консоль.

**8. class PrintHangman**

+ 👍 Хорошо, что картинки и метод их распечатки вынесены в отдельный класс.
Это разгружает класс с логикой `Game` от картинок.

- Используй многострочные строки
```
String[] positions = {
    "          +---+\n" +
    "          |   |\n" +
    "              |\n" +
    "              |\n" +
    "              |\n" +
    "        =========",
    //...
}

//ПРАВИЛЬНО:
String[] positions = {
    """
      +---+
      |   |
          |
          |
          |
    =========
    """,
    //...
}
```
Многострочные строки(текстовые блоки) имеются в java начиная с 13 версии.

- Картинки должны храниться в массиве-константе класса. 
Сейчас картинки хранятся в массиве-переменной метода
```
public class PrintHangman {
  public static void draw(int countOfErrors) {
    String[] positions = {
        //картинки
    };
    //...
  }
}

//ПРАВИЛЬНО:
public class HangmanRenderer {
  private static final String[] PICTURES = {
        //картинки
    };

  public static void draw(int countOfErrors) {
    //...
  }
}
```
Если массив картинок хранить в методе, то этот объект(а массив это объект) будет пересоздаваться в памяти каждый раз при вызове метода.  
Если картинки хранить в константе- они создадутся в памяти только один раз.

- Всё намного проще
```
switch (countOfErrors) {
  case 0, 1, 2, 3, 4, 5, 6 -> System.out.println(positions[countOfErrors]);
  default -> System.out.println("Некорректное значение");
}

//ПРАВИЛЬНО:
String picture = positions[countOfErrors];
System.out.println(picture);
```
Во втором случае, если значение `countOfErrors` будет некорректным, 
то вместо распечатки сообщения "Некорректное значение", выкинет исключение.
Нам это и надо, потому что если алгоритм попытается распечатать картинку с несуществующим номером, 
то это баг, который нужно максимально быстро выявить и устранить.

**9. class Game**

- Магический символ: '*'

- Делай программу проще и понятнее, вводи вспомогательные методы
```
if ((letter >= 'а' && letter <= 'я') || (letter >= 'А' && letter <= 'Я')) {...}

//ПРАВИЛЬНО:

if (isRussianLetter(letter)) {...}

private boolean isRussianLetter(char letter) {
  char c = Character.toLowerCase(letter);
  return c >= 'а' && c <= 'я' || c == 'ё';   
}
```

- Ты уже находишься внутри Game
```
if (Game.inputLetterIsCorrect(attemptGuess)) {...}

//ПРАВИЛЬНО:
if (inputLetterIsCorrect(attemptGuess)) {...}
```

- Сразу выполняй return
```
boolean letterFound(char symbol, char[] array, char[] secretArray) {
  boolean found = false;
  //...
  if (array[i] == symbol) {
    found = true;
  }
  return found;
}

//ПРАВИЛЬНО:
boolean letterFound(char symbol, char[] array, char[] secretArray) {
  //...
  if (array[i] == symbol) {
    return true;
  }
  return false;
}
```

- Код должен читаться, как открытая книга. Создавай вспомогательные методы
```
while (!wordIsGuessed(wordWithStars)) {
  //...
  if (counterOfMistake == NUMBER_OF_MISTAKE) {
    System.out.println("Вы проиграли.\nБыло загадано слово: " + wordForGuess);
    //...
  }
  if (wordIsGuessed(wordWithStars)) {
    System.out.println("Вы выиграли! Поздравляю!!!\n");
  }
}

//ПРАВИЛЬНО:
while(!isGameOver()) {
  //...
  if(isLose()) {
    printLoseMessage();
  } else if(isWin()) {
    printWinMessage();
  }
}

boolean isWin() {
  return wordIsGuessed(wordWithStars);  
}

boolean isLose() {...}

boolean isGameOver() {
  return isWin() || isLose();  
}
```

- Закомментированный код- антипаттерн "Лодочный якорь". 
Этот код никогда уже не понадобится
```
//static void gameLoop() {
//    String wordForGuess = ChoiceOfWord.randomWord();
```
Не превращай программу в мусорку. 
Если что-то хочешь сохранить на память, сделай коммит в гите с соответствующим комментарием.  
Правильное использование комментариев: *"Чистый код", гл.4*

- При прочих равных, бери данные из окружения класса

Если одни и те же данные активно используются в разных методах класса, то эти данные нужно не перекидывать туда-сюда между методами, а сделать их полями класса
```
static boolean letterFound(char symbol, char[] array, char[] secretArray) {
  //...
}

//ЗДЕСЬ ЛУЧШЕ:
private static char[] word;  
private static char[] secretWord;

static boolean letterFound(char letter) {
  //берет данные из окружения: word, secretWord
}
```
Да, вопрос о том, в каких случаях и когда поля должны браться из окружения, а когда передаваться между методами, не так прост и дискуссионен.
Об этом нужно думать отдельно в каждом конкретном случае.

- Побочный эффект.

Судя по названию, метод должен только искать букву(в слове).  
Но кроме этого метод неявно меняет входящие данные- это и есть побочный эффект
```
static boolean letterFound(char symbol, char[] array, char[] secretArray) {
  int placeOfLetter;
  boolean found = false;
  for (int i = 0; i < array.length; i++) {
    if (array[i] == symbol) {
      placeOfLetter = i;
      secretArray[placeOfLetter] = symbol;  <-- ПОБОЧНЫЙ ЭФФЕКТ
      found = true;
    }
  }
  return found;
}
```

Смотри, как работает этот побочный эффект:
```
public class Test {
  public static void main(String[] args) {
    char[] word = "HELLO".toCharArray();
    char[] mask = "HE***".toCharArray();

    System.out.println("До вызова метода Game.letterFound(...) : ");
    System.out.println(word);
    System.out.println(mask);

    Game.letterFound('L', word, mask);

    System.out.println("\nПосле вызова метода Game.letterFound(...) : ");
    System.out.println(word);
    System.out.println(mask);
  }
}

//РЕЗУЛЬТАТ:
До вызова метода Game.letterFound(...) : 
HELLO
HE***

После вызова метода Game.letterFound(...) : 
HELLO
HELL*
```
Как видим, метод неявно внёс изменения в массив, который ему передали в качестве входящего аргумента.  
Так и было задумано? Конечно. 
Но такое название метода и наличие возвращаемого значения вводят в заблуждение. 
Посторонний пользователь этого метода не будет ожидать изменений передаваемых аргументов.

Придерживайся правила: если метод вносит изменения во входящие аргументы, то этот метод должен быть `void`.

Метод `letterFound(...)` нужно разделить на несколько, каждый из которых будет выполнять только одну задачу.  
Один метод должен проверять наличие буквы в слове.  
Второй метод- открывать эту букву в маске.  

*Мартин "Чистый код", гл.3, "Избавьтесь от побочных эффектов"*  
*Фаулер "Рефакторинг", гл.6, "Извлечение метода"*  

- Разделяй команды и запросы

Этот метод должен или выполнить действие. То есть добавить букву в список введенных букв.  
Или выполнить запрос. То есть, сказать, есть ли эта буква в списке введенных букв.  
Но не то и другое вместе
```
static boolean enterLetterAgain(char symbol, ArrayList<String> list) {
  if (list.contains(String.valueOf(symbol))) {
    return true;  <-- ВЫПОЛНЕНИЕ ЗАПРОСА
  } else {
    list.add(String.valueOf(symbol));  <-- ВЫПОЛНЕНИЕ КОМАНДЫ
    return false;  <-- ВЫПОЛНЕНИЕ ЗАПРОСА
  }
}
```
*Мартин, "Чистый код", гл.3, "Разделение команд и запросов"*   

**10. class Main**, содержит точку входа main

+ 👍 Только запускает Игру, в данном случае, через диалог "Играть или выйти". Это хорошо.

- Избыточно, используй вечный цикл
```
boolean play = true;
while (play) {
  System.out.println("Сыграем в \"Виселицу\"?");
  //...
  case "0" -> {
    play = false;
    return;
  }
  //...
}

//ПРАВИЛЬНО:
while (true) {
  System.out.println("Сыграем в \"Виселицу\"?");
  //...
  case "0" -> return;
  //...
}
```

## ВЫВОД

Для развития навыков процедурной декомпозиции, посмотреть стрим Сергея [Крестики-нолики в процедурном стиле](https://www.youtube.com/watch?v=PPikj1qHxrA)  
Подробное объяснение, как делать эту программу в процедурном и ООП стилях, есть у Сергея в расширенных материалах.

n.140(257)  
#ревью #виселица 