https://github.com/AlenaBormatova/hangman  
[Alena]

Игра в смешанном стиле: процедурный/ООП.

## НЕДОСТАТКИ РЕАЛИЗАЦИИ

1. Интригует. Хорошо, сыграю, но тогда объясни правила 
```
Приветствую Вас. Вы меня не знаете, но я Вас знаю. Я хочу поиграть с Вами в Виселицу.
Начать новую игру?
1 - да / 0 - нет
```

2. Лишние неудобства для пользователя. 
Если программе нужны буквы определенного регистра, то она сама должна переводить буквы в этот регистр, а не создавать юзеру сложности
```java
Введите букву: 
Щ
Введите маленькую букву кириллицы.
```

## ХОРОШО

+ 👍 Игра запускается
+ 👍 Можно ввести только одиночную букву русского алфавита
+ 👍 Есть список введенных букв

## ЗАМЕЧАНИЯ

**1. Нейминг**

- Если название класса имеет постфикс "List", то класс должен реализовывать стандартный интерфейс List.  
Примеры: ArrayList, LinkedList.  
Этот класс не реализует List
```java
class WordList

//ПРАВИЛЬНО(в контексте выполняемых действий):
class Dictionary
```

- Название должно объяснять, что делает метод.

Я вижу, что метод проверяет символ на принадлежность к русской букве.  
Я не вижу, чтобы он дополнительно проверял символ еще на какую-то валидность
```java
private boolean isValidRussianLetter(char ch) {
  return (ch >= 'а' && ch <= 'я') || ch == 'ё';
}

//ПРАВИЛЬНО:
private boolean isRussianLetter(char ch) {
  return (ch >= 'а' && ch <= 'я') || ch == 'ё';
}
```
*Oracle Java code conventions, part."Naming conventions"*  
*Мартин, "Чистый код", гл.2*  
*Ютуб, Немчинский "Как называть переменные, методы и классы?"*

**2. Текст в exception всегда должен быть на английском языке**
```java
throw new IllegalStateException("Словарь пуст: " + fileName);
```
Исключение это не просто телеграмма, которая летит сквозь слои. 
У exception особое назначение- если исключение вылетит и не будет перехвачено внутри программы, то аварийно прекратит выполнение программы.
Тогда на экране будет распечатано сообщение эксепшена, и это сообщение должно быть понятно сисадмину в любой точке планеты. 
А значит, сообщение должно быть на английском. 
Интерпритация исключения и перевод его на локальный язык должно происходить там, где это соответствует архитектуре программы или не происходить вовсе, если исключение не планируется перехватывать.

**3. Форматирование строк**

Если нужно печатать или создавать строку с более, чем одним подстановочным значением или значение вставляется внутрь сообщения, используй форматирование- тогда сразу будет виден весь шаблон
```java
System.out.println(START_COMMAND + " - да / " + EXIT_COMMAND + " - нет");
System.out.println("\nОшибок: " + errors + " из " + MAX_ERRORS);

//ПРАВИЛЬНО:
System.out.printf("%s - да / %s - нет  %n", START_COMMAND, EXIT_COMMAND);
System.out.printf("\nОшибок: %d из %d  %n", errors, MAX_ERRORS);
```

**4. Используй классы через их интерфейсы**
```java
TreeSet<Character> usedLetters = new TreeSet<>();

//ПРАВИЛЬНО:
Set<Character> usedLetters = new TreeSet<>();
```
Общее правило: ArrayList нужно использовать через List, HashMap- через Map и т.д.  
Это позволяет пользоваться преимуществами полиморфизма.

Да, бывают ситуации, когда, например, с LinkedList нужно работать именно как с LinkedList, а не с List. 
Но это уже нюансы.  
*"Java. Эффективное программирование", изд.3, гл.9.8*
```
"Если вы выработаете привычку использовать в качестве типов интерфейсы, ваша программа будет гораздо более гибкой" - Блох.
```

**5. class WordList**

- Нарушение конвенции кода. Константы должны стоять выше остальных полей.

- Количество букв в слове нужно не хардкорить в коде, а принимать в конструктор- так же, как сейчас в конструктор принимается путь к файлу
```java
private static final int MIN_WORD_LENGTH = 5;
```
Нет никаких оснований хардкодить это число прямо в классе.  
Класс должен быть универсальным и доступным для переиспользования в других проектах.  
Возможно, где-то в других проектах тебе тоже понадобится класс, который читает слова из файла, но требования к длине слов там будет другим.

- Проверямые исключения перехватывай в точке их возникновения и заменяй на непроверяемые
```java
private List<String> initializeFromFile(String fileName) throws IOException {...}

//ПРАВИЛЬНО:
private List<String> initializeFromFile(String fileName) {
  try {
    rawLines = Files.readAllLines(Paths.get(fileName), StandardCharsets.UTF_8);
  } catch (IOException e) {
    throw new IllegalArgumentException(FAIL_MESSAGE, e);
  }
  //...
}
```
Изучи различия проверяемых и непроверяемых исключений.  
*"ЧК", гл.7, "...проверяемые исключения"*

- Реакция на ошибки

Если по указанному адресу не будет найден файл со словами, то программа аварийно вылетит и напечатает непонятные для юзера сообщения в консоль
```java
Exception in thread "main" java.lang.RuntimeException: java.nio.file.NoSuchFileException: words.txt1
	at WordList.initializeFromFile(WordList.java:24)
Caused by: java.nio.file.NoSuchFileException: words.txt
```
Здесь должна быть другая реакция на отсутствие файла со словами.  
Нужно сказать юзеру, что файл со словами по указанному пути открыть не удалось и поэтому работа программы будет завершена. 

Причем, путь нужно указать не относительный, как у тебя(`words.txt`).  
А абсолютный, чтобы юзер знал, где конкретно ему нужно искать файл: `c:/..../words.txt`.

После этого корректно завершить работу программы без вываливания красных эксепшенов в консоль.

**6. class HangmanDrawer**

+ 👍 Хорошо, что картинки и метод их печати вынесены в отдельный класс. Это разгружает классы с логикой от графики.

- Нарушение конвенции кода. Методы должны стоять ниже полей.

- Неправильная реакция на ошибки.

Есть три основных реакции на ошибки: игнорирование, исправление, падение.  
Здесь выбрано исправление- при получении некорректного номера картинки, метод подгоняет номер под последний корректный
```java
public static void draw(int errors) {
  int index = Math.min(errors, HANGMAN_STATES.length - 1); <-- ЕЛИ ПРИДЕТ НЕКОРРЕКТНЫЙ НОМЕР- СТАВИТ БЛИЖАЙШИЙ КОРРЕКТНЫЙ
  System.out.print(HANGMAN_STATES[index]);
}
```

Правильная реакция должна быть другой, нужно сделать падение:
```java
public static void draw(int errors) {
  System.out.print(HANGMAN_STATES[errors]);  <-- КИНЕТ ИСКЛЮЧЕНИЕ, ЕСЛИ КАРТИНКИ С ТАКИМ НОМЕРОМ НЕТ В МАССИВЕ
}
```
Потому что запрос картинки с неправильным номером означает, что в алгоритме игры есть баг.  
Правильно работающий алгоритм не будет пытаться распечатать некорректные данные.

Этот баг нужно не маскировать- это приведет только к тому, что игра будет происходить некорректно.  
Баг нужно максимально быстро выявить и устранить.

**7. class HangmanGame**

+ 👍 Используются константы.

+ 👍 Хорошо, что класс принимает эту зависимость в конструктор, а не создает ее внутри себя. 
Благодаря этому, класс может работать со словарями, в которых могут быть прочитаны разные файлы
```java
public HangmanGame(WordList wordList, ...)
```

- Диалог "играть или выйти" должен происходить не здесь, а на уровень выше.
```java
void runMainMenu() {
  System.out.println("Начать новую игру?");
  System.out.println(START_COMMAND + " - да / " + EXIT_COMMAND + " - нет");
  //...
}
```
Потому что если юзер при первом заходе выберет "нет", то значит экземпляр этого класса создался зря.  

Дополнительное упражнение на это замечание: создай в проекте два класса `Main`.  
По запуску одного из них, игра должна запускаться через диалог "играть или выйти".  
По запуску другого- сразу запуститься игра и сыграть только один раз. 

- При прочих равных, бери данные из окружения класса.

Если одни и те же данные используются в разных методах класса, то эти данные нужно не перекидывать туда-сюда между методами, а сделать их полями класса
```java
private int processLetter(String word, char[] answer, TreeSet<Character> usedLetters, char letter, int currentErrors) {...}

//ЗДЕСЬ ЛУЧШЕ:
private String word;  //эти данные нужны на протяжении всего игрового процесса
private char[] answer;
private Set<Character> usedLetters = new TreeSet<>();
private int currentErrors;

private int processLetter(char letter) {...}
```
Да, вопрос о том, в каких случаях и когда поля должны браться из окружения, а когда передаваться между методами, дискуссионен и не так прост.
Об этом нужно думать отдельно в каждом конкретном случае.

- Разделяй команды и запросы.

Метод должен или выполнить команду, или ответить на запрос. Но не то и другое вместе
```java
private boolean isGameOver(...) {
  if (errors >= MAX_ERRORS) {
    displayGameState(answer, usedLetters, errors);  <-- ВЫПОЛНЕНИЕ КОМАНДЫ
    System.out.println("Вы проиграли! Загаданное слово: " + word);  <-- ВЫПОЛНЕНИЕ КОМАНДЫ
    return true;  <-- ВЫПОЛНЕНИЕ ЗАПРОСА
  }
  if (isWordGuessed(answer)) {
    System.out.println("Поздравляем! Вы отгадали слово: " + word);  <-- ВЫПОЛНЕНИЕ КОМАНДЫ
    return true;  <-- ВЫПОЛНЕНИЕ ЗАПРОСА
  }
  return false;  <-- ВЫПОЛНЕНИЕ ЗАПРОСА
}
```
Метод с названием `isGameOver()` должен только сказать, закончилась игра или нет. Примерно, вот так:
```java
private boolean isGameOver() {
  return isWin() || isLose();    
```

Та же ситуация с другими методами:
```java
//ЗАПРОС: вернуть количество ошибок
//КОМАНДА: <сделать всякое разное>
int processLetter(String word, char[] answer, TreeSet<Character> usedLetters, char letter, int currentErrors)
```
*Мартин, "Чистый код", гл.3, "Разделение команд и запросов"*

- Вводи вспомогательные методы.

Этот комментарий призван объяснить плохой код. Вместо комментария, введи вспомогательный метод, который своим названием будет объяснять алгоритм
```java
if (word.indexOf(letter) != -1) { // все вхождения буквы
  //...
}

//ПРАВИЛЬНО:
if (isWordLetter(letter)) {...} 
```
*Мартин, "ЧК", гл.4*  

- Если в названии есть приставка "And", значит с методом что-то не так
```java
private char readAndValidateLetter(TreeSet<Character> usedLetters)
```
Либо метод делает больше одного дела.  
Либо неправильно сформулировано, что делает метод.

Лично я бы разделил этот метод, но согласен на простое переименование:
```java
//ввод неиспользованной русской буквы
private char inputUnusedRussianLetter(TreeSet<Character> usedLetters)
```

**8. class Main**, содержит точку входа main

+ 👍 Только создает и запускает Игру, это хорошо.

- Диалог "играть или выйти" должен происходить здесь.

## АРХИТЕКТУРА

Программа в ООП стиле должна быть декомпозирована по правилам ООП и использовать ООП подход для реализации типичных задач.
Здесь есть признаки и процедурного и ООП стилей.

Признаки ООП:
+ Программа разделена на классы, каждый из которых описывает отдельную сущность: общая игровая логика, словарь и рендерер.
+ Отдельный класс с точкой входа main, который только создает и запускает Игру.

Признаки процедурного стиля: 
+ Не выявлены и не сделаны в виде классов менее очевидные, чем Словарь и Рендерер, сущности. Напимер, Секретное слово. 
Значит нет системного понимания объектно-ориентированной декомпозиции.

## ВЫВОД

В целом, нормально.

Сравнить различия между ООП и процедурным стилями:  
Стрим Сергея [Крестики-нолики в процедурном стиле](https://www.youtube.com/watch?v=PPikj1qHxrA)  
Мой стрим [Крестики-нолики в ООП стиле](https://t.me/zhukovsd_it_chat/53243/187097)

Подробное объяснение, как делать эту программу в процедурном и ООП стилях, есть у Сергея в расширенных материалах.

n.148(281)  
#ревью #виселица 