https://github.com/skipaq/HangmanGame  
[Skp]

Игра написана в смешанном силе: процедурный/ООП.

## НЕДОСТАТКИ РЕАЛИЗАЦИИ

1. Игра не запускается
```java
Начать новую игру? Да / Нет: 
да
Игра началась!
java.lang.NullPointerException
	at java.base/java.io.Reader.<init>(Reader.java:280)
	at java.base/java.io.InputStreamReader.<init>(InputS
```

2. Неправильная последовательность в порядке отображения введенных букв: букву 'ц' я ввел второй, но в распечатке она стала первой. 
При этом введенная третьей буква 'к' в распечатке стала на третью позицию
```
Введите букву: 
й
Введенные буквы: [й]

Введите букву: 
ц
Введенные буквы: [ц, й]

Введите букву: 
к
Введенные буквы: [ц, й, к]
```
Введенные буквы должны размещаться по алфавиту(й, к, ц) либо в порядке ввода(й, ц, к).

## ХОРОШО

+ 👍 Можно ввести только одиночную букву русского алфавита
+ 👍 Есть список введенных букв

## ЗАМЕЧАНИЯ

Так как проект состоит из пяти классов, то предположу, что планировалось написать программу в ООП стиле.  
Поэтому критика классов будет с точки зрения ООП.

**1. Нейминг**

- Не нужно всем классам добавлять префикс "Game"- если одна и та же приписка есть во всех классах, это обесценивает ее информативную ценность.  
Сейчас эту приписку содержат 4 класса из 5  
```java
class GameDictionary
class GameLogic
class GameSettings
class GameStarter
```
Если отрезать от этих названий "Game", это не убавит информативности.

- В названии переменных не пиши тип данных, к которым они относится. И вообще не употребляй венгерскую ноттацию.  
Название переменной должно отвечать на вопрос что хранит переменная, а не как хранит
```java
List<String> wordsList = createDictionary();
char enteredChar = InputValidation.userInputChar(scan);

//ПРАВИЛЬНО:
List<String> words = createDictionary();
char letter = InputValidation.userInputChar(scan);
```

- Лучше называть сущности более персонализировано. Примеры: Reader, а не Reading; Manager, а Menagement; Renderer, а не Rendering
```java
class InputValidation

//ПРАВИЛЬНО:
class InputValidator
```

- Название метода должно быть глаголом
```java
public void game()  <-- ЭТО НЕ ГЛАГОЛ
```

- Если метод называется "search", то он должен возвращать результаты поиска. 
Если метод `void`, то он не должен так называться. 
Название должно объяснять, что делает метод, это название только путает
```java
void searchForEnteredChar(char c)
```

- КОНСТАНТЫ_НУЖНО_ПИСАТЬ_СТИЛЕМ_UPPER_SNAKE
```java
public static final String alphabet = GameSettings.ALPHABET;

//ПРАВИЛЬНО:
public static final String ALPHABET = GameSettings.ALPHABET;
```
*Oracle Java code conventions, part."Naming conventions"*  
*Мартин, "Чистый код", гл.2*  
*Ютуб, Немчинский "Как называть переменные, методы и классы?"*

**2. Нарушение DRY**, магические буквы, числа, слова. Вводи константы 
```java
System.out.println("Начать новую игру? Да / Нет: ");
if (userInput.equals("да")) {...} 
if (userInput.equals("нет"))  {...}
System.out.println("Некорректный ввод! Введите ДА либо НЕТ!");

//ПРАВИЛЬНО:
private final static String START = "да";
private final static String QUIT = "нет";  

System.out.println("Начать новую игру? %s / %s:  %n", START, QUIT);
if (userInput.equals(START)) {...} 
if (userInput.equals(QUIT))  {...}
System.out.println("Некорректный ввод! Введите %s либо %s!  %n", START, QUIT);
```
*Фаулер, "Рефакторинг", гл.8, "Замена магического числа символической константой"*  
*refactoring.guru "Замена магического числа символьной константой"*

**3. Текст в exception** всегда должен быть на английском языке
```java
throw new IllegalStateException("Словарь пуст");
```
Исключение это не просто телеграмма, которая летит сквозь слои. 
У exception особое назначение- если исключение вылетит и не будет перехвачено внутри программы, то аварийно прекратит выполнение программы.
Тогда на экране будет распечатано сообщение эксепшена, и это сообщение должно быть понятно сисадмину в любой точке планеты. 
А значит, сообщение должно быть на английском. 

Интерпритация исключения и перевод его на локальный язык должно происходить там, где это соответствует архитектуре программы или не происходить вовсе, если исключение не планируется перехватывать.

**4. Не создавай строки через new**

Никогда не создавай строки не через `new`- это связано с особеностями устройства класса String.  
Создавай строки через специально предусмотренные для этого методы
```java
System.out.println("Есть такая буква! Текущее слово: " + new String(maskWord));

//ПРАВИЛЬНО:
System.out.println("Есть такая буква! Текущее слово: " + String.valueOf(maskWord));
```

**5. Форматирование строк**

Если нужно печатать или создавать строку с более, чем одним подстановочным значением или значение вставляется внутрь сообщения, 
то используй форматирование- тогда сразу будет виден весь шаблон
```java
System.out.println("Нет такой буквы! Ошибок: " + mistakesCounter + "/" + GameSettings.MAX_MISTAKES + ". " +
    "Текущее слово: " + new String(maskWord));

//ПРАВИЛЬНО:
System.out.printf("Нет такой буквы! Ошибок: %d/%d. Текущее слово: %s  %n" + mistakesCounter, GameSettings.MAX_MISTAKES, String.valueOf(maskWord));
```

**6. Используй классы через их интерфейсы**
```java
private HashSet<Character> enteredChars = new HashSet<>();

//ПРАВИЛЬНО:
private Set<Character> enteredChars = new HashSet<>();
```
Общее правило: ArrayList нужно использовать через List, HashMap- через Map и т.д.  
Это позволяет пользоваться преимуществами полиморфизма.

Да, бывают ситуации, когда, например, с LinkedList нужно работать именно как с LinkedList, а не с List. 
Но это уже нюансы.  
*"Java. Эффективное программирование", изд.3, гл.9.8*
```
"Если вы выработаете привычку использовать в качестве типов интерфейсы, ваша программа будет гораздо более гибкой" - Блох.
```

**7. Реакция на ошибки** 

Если по указанному адресу в `class GameDictionary` не будет найден файл со словами, то программа аварийно вылетит и напечатает непонятные для юзера сообщения в консоль
```java
java.lang.NullPointerException
	at java.base/java.io.Reader.<init>(Reader.java:280)
    //еще миллион непонятных юзеру строк
    Exception in thread "main" java.lang.IllegalStateException: Словарь пуст 
```
Причем, сообщения обманывают- проблема не в том, что словарь пуст. 
А в том, что файл со словами не обнаружен по указанному пути.

Здесь должна быть другая реакция на отсутствие файла со словами.  
Нужно сказать юзеру, что файл со словами по указанному пути открыть не удалось и поэтому работа программы будет завершена. 

Причем, путь нужно указать абсолютный, чтобы юзер знал, где конкретно ему нужно искать файл: `c:/..../words.txt`.  
После этого корректно завершить работу программы без вываливания красных эксепшенов в консоль.

**8. class GameDictionary**

- Использование статических изменяемых полей(не констант) в программе, которая притендует на ООП, это абсолютное зло
```java
public static List<String> wordsList = createDictionary();
```

- Нарушение инкапсуляции. 

Все операции с хранимыми данными должны выполняться клиентами не напрямую, а через методы.  
Например, через геттеры и сеттеры
```java
public static List<String> wordsList = createDictionary();
```
*Блох "Java. Эффективное программирование", изд.3, гл.4.2.*
```
"Используйте в открытых классах методы доступа, а не открытые поля" - Блох.
```

- Путь к файлу класс должен получать в конструктор или в данном случае- в метод `createDictionary()`. 

Иначе класс становится неуниверсальным.
Допустим, в программе будет несколько разных текстовых файлов- на разных языках или по разным темам. 
Тогда вместо нескольких специализированных классов Словаря, где имя файла словаря будет жестко прописано в каждом из классов, 
можно будет использовать один универсальный класс Словарь.

- Нарушение принципа минимального открытого интерфейса.

В классе поиска пути должен быть только один публичный метод- `getRandomWord()`. 
Метод `createDictionary()` используется только внутри этого класса и поэтому должен быть private.  
Клиент должен видеть только те методы, которые нужны для выполнения поставленной задачи.  
*Вайсфельд "Объектно-ориентированный подход", гл.5, "Минимальный открытый интерфейс"*

- Зачем происходит начальная загрузка слов из файла, если она потом происходит каждый раз при получении случайного слова?
```java
public static List<String> wordsList = createDictionary(); <-- один раз читает файл на старте

  public static List<String> createDictionary() {...}

  public static String getRandomWord() {
    List<String> wordList = createDictionary(); <-- каждый раз читает файл на старте
    //...
  }
}
```

- Неправильная реакция на ошибки.

Есть три основных реакции на ошибки: игнорирование, исправление, падение.
Здесь выбрано игнорирование- просто распечатывается текст эксепшена
```java
public static List<String> createDictionary() {
  try (...) {
     //чтение файда    
  } catch (Exception e) {
    e.printStackTrace();  <-- ИГНОРИРОВАНИЕ
  }
  return wordsList;
}
```

Игнорирование здесь не помогает- как я писал выше, это все равно приводит к аварийному прекращению работы программы.

- Некорректный возврат случайного слова- если в списке будет только одно слово, то вылетает исключение
```java
public static List<String> createDictionary() {
  List<String> wordsList = new ArrayList<>();
  //я тут отключил логику чтения файла
  wordsList.add("привет");
  return wordsList;
}

//РЕЗУЛЬТАТ:
Начать новую игру? Да / Нет: 
да
Игра началась!
Exception in thread "main" java.lang.IndexOutOfBoundsException: Index 1 out of bounds for length 1
	at java.base/jdk.internal.util.Preconditions.outOfBounds(Preconditions.java:100)
```
Алгоритм должен корректно отрабатывать список слов, который состоит только из одного слова.

**9. class GameSettings**

Проверка соответствия буквы алфавиту сейчас- индусский код.

Сейчас соотвествие буквы русскому алфавиту происходит путем поиска буквы в строке. Это очень избыточный алгоритм
```java
public static final String ALPHABET = "абвгдеёжзийклмнопрстуфхцчшщъыьэюя";

//ПРАВИЛЬНО:
private static boolean isRussianLetter(char symbol) {
  symbol = Character.toLowerCase(symbol);
  return (symbol >= 'а' && symbol <= 'я') || symbol == 'ё';
}
```
Почитай про то, как символы хранятся в таблице ASCII/юникод.

**10. class GameLogic**

- Приоритет- читаемость кода.

Иногда лучше писать код в несколько строк. Приоритет- читаемость, а не экономия строчек
```java
System.out.println("Загаданное слово:\n" + new String(maskWord));
```
Здесь лучше написать в две строки. 
Тогда сразу будет понятно, что текст распечатается в две строки и не придется в голове проводить реконсрукцию событый с учетом "\n"
```java
System.out.println("Загаданное слово:");
System.out.println(String.valueOf(maskWord););
```

- Используй многострочные строки
```java
System.out.println(
   "  ____ \n" +
   "/     | \n" +
   "|     O \n" +
   "|        \n" +
   "|        \n" +
  "/\\");

//ПРАВИЛЬНО:
System.out.println("""
     ____ 
   /     | 
   |     O 
   |       
   |       
  /\\
  """);
```
Многострочные строки(текстовые блоки) имеются в java начиная с 13 версии.

- Печать картинки виселицы через switch-case или if-elseif - индусский код. 

Картинки нужно хранить в статическом массиве и печатать по номеру.  
Например, так
```java
public class HangmanRenderer {
  private static final String[] PICTURES = {
    """
    -----   
    |       
    |       
    |       
    |       
    ------- 
    """,
   """
     -----   
     |   |   
     |   O   
     |       
     |       
     ------- 
     """
   ,
   // more pics
  };

  public void render(int numPicture) {  
    String picture = PICTURES[numPicture];
    System.out.println(picture);
  }
}
```
Желательно картинки и метод распечатки вынести в отдельный класс, чтобы в основном классе не смешивать логику и графику.

- Вводи вспомогательные методы, делай программу более простой и понятной
```java
while (!isWordEquals() && mistakesCounter < GameSettings.MAX_MISTAKES) {...}

//ПРАВИЛЬНО:
while(!isGameOver()) {...}
```
Еще советую сделать методы: `isLose()`, `isWon()`.

- Соблюдай правил одной операции- один метод должен делать одно дело.

Если в методе есть какие-то обособленные смысловые блоки, выноси их во вспомогательные методы
```java
private void searchForEnteredChar(char c) {
  //много кода  
  boolean foundChar = false;
  for (int i = 0; i < randomWord.length; i++) {
    if (randomWord[i] == c) {
      maskWord[i] = c;
      foundChar = true;
    }
  } 
  if (foundChar) {
    System.out.println("Есть такая буква! Текущее слово: " + new String(maskWord));
  } else {...}
  //еще много кода
}

//ПРАВИЛЬНО:
private void searchForEnteredChar(char c) {
  //много кода  
  if(isWordLetter(c)) {
    openLetter(c);
    System.out.println("Есть такая буква! Текущее слово: " + String.valueOf(maskWord));
  } else {...}
  //еще много кода
}
```

- Нарушение DRY. Многочисленное дублирование текстовых сообщений. Общие данные выноси в константы
```java
private void displayMaskedCurrentWord() {
  System.out.println("Загаданное слово:\n" + new String(maskWord));  <-- "Загаданное слово"
}

private void displayGameResult() {
  System.out.println("\nПОРАЖЕНИЕ! Загаданное слово было: " + ...);  <-- "Загаданное слово"
  //...
}

private void searchForEnteredChar(char c) {
  //...
  System.out.println("Есть такая буква! Текущее слово: " + new String(maskWord));  <-- "Текущее слово"
  System.out.println("Нет такой буквы! Ошибок: " + ... + "Текущее слово: " + new String(maskWord));  <-- "Текущее слово"
}
```

**11. class GameStarter**, содержит точку входа main

+ 👍 Тут все прерасно, олько создает и запускает GameLogic, это хорошо.  
*"ЧК", гл.11, "Отделение конструирования системы от использования"*

+ 👍 Диалог "играть или выйти" происходит здесь, это хорошо.

## АРХИТЕКТУРА

Если планировалось написать эту программу не в ООП, то с точки зрения процедурного стиля, претензий к архитектуре нет.  
Хотя в этом случае можно было бы сделать всё проще.

Если планировался объектно-ориентированный стиль стиль, то программа в ООП стиле должна быть декомпозирована по правилам ООП и использовать ООП подход для реализации типичных задач.  
Эта реализация имеет признаки и процедурного и объектно-ориентированного стилей.

Признаки ООП: 
+ Программа разделена на классы, каждый из которых описывает отдельную сущность: общая игровая логика, словарь и т.д.
+ Отдельный класс с точкой входа main, который тут используется правильно- ведет диалог "играть или выйти" и запускает игру.

Признаки процедурного стиля: 
+ Не выявлены и не сделаны в виде классов менее очевидные, чем Словарь, сущности. Напимер, Секретное слово. 
Значит нет системного понимания объектно-ориентированной декомпозиции.

+ Хранение картинок и метод их распечатки тоже не вынесен в отдельный класс, а находится в классе с общей игровой логикой.
+ Те классы, на которые сейчас поделена программа, могут быть и в процедурной программе- процедцрный стиль сам по себе не отрицает возможность создания нескольких классов.
+ Использование в Словаре статических изменяемых полей.

Менее очевидные процедурные приёмы:  

Класс валидатора вводимых букв содержит статический метод.  
На первый взгляд- какая разница?  
Но если метод валидатора сделать не статическим, это откроет возможности для создания валидаторов других языков, которые можно будет использовать в программе через полиморфизм.

И дело тут не в том, собираешься ли ты действительно развивать программу и вводить туда несколько разных языков для ввода букв.  
Дело в общем архитектурном подходе при выборе между созданием обычных классов или утилит(классов только со статическими методами и без состояний).

## ВЫВОД

Сравнить различия между ООП и процедурным стилями:  
Стрим Сергея [Крестики-нолики в процедурном стиле](https://www.youtube.com/watch?v=PPikj1qHxrA)  
Мой стрим [Крестики-нолики в ООП стиле](https://t.me/zhukovsd_it_chat/53243/187097)

Подробное объяснение, как делать эту программу в процедурном и ООП стилях, есть у Сергея в расширенных материалах.

n.146(275)  
#ревью #виселица 