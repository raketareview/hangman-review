https://github.com/nadillustrator/HangmanGame  
[Nadia Popova]

Игра в процедурном стиле, выполнена в нескольких классах.

## НЕДОСТАТКИ РЕАЛИЗАЦИИ

1. Нет списка введенных букв 

## ХОРОШО

+ 👍 Игра запускается
+ 👍 Можно ввести только одиночную букву русского алфавита

## ЗАМЕЧАНИЯ

**1. Нейминг**

- Осознанно придерживайся конвенции кода.

Если придерживаться Oracle Java code conventions, эту константу нужно писать стилем UPPER_SNAKE.  
А если конвенции Google, то так, как здесь написано.  
Неважно, какой конвенции ты придерживаешься. Главное, делать это осознанно
```java
//СЕЙЧАС ТАК:
private final static List<String> pictures

//КОНВЕНЦИЯ ORACLE:
private final static List<String> PICTURES

//КОНВЕНЦИЯ GOOGLE:
private final static List<String> pictures
```

- Если в проекте есть класс `Picture`, то все переменные с именем, включающим это название, должны быть экземплярами этого класса
```java
public class Picture {
  private final static List<String> pictures = List.of(...);
  
  public static List<String> getPictures() {...}
}

//ПРАВИЛЬНО:
public class Picture {
  private final static List<String> SPRITES = List.of(...);

  public static List<String> getSprites() {...}
}
```
Если разные концепции называются одним и тем же именем, это приводит к путанице.  
Например, какой тип данных будет иметь здесь переменная "x"? Что это, список экземпляров Picture или что-то иное?
```java
var x = Picture.getPictures();
```

- Не запутывай читателя твоего кода
```java
public Boolean getVictory() {
  return isVictory;
}

//ПРАВИЛЬНО:
public Boolean isVictory() {
  return isVictory;
}
```

- Если метод что-то проверяет(чекает), то результат проверки он должен вернуть в виде boolean.  
Если метод `void`, значит он должен называться по-другому
```java
void checkVictory(Game game)
```
Название метода должно объяснять, что он делает.  
Этот метод не проверяет победу, он печатает информацию в конце игры.

- Название должно хорошо, но лаконично объяснять, то происходит
```java
boolean isLetterWasNamedBefore(String inputValue, Game game) {
  return game.getNamedLetters().contains(inputValue);
}

//ПРАВИЛЬНО:
boolean isNamedLetter(String inputValue, Game game)
```

- Название метода прямо противоположно тому, что он делает
```java
private static boolean isOneSymbol(String inputValue) {  
  return inputValue.length() > 1;  <-- вернет true если символ НЕ ОДИН
}
```

- Частица "to" используется в методах конвертации: `toList()`, `toString()`, `toInt()`, `meterToFeet()`.  
В остальных случаях воздерживайся от этого.  
Иначе кажется, что метод конвертирует установленную букву в дисплейное слово, а это не так
```java
private void setLetterToDisplayWord(Game game, String letter)

//ПРАВИЛЬНО:
private void setDisplayWordLetter(Game game, String letter)
```
*Oracle Java code conventions, part."Naming conventions"*  
*Мартин, "Чистый код", гл.2*  
*Ютуб, Немчинский "Как называть переменные, методы и классы?"*

**2. Если в блоке if есть return**(break, continue, throw, exit и т.д.), то else не пишется.
 
В этом случае неважно, будет else или нет, так как программа будет работать одинаково, а код без `else` будет выглядеть читабельней
```java
if (isOneSymbol(inputValue)) {
  System.err.println("Нужно ввести один символ");
  return false;
} else if (isLetterWasNamedBefore(inputValue, game)) {
  System.err.println("Вы уже называли " + inputValue);
  return false;
}

//ПРАВИЛЬНО:
if (isOneSymbol(inputValue)) {
  System.err.println("Нужно ввести один символ");
  return false;
} 

if (isLetterWasNamedBefore(inputValue, game)) {
  System.err.println("Вы уже называли " + inputValue);
  return false;
}
```

**3. Форматирование строк**

Если нужно печатать или создавать строку с более, чем одним подстановочным значением или значение вставляется внутрь сообщения, используй форматирование- тогда сразу будет виден весь шаблон
```java
System.out.println("Буквы \"" + letter + "\" нет в этом слове");

//ПРАВИЛЬНО:
System.out.printf("Буквы '%s' нет в этом слове  %n", letter);
```

**4. Разделение по пакетам**

Сейчас классы разделены по двум пакетам: "service" и "model".  
Но классы в них не всегда соответствуют критериям моделей и сервисов.

В пакете `model` есть класс `Picture`, который не является моделью- он хранит спрайты виселицы для консоли.  
То есть этот класс хранит некоторые данные в интересах представления.

В пакете `service` все сервисы-не сервисы, потому что печатают инфу в консоль.  
Сервис это слой бизнес-логики и он не должен зависеть от представления.

Пока не разобралась с ООП на уровне понимания сути архитектур MVC и MVC(S), не называй классы сервисами. 
Есть много других хороших названий.  

Например, здесь вместо `MenuService` можно назвать просто `Menu`.  
А связка классов `GameService` + `Game` должна называться `GameController` + `Game`.  
Контроллерами тоже не называй классы без понимания MVC, но в данном случае это именно контроллер.  
С моделями тоже аккуратнее.

**5. class Picture**

+ 👍 Картинки в отдельном классе это хорошо, это разгружает классы с логикой от графики.

- Если нужно возвращать просто список картинок, то для этого не нужно делать специальный метод. 
Достаточно публичной константы:
```java
public class Picture {
 private final static List<String> pictures = List.of(
   //спрайты изображений  
 );

 public static List<String> getPictures() {
   return pictures;
 }
}

//ПРАВИЛЬНО:
public class Picture {
 public final static List<String> SPRITES = List.of(
   //спрайты изображений  
 );
}
```

Другое дело, что стоит переделать концепцию самого класса.  
Путь он будет не просто хранилищем констант, а рендерером:
```java
public class HangmanRenderer {
  private final static List<String> pictures = List.of(...);

  public void render(int num) {
    //печатает в консоль картинку по ее номеру
  }
}
```

**6. class Game**

- Позиционирование класса.

Какую сущность представляет из себя этот класс?
Класс хранит только текущие состояния игры, не содержит игровой логики.  
Ок, это анемичная модель сущности Игра.  

Но тогда этот класс не должен хранить спрайты изображений- это делает класс зависимым от представления.  
А значит модель перестает быть моделью
```java
private String picture;
```
Придерживайся принципа отделения модели данных от представления данных.

**7. class MenuService**

- Содержит только статические методы- это не сервис, это утилита.

- Нарушение инкапсуляции. 

Публичным должен быть только метод `startGame`. 
Остальные методы и поля здесь должны быть приватными, потому что они используются только внутри этого класса и являются подробостями его внутренней организации.

**8. Реакция на ошибки** 

Если по указанному адресу в `class GameService` не будет найден файл со словами, то программа аварийно вылетит и напечатает непонятные для юзера сообщения в консоль
```java
Exception in thread "main" java.lang.RuntimeException: java.nio.file.NoSuchFileException: dictionary\singular.txt
	at service.GameService.fillInWordList(GameService.java:38)
```
Здесь должна быть другая реакция на отсутствие файла со словами.  
Нужно сказать юзеру, что файл со словами по указанному пути открыть не удалось и поэтому работа программы будет завершена. 

Причем, путь нужно указать не относительный, как у тебя(`dictionary\singular.txt`).  
А абсолютный, чтобы юзер знал, где конкретно ему нужно искать файл: `c:/..../dictionary/singular.txt`.

После этого корректно завершить работу программы без вываливания красных эксепшенов в консоль.

**9. class GameService**

- Нарушение ООП дизайна.

Это поле не должно быть static
```java
private static List<String> words = new ArrayList<>();
```
Статические поля в ООП программе нужно применять очень осторожно. 
Потому что сама идея классов состоит в том, что у класса может быть несколько экземпляров одновременно. 
И при этом все они должны корректно работать.

В данном случае если в проекте будет существовать несколько экземпляров класса одновременно, они будут работать некорректно. 
Потому что будут одновременно вносить изменения в одно и то же статическое поле.

И тут вопрос не в том, должно ли *конкретно в этом* проекте быть два экземпляра одного класса, или не должно. 
Если класс не утилитный, не синглтон и можно создать несколько его экземпляров, то по умолчанию мы признаем, что его экземпляров в проекте может быть более одного.

Используй статические поля только если они `final`- то есть, константы. 
Причем, константы именно в понимании конвенции Google.

- Не увлекайся статическими методами.

Экземпляр `Game` стоит сделать полем класса. А методы, которые его используют- не статическими
```java
public void playNewGame() {
  Game game = createNewGame();
  //...
}

private static boolean isLetterWasNamedBefore(String inputValue, Game game) {
  return game.getNamedLetters().contains(inputValue);
}

//ПРАИВЛЬНО:
private Game game;

public void GameService() {
  this.game = createNewGame();
  //...
}

private boolean isLetterWasNamedBefore(String inputValue) {
  return game.getNamedLetters().contains(inputValue);
}
```

- Этот метод делает два дела сразу и совмещает команду и запрос.

Методы должны делать только каждый одно дело и не совмещать команды и запросы.

Здесь два дела: проверка того, что буква валидная и печать соответствующей информации
```java
private Boolean isInputValueValid(String inputValue, Game game) {
 if (isOneSymbol(inputValue)) {
  System.err.println("Нужно ввести один символ");  <-- ВЫПОЛНЕНИЕ КОМАНДЫ
  return false;   <-- ОТВЕТ НА ЗАПРОС
 } else if (isLetterWasNamedBefore(inputValue, game)) {...}
 else if (!isLetterCyrillicSymbol(inputValue)) {...}
 return true;   <-- ОТВЕТ НА ЗАПРОС
}
```
*Мартин, "Чистый код", гл.3, "Разделение команд и запросов"*

**10. class Main**, содержит точку входа main

+ 👍 Только создает и запускает Игру, в данном случае через Меню, это хорошо.

- Не используй статические импорты. 

Иначе становится непонятно, что используемый в классе метод или константа не его собственные, а чьи-то чужие
```java
import static service.MenuService.*;

public class Main {
  public static void main(String[] args) {
    startGame();
  }
}

//ПРАВИЛЬНО:
public class Main {
  public static void main(String[] args) {
    MenuService.startGame();
  }
}
```
*"ЧК", гл.17, G18*

## АРХИТЕКТУРА

Несмотря на то, что в проекте несколько классов, он написан в процедурном стиле. 
Программа в ООП стиле должна быть декомпозирована по правилам ООП и использовать ООП подход для реализации типичных задач.  
Здесь не видно ООП декомпозиции. Не выявлены и не сделаны в виде классов даже очевидные сущности- рендерер, файловый ридер.

## ВЫВОД

Сравнить различия между ООП и процедурным стилями:  
Стрим Сергея [Крестики-нолики в процедурном стиле](https://www.youtube.com/watch?v=PPikj1qHxrA)  
Мой стрим [Крестики-нолики в ООП стиле](https://t.me/zhukovsd_it_chat/53243/187097)

Подробное объяснение, как делать эту программу в процедурном и ООП стилях, есть у Сергея в расширенных материалах.

n.145(274)  
#ревью #виселица 