https://github.com/nj32gmail/Hangman  
[Красный телефон]

Игра в ООП стиле.  
В целом, ок.

## НЕДОСТАТКИ РЕАЛИЗАЦИИ

Не обнаружено.

## ХОРОШО

+ 👍 Игра запускается
+ 👍 Можно ввести только одиночную букву русского алфавита
+ 👍 Есть список введенных букв

## ЗАМЕЧАНИЯ

**1. Нейминг**

- Не называй метод "run" если он не переопределяет `Runnable.run()`- название этого метода ассоциируется с этим стандартным интерфейсом и применением потоков. 
Здесь этот метод не имеет отношение к `Runnable.run()`
```java
public class Main {
  void run(String dictionaryFilename) {...}
}
```

- Какой смысл здесь несет уточнение "ANSI" и "ESCAPE"?  
Не вижу, что добавляют эти уточнения в названии к пониманию сути этих констант.  
Если их убрать, ничего не изменится, это слова-паразиты
```java
public class GallowsRenderer implements Renderer {
  private static final String ANSI_ESCAPE_COLOR_RED = "\033[31m";
  private static final String ANSI_ESCAPE_COLOR_GREEN = "\033[32m";
  //...
}

//ПРАВИЛЬНО:
private static final String RED_COLOR = "\033[31m";
private static final String GREEN_COLOR = "\033[32m";
```

- Название должно объяснять, что делает метод. Название "пробовать букву" только вызывает новые вопросы
```java
void tryLetter(final Character letter) 
```

*Oracle Java code conventions, part."Naming conventions"*  
*Мартин, "Чистый код", гл.2*  
*Ютуб, Немчинский "Как называть переменные, методы и классы?"*

**2. Используй классы через их интерфейсы**
```java
ArrayList<String> words = new ArrayList<>();
ArrayList<Character> usedLetters = new ArrayList<>();

//ПРАВИЛЬНО:
List<String> words = new ArrayList<>();
List<Character> usedLetters = new ArrayList<>();
```
Общее правило: ArrayList нужно использовать через List, HashMap- через Map и т.д.  
Это позволяет пользоваться преимуществами полиморфизма.

Да, бывают ситуации, когда, например, с LinkedList нужно работать именно как с LinkedList, а не с List. 
Но это уже нюансы.  
*"Java. Эффективное программирование", изд.3, гл.9.8*
```
"Если вы выработаете привычку использовать в качестве типов интерфейсы, ваша программа будет гораздо более гибкой" - Блох.
```

**3. Текст в exception** всегда должен быть на английском языке
```java
throw new DictionaryReadException(
  "Не удалось прочитать слова из файла. Убедитесь, что файл существует и доступен для чтения. файл словаря: "
  + fileName, e
);
```
Исключение это не просто телеграмма, которая летит сквозь слои. 
У exception особое назначение- если исключение вылетит и не будет перехвачено внутри программы, то аварийно прекратит выполнение программы.
Тогда на экране будет распечатано сообщение эксепшена, и это сообщение должно быть понятно сисадмину в любой точке планеты. 
А значит, сообщение должно быть на английском. 
Интерпритация исключения и перевод его на локальный язык должно происходить там, где это соответствует архитектуре программы или не происходить вовсе, если исключение не планируется перехватывать.

**4. Нарушение DRY**, магические буквы, числа, слова. Вводи константы 
```java
System.out.println("| 1. Новая игра |");
System.out.println("| 2. Выход      |");
if (input.equals("1")) {...}
if (input.equals("2")) {...}

//ПРАВИЛЬНО:
private final static String START = "1";
private final static String QUIT = "2";

System.out.printf("| %s. Новая игра |  %n", START);
System.out.printf("| %s. Выход      |  %n", QUIT);
if (input.equalsIgnoreCase(START)) {...}
if (input.equalsIgnoreCase(QUIT)) {...}
```
*Фаулер, "Рефакторинг", гл.8, "Замена магического числа символической константой"*  
*refactoring.guru "Замена магического числа символьной константой"* 

**5. Если в блоке if есть return**(break, continue, throw, exit и т.д.), то else не пишется.
 
В этом случае неважно, будет else или нет, так как программа будет работать одинаково, а код без `else` будет выглядеть читабельней
```java
if (input.equals("1")) {
  return StartMenuItemEnum.NEW_GAME;
} else if (input.equals("2")) {
  return StartMenuItemEnum.EXIT;
} else {
  showError();
}

//ПРАВИЛЬНО:
if (input.equals("1")) {
  return StartMenuItemEnum.NEW_GAME;
} 
if (input.equals("2")) {
  return StartMenuItemEnum.EXIT;
} 
showError();
```

**6. Не нужно во входящих аргументах методов писать final**

Такое защитное программирование избыточно и захламляет код
```java
public ConsoleInputReader(final InputStream inputStream)
public FileWordsProvider(final String fileName)
public HangmanGame(final WordsProvider wordsProvider, final Renderer renderer, final InputReader reader)

//ПРАВИЛЬНО:
public ConsoleInputReader(InputStream inputStream)
public FileWordsProvider(String fileName)
public HangmanGame(WordsProvider wordsProvider, Renderer renderer, InputReader reader)
```
Наверное, в отдельных случаях можно писать `final` в аргументах методов.  
Но это должно быть как-то обосновано в контексте кода, а не использоваться повсеместно.

**7. interface WordsProvider**

👍 Интерфейс для получения слова для игры.  
Это хорошо- можно делать разные реалиации для получения слова изразных источников: из файла, по сети, просто из памяти.

**8. class FileWordsProvider implements WordsProvider**, получение слов из файла

- Количество букв слов не должно быть захардкорено в константе, а должно передаваться в конструктор. 
Нет никаких причин количество букв жестко ограничивать через константу 
```java
private static final int WORD_LENGHT_MIN = 5;

public FileWordsProvider(final String fileName) {...}

//ПРАВИЛЬНО:
public FileWordsProvider(final String fileName, int minWordLength) {...}
```

- Не нужно в сигнатуре методов прописывать непроверяемые исключения- это визуальный шум
```java
private void readWords() throws DictionaryReadException {...}

//ПРАВИЛЬНО:
private void readWords() {...}
```
Здесь `DictionaryReadException` наследуется от RuntimeException, а значит является непроверяемым(unchecked) исключением.

- Реакция на ошибки при чтении файла.

👍 Если по указанному пути не будет найден файл со словами, то программа отреагирует правильно: напечатает сообщение и корректно прервет свою работу
```java
Ошибка чтения файла слов (words.txt). 
Проверьте, пожалуйста, наличие файла и его доступность для чтения. 
Игра прервана. До свидания!
```
Но путь нужно указать не относительный, как у тебя(`words.txt`).  
А абсолютный, чтобы юзер знал, где конкретно ему нужно искать файл: `c:/..../words.txt`.

- Нет смысла держать в классе этот параметр в виде поля
```java
private final String fileName;
```
Имя файла нужно только один раз- при чтении файла, которое происходит один раз в конструкторе. 
А дальше в хранении имени файла нет необходимости
```java
public class FileWordsProvider implements WordsProvider {
  //...  
  private final Random random = new Random();

  public FileWordsProvider(final String fileName) {
    this.fileName = fileName;
    readWords();
  }

  private void readWords() {
    //чтение файла по имени this.fileName
  }  
}

//ПРАВИЛЬНО:
public class FileWordsProvider implements WordsProvider {
  //...  
  public FileWordsProvider(final String fileName) {
    readWords(fileName);
  }

  private void readWords(String fileName) {...}  
}
```

**9. class StartMenu**

- Меню в процедурном стиле. Для простой программы- почему бы и нет.  
Про меню в ООП стиле я писал [ТУТ](https://t.me/zhukovsd_it_chat/53243/114908).

- Никогда не возвращай null
```java
public StartMenuItemEnum getChoice() {
  //...
  if (input == null) {
    return null;
  }
}
```
Возврат null повышает риск возникновения NullPointerException в программе.  
*Мартин, "Чистый код", гл.7.7-8*  
*Ютуб, Немчинский "Почему нельзя возвращать NULL?"*

- Этот метод в стандартной библиотеке java перегружен, его можно вызывать без аргументов
```java
System.out.println("");

//ПРАВИЛЬНО:
System.out.println();
```

**10. interface InputReader**

+ 👍 Интерфейс для получия данных от пользователя, это хорошо.  
Можно сделать разные реализации, например, для получения ввода через клавиатуру, мышку или по интерфейсу RS-232 из аркадного автомата
```java
public interface InputReader {
  String getInput();
  Character getLetter() throws InputNullException;
}
```

- При прочих равных, нужно использовать примитивный тип, а не класс-обертку
```java
Character getLetter() throws InputNullException;

//ПРАВИЛЬНО:
char getLetter() throws InputNullException;
```
Для применения класса-обертки над примитивным типом данных, должна быть какая-то причина. 
Здесь использование обертки не дает ничего большего по сравнению с примитивом.  
*Блох "Java. Эффективное программирование", изд.3, гл.9.5*
```
"Предпочитайте примитивные типы упакованным примитивным типам" - Блох.
```

**11. class ConsoleInputReader implements InputReader**

- Избыточно. 

Здесь не нужна двойная проверка на принадлежность символа к буквам всех алфавитов и к букве конкретного алфавита. 
Сразу проверяй букву на принадлежность к русскому алфавиту.
```java
final char ch = input.charAt(0);
if (Character.isLetter(ch)) {  <--- ПРОВЕРЯЕМ, ЧТО ЭТО КАКАЯ_ТО БУКВА
  if ((ch >= 'а' && ch <= 'я') || (ch >= 'А' && ch <= 'Я') || ch == 'ё' || ch == 'Ё') {...}  <--- ПРОВЕРЯЕМ, ЧТО ЭТО РУССКАЯ БУКВА
}

//ПРАВИЛЬНО:
final char symbol = input.charAt(0);
if(isRussianLetter(symbol)) {...}

private boolean isRussianLetter(char symbol) {
  symbol = Character.toLowerCase(symbol);
  return (symbol >= 'а' && symbol <= 'я') || symbol == 'ё';
}
```

- Другие языки.

```java
// TODO: предусмотреть режим работы с английскими словарями (отключить проверку
```
Спойлерну:  
Просто сделай валидаторы для разных языков и инжекти их в ConsoleInputReader
```java
public interface LetterValidator {
  boolean isValid(char symbol);
}

public class ConsoleInputReader implements InputReader {
  //...
  public ConsoleInputReader(InputStream inputStream, LetterValidator letterValidator) {...}
}
```

- Не возвращай null
```java
public Character getLetter() throws InputNullException {
  //...
  return null;
}
```

- Комментарии констатируют очевидное 
```java
// Проверяем, что символ является буквой любого алфавита
if (Character.isLetter(ch)) {...}

// Проверяем, что символ является русской буквой
if ((ch >= 'а' && ch <= 'я') || (ch >= 'А' && ch <= 'Я') || ch == 'ё' || ch == 'Ё') {...}
```
Когда в проекте много каментов, это плохо- пользы от них практически нет, они только забивают пространство и мешают читать код.  
В идеале, комментариев вообще не должно быть(кроме `TODO`). Код должен объяснять сам себя через правильный нейминг и лаконичный код.  
*Мартин, "ЧК", гл.4*

**12. class GameState**

- Позиционирование класса.

Непонятно, какую сущность из себя представляет этот класс.

Судя по названию, класс должен хранить некие данные, которые характеризуют состояние игры.  
В ООП под состоянием понимают значения всех полей.  
В данном случае, по видимому, значение всех полей, которые характеризуют процесс игры.

Ок, но кроме полей(`secretWord`, `maxAttempts` etc) класс содержит еще сложную прикладную логику вроде `tryLetter(...)` или `updateGuessedWord()`.  
Значит, эта сущность не "State", а что-то другое.

Может, это движок игры?  
То есть класс, который содержит чать алгоритма игры, которая не использует представление?  
Но в этом классе есть методы, которые составляют логику представления, а не логику процесса игры.  
Например, метод, который форматирует строку для ее представления в консоли: `String getFormattedUsedLetters()`.

Ответь для себя, какую сущность представляет из себя этот класс.  
Если не сможешь ответить, значит это не объект в понимании ООП, а сборник функций в стиле процедурного программироваия.

- Использование в рендерере.

Этот объект передается в рендерер для печати
```java
renderer.showWin(gameState);
```
Если бы класс имел только состояние(поля) и не имел развитое поведения(методы, кроме геттеров и сеттеров), то это было бы нормально.  
Но передавать такой сложный класс в рендерер нельзя.

Нужно или оставить в этом классе только состояние и геттеры с сеттерами.  
Или передавать в рендерер не весь объект, а только нужные для распечатки данные из него 
```java
//СЕЙЧАС ТАК:
public class GallowsRenderer implements Renderer {

  @Override
  public void showWin(final GameState gameState) {
    //...
    out.println("Загаданное слово: " + gameState.getSecretWord());
  }
}  

//ПРАВИЛЬНО ТАК:
public void showWin(String secretWord) {...}
```
*Макконнелл, "Совершенный код", гл.7.5*
```
"Передавайте в метод те переменные или объекты, которые нужны ему для поддержания абстракции интерфейса" - Макконнелл.
```

**12. interface Renderer**

👍 Интерфейс рендерера это хорошо.  
Можно делать разные его реализации, которые будут печатать информацию по-разному.  
Например, большие картинки или маленькие, монохромная печать или цветная.

**13. class GallowsRenderer implements Renderer**

- Нарушение DRY. Общий код выноси во вспомогательные методы
```java
private void setColorGreen() {
  out.print(ANSI_ESCAPE_COLOR_GREEN);
}

private void setColorRed() {
  out.print(ANSI_ESCAPE_COLOR_RED);
}

//ПРАВИЛЬНО:
private void setColorGreen() {
  setColor(ANSI_ESCAPE_COLOR_GREEN);
}

private void setColorRed() {
  setColor(ANSI_ESCAPE_COLOR_RED);
}

private void setColor(String color) {
  out.print(color);
}
```

- Выбранный способ распечатки хангмана- сложный и непонятный.

Понятно желание соптимизировать хранение картинок и все слайды сгруппировать в максимально сжатый объем.  
Но здесь это делает код значительно более худшим для понимания.  
И это касается не только картинки, но и способа их распечатки через последовательности условных операторов
```java
if (mistakeCount > 0) {
  out.println("|/   O");
} else {
  out.println("|/");
}
if (mistakeCount > 3) {
  out.println("|   -|-");
} //+ еще миллион строк загадочного кода
```
Для такой простой игры приоритет- читаемость кода, а не оптимизация хранения картинок в памяти.

Картинки храни в массиве и печатай по номеру.  
Желательно, картинки и метод их распечатки вынести в отдельный класс-рендерер
```java
public class HangmanRenderer {
  private static final String[] PICTURES = {
      """
    -----   
    |       
    | 
    |       
    |       
    ------- 
    """,
      """
     -----   
     |   |   
     |   O   
     |       
     | 
     ------- 
     """
      ,
      // more pics
  };
  
  public void render(int num) {
    System.out.println(PICTURES[num]);
  }
}
```

- Раз у тебя тут прям накал ООП с интерфейсами, сделай распечатку в этом классе монохромной, а весь цветной функционал вынеси в потомка.
Например, так:
```java
public class ColorGallowsRenderer extends GallowsRenderer {
   //...

  @Override
  public void showLose(GameState gameState) {
    setColorRed();
    super.showLose(gameState);
    resetColor();
  }
}
```

**14. class HangmanGame**

+ 👍 Принимает в конструктор достаточное количество зависимостей.  
При этом интерфейсы используются правильно- они не инициализируются конкретными реализациями внутри `HangmanGame`, а инжектятся ему в конструктор
```java
class HangmanGam {
  public HangmanGame(
    final WordsProvider wordsProvider,    <-- интерфейс
    final Renderer renderer,   <-- интерфейс
    final InputReader reader   <-- интерфейс
  ) {...}
}
```

+ 👍 В классе все ок.

**15. class Main**, содержит точку входа main

+ 👍 Только создает и запускает Игру, это хорошо.

+ 👍 Диалог "играть или выйти" происходит здесь, а не в `Game`, это хорошо.

- Нарушение конвенции кода. Метод `main` должен стоять самым первым из методов.

- Не оборачивай большие куски кода блоком try-catch непосредственно, изолируй его 
```java
public static void main(final String[] args) {
  new Main().run(DICTIONARY_FILENAME);
}

void run(String dictionaryFilename) {
  try {
    //миллион строк кода
  } catch (final InputNullException e) {
    //...
  } catch (final DictionaryReadException e) {
    //...
  }
}

//ПРАВИЛЬНО:
public static void main(final String[] args) {
  try{
    start();
  }catch (final InputNullException e) {
    //...
  } catch (final DictionaryReadException e) {
    //...
  }
}

private static start() {
  //миллион строк кода 
}
```
*"ЧК", гл.3, "Изолируйте блоки try/catch"*

## ВЫВОД

В целом, все хорошо. Если при написании проекта не вайбкодил, то вообще молодец.

n.147(278)  
#ревью #виселица