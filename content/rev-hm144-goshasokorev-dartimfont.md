https://github.com/dartimfont/hangman  
[Gosha Sokorev]

Игра в процедурном стиле, выполнена в одном классе.  
В целом, ок.

## НЕДОСТАТКИ РЕАЛИЗАЦИИ

1. Кракозябры
```
1. ������ ����
2. ����� �� ����
�����: 1
    |-------|
            |     �����: _______
            |     ������ (0): 
            |     �����:  
            |
            |
|-----------|

������� �����:
```

2. Можно ввести не только русскую букву, но и буквы других алфавитов
```
Ошибки (3): s, й, 魚, 
```

3. Неправильная последовательность в порядке отображения введенных букв: букву 'ц' я ввел второй, но в распечатке она стала первой. 
При этом введенная третьей буква 'к' в распечатке стала на третью позицию
```
Введите букву: 
й
Ошибки (1): й, 

Введите букву: 
ц
Ошибки (2): ц, й, 

Введите букву: 
к
Ошибки (3): ц, й, к, 
```
Введенные буквы должны размещаться по алфавиту(й, к, ц) либо в порядке ввода(й, ц, к).

4. После проигрыша не показывает отгадываемое слово

Во-первых, мне любопытно, какое слово я отгадывал.  
Во-вторых, откуда я знаю, что твоя программа работает правильно? 
Может быть, я на самом деле отгадал слово.

## ХОРОШО

+ 👍 Игра запускается
+ 👍 Можно ввести только одиночную букву
+ 👍 Есть список введенных букв
+ 👍 Распечатка виселицы и сообщений в одной горизонтали, это сделать сложнее
```
    |-------|
    O       |     Слово: ____ец
   \|/      |     Ошибки (6): ё, s, у, й, 魚, к, 
    |       |     Буква: к
   / \      |
            |
|-----------|
```
+ 👍 Простой понятный алгоритм

## ЗАМЕЧАНИЯ

**1. Нейминг**

- Название методов должно быть глаголом в повелительном наклонении. Это название- существительное
```java
void mainMenu()
```

- Буква не может быть String. Потому что буква это один символ, а строка- ноль, один и более символов.  
Эта переменная должна либо называться по-другому, либо не быть String
```java
String letter = "";
```

- В названия не нужно вставлять частицы "Of", "The" и т.д. 
Если конечно там "Of" не используется в контексте valueOf()
```java
List<Character> lettersOfRandomWord

//ПРАВИЛЬНО:
List<Character> randomWordLetters
```
По аналогии с уже применяющимися тобой названиями: `Set<String> mistakeLetters`

- Метод не обновляет outputWord, он его создает
```java
String updateOutputWord(Set<String> rightLetters, String randomWord)

//ПРАВИЛЬНО:
String createOutputWord(Set<String> rightLetters, String randomWord)
```

- Название должно как можно лучше объяснять суть явления
```java
String wordForOutput = updateOutputWord(allLetters, randomWord);

//ЛУЧШЕ:
String mask = createMask(allLetters, randomWord);
```

*Oracle Java code conventions, part."Naming conventions"*  
*Мартин, "Чистый код", гл.2*  
*Ютуб, Немчинский "Как называть переменные, методы и классы?"*

**2. Нарушение инкапсуляции**. 

Публичным должен быть только метод `main()`.  
Остальные методы и поля здесь должны быть приватными.

**3. Нарушение DRY**, магические буквы, числа, слова. Вводи константы. А если они уже есть- пользуйся 
```java
public static final int START_GAME = 1;
public static final int QUIT_GAME = 2;

System.out.println("1. Начать игру");
System.out.println("2. Выйти из игры");
if (result < 0 || result >= 2) {
  System.out.println("Можно выбрать 1 или 2 пункт!");
}
System.out.println("Данное выражение " + string
     + " не подходит можно вводить только целые числа от 1 до 2!");

//ПРАВИЛЬНО:
System.out.println(START_GAME + ". Начать игру");
System.out.println(QUIT_GAME + ". Выйти из игры");
if (result != START_GAME || result != QUIT_GAME) {
  System.out.printf("Можно выбрать %d или %d пункт! \n", START_GAME, QUIT_GAME);
}
System.out.printf("Данное выражение %s не подходит можно вводить только целые числа:  %d - %d  \n", string, START_GAME, QUIT_GAME);
```
*Фаулер, "Рефакторинг", гл.8, "Замена магического числа символической константой"*  
*refactoring.guru "Замена магического числа символьной константой"*

**4. Используй StringBuilder**

Плюсование строк в цикле это расточительно- постоянно пересоздаются объекты.  
Для складывания строк в цикле применяй StringBuilder- это с точки зрения ресурсов более оптимальный процесс.  
Посмотри, idea подчеркивает здесь плюсование желтым цветом, это она предлагает заменить складывание строк использованием стрингбилдера.  
Поставь курсор на желтое подчеркивание, слева появится желтая лампочка, клацни на нее, выбери пункт "Convert..." и идея автоматически заменит плюсование на StringBuilder
```java
String errorMessage = "";
for (String mistakeLetter : mistakeLetters) {
  errorMessage += mistakeLetter + ", ";
}

//ПРАВИЛЬНО:
StringBuilder errorMessage = new StringBuilder();
for (String mistakeLetter : mistakeLetters) {
  errorMessage.append(mistakeLetter).append(", ");
} 
```

**5. Реакция на ошибки** 

Если по указанному адресу не будет найден файл со словами, то произойдет странное:  
Игра продолжит свою оработу и закончится победой
```java
1. Начать игру
2. Выйти из игры
Выбор: 1
.\src\main\resources\words.txt_ (Не удается найти указанный файл)
    |-------|
            |     Слово: 
            |     Ошибки (0): 
            |     Буква:  
|-----------|

Введите букву: 
й
    |-------|
    O       |     Слово: 
            |     Ошибки (1): й, 
            |     Буква: й
|-----------|

Вы победили!
```
Здесь должна быть другая реакция на отсутствие файла со словами.  
Нужно сказать юзеру, что файл со словами по указанному пути открыть не удалось и поэтому работа программы будет завершена. 

Причем, путь нужно указать не относительный, как у тебя(`.\src\main\resources\words.txt_`).  
А абсолютный, чтобы юзер знал, где конкретно ему нужно искать файл: `c:/..../words.txt_`.

После этого корректно завершить работу программы без вываливания красных эксепшенов в консоль.

**6. Создавай вспомогательные методы**, делай программу более простой и понятной
```java
if (!Character.isLetter(letter.charAt(0)) || letter.length() != 1) {
  System.out.println("Введите одну букву кириллицей!");
  continue;
}

//ПРАВИЛЬНО:
if (!isRussianLetter(letter)) {...}
```

**7. Создавай вспомогательные методы**, делай программу более простой и понятной 
```java
while (mistakeLetters.size() < PARTS_OF_BODY) {
  //...
  if (Objects.equals(wordForOutput, randomWord)) {
    printStep(wordForOutput, mistakeLetters, letter);
    System.out.println("Вы победили!");
    break;
  }

  if (mistakeLetters.size() == PARTS_OF_BODY) {
    System.out.println("Вы проиграли!");
  }
}

//ПРАВИЛЬНО:

while (!isGameOver(...)) {
  //...
  if (isWin(...)) {
    printStep(wordForOutput, mistakeLetters, letter);
    System.out.println("Вы победили!");
  } 
  
  if (isLose(...)) {
    System.out.println("Вы проиграли!");
  }
}
```

**8. Используй оптимальные типы данных**

Здесь алгоритм будет проще, если использовать более подходящий для него тип данных
```java
public static String updateOutputWord(Set<String> rightLetters, String randomWord) {

  List<Character> lettersOfRandomWord = new ArrayList<>();
  for (int i = 0; i < randomWord.length(); i++) {
    lettersOfRandomWord.add(randomWord.toLowerCase().charAt(i));
  }

  for (int i = 0; i < lettersOfRandomWord.size(); i++) {
    if (!rightLetters.contains(Character.toString(lettersOfRandomWord.get(i)))) {
      lettersOfRandomWord.set(i, '_');
    }
  }

  String result = "";
  for (Character letter : lettersOfRandomWord) {
    result += letter;
  }

  return result;
}

//ПРАВИЛЬНО:
public static String updateOutputWord(Set<String> rightLetters, String randomWord) {

  StringBuilder outputWord = new StringBuilder();
  for (int i = 0; i < randomWord.length(); i++) {
    char letter = randomWord.charAt(i);

    if(rightLetters.contains(letter)) {
      outputWord.append(letter);
    } else {
      outputWord.append(MASK_SYMBOL);
    }
  }

  return outputWord.toString();
}
```

**9. Использование классов в программе, написанной в процедурном стиле**

Программа, написаная в процедурном стиле, может быть разделена на классы.  
В этом случае, классы играют ту же роль, что и модули в процедурных языках программирования.

Здесь, кроме Main, стоит создать еще несколько классов:  
Словарь- читает текстовый файл и возвращает случайное слово.  
Рендерер- содержит картинки виселицы и печатает её.

Тем самым увеличится читаемость кода.

## ВЫВОД

Для процедурного стиля, вполне неплохо.

n.144(268)  
#ревью #виселица 
