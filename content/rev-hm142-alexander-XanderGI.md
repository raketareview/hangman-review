https://github.com/XanderGI/hangman  
[Alexander]

Игра в процедурном стиле, выполнена в нескольких классах.

## НЕДОСТАТКИ РЕАЛИЗАЦИИ

1. Можно ввести не только букву из алфавита текущего словаря, но и буквы любых других алфавитов
```
Ошибки (3): й, w, 魚
```

2. Можно ввести не одну букву, а набор букв и это будет считаться допустимым вводом
```
Введите букву: ццц
Слово: _ _ _ _ _
Ошибки (4): й, w, 魚, ц
```

3. Да, из-за меня. Но он все равно одноногий, его не жалко
```
    +---+
    |   |
    |   O
    |  /|\
    |  /
    |
    =========
Вы проиграли, из-за вас повесился человек!
```

## ХОРОШО

+ 👍 Игра запускается
+ 👍 Можно ввести только буквы
+ 👍 Есть список введенных букв
+ 👍 Два словаря на выбор: русский или английский

## ЗАМЕЧАНИЯ

Так как в "read.me" заявлено следование принципам SOLID, то критика к классам будет с точки зрения ООП.

**1. Нейминг**

- Придерживайся единообразия. Одной концепции- одно имя
```java
public ChoiceWord(Path pathToDictionary) {
  reloadDictionary(pathToDictionary);
}

public void reloadDictionary(Path path) {...}

//ПРАВИЛЬНО:
public ChoiceWord(Path path) {...}

public void reloadDictionary(Path path) 
```

- Название путает, "log" у читателя кода будет ассоциироваться с Logger
```java
StringBuilder log = new StringBuilder();

//ПРАВИЛЬНО:
StringBuilder stringBuilder = new StringBuilder();
```

- Какой конвенции кода ты придерживаешься?
```java
private static final Properties properties = new Properties();  <-- СЕЙЧАС ТАК, СООТВЕТСТВУЕТ КОНВЕНЦИИ GOOGLE

private static final Properties PROPERTIES = new Properties();  <-- А ТАК ДОЛЖНО БЫТЬ ПО КОНВЕНЦИИ ORACLE
```

- КОНСТАНТЫ_НУЖНО_ПИСАТЬ_СТИЛЕМ_UPPER_SNAKE. 
Это константа с точки зрения любой конвенции
```java
public static final String[] hangman

//ПРАВИЛЬНО:
public static final String[] HANGMAN
```

- Не увлекайся поэтическими названиями. Этот "художник" ничего не рисует, это хранилище картинок. 
Задача класса- вернуть каринку по номеру
```java
class Painter
```
*Oracle Java code conventions, part."Naming conventions"*  
*Мартин, "Чистый код", гл.2*  
*Ютуб, Немчинский "Как называть переменные, методы и классы?"*  

**2. Нарушение DRY**, магические буквы, числа, слова. Вводи константы 
```java
System.out.println("1. Русский");
System.out.println("2. English");
case 1 -> //...
case 2 -> //...

//ПРАВИЛЬНО:
private final static int RUSSIAN_DICTIONARY = 1;
private final static int ENGLISH_DICTIONARY = 2;

System.out.println(RUSSIAN_DICTIONARY + ". Русский");
System.out.println(ENGLISH_DICTIONARY + ". English");

case RUSSIAN_DICTIONARY -> //...
case ENGLISH_DICTIONARY -> //...
```

- Не называй метод "run" если он не переопределяет `Runnable.run()`- название этого метода ассоциируется с этим стандартным интерфейсом и применением потоков. 
Здесь этот метод не имеет отношение к `Runnable.run()`
```java
public class GameController {
  public void run() {...}
  //...
}

//ПРАВИЛЬНО:
start(){...} //go(), execute(), perform(), поехали() 
```
*Фаулер, "Рефакторинг", гл.8, "Замена магического числа символической константой"*  
*refactoring.guru "Замена магического числа символьной константой"*  

**3. Два класса в одном файле**

Не размещай два класса в одном файле в том случае, когда они равноценны и один класс не является внутренним для другого
```java
//--Начало файла GameController.java
enum Language {
  RU,
  EN
}

public class GameController {...}
//--Конец файла

//ПРАВИЛЬНО (в случае расположения в одном файле):
public class GameController {
  //...
  private enum Language {
    RU,
    EN
  }
}
```

**4. Используй классы через их интерфейсы**
```java
ArrayList<String> loadWords(Path path) {...}
ArrayList<String> listOfWords = new ArrayList<>();

//ПРАВИЛЬНО:
List<String> loadWords(Path path) {...}
List<String> listOfWords = new ArrayList<>();
```
Общее правило: ArrayList нужно использовать через List, HashMap- через Map и т.д.  
Это позволяет пользоваться преимуществами полиморфизма.

Да, бывают ситуации, когда, например, с LinkedList нужно работать именно как с LinkedList, а не с List. 
Но это уже нюансы.  
*"Java. Эффективное программирование", изд.3, гл.9.8*
```
"Если вы выработаете привычку использовать в качестве типов интерфейсы, ваша программа будет гораздо более гибкой" - Блох.
```

**5. Приоритет- читаемость кода**
```java
System.out.println("Вы выиграли! \n" +
  "загаданное слово было : " + secretWord);

//ПРАВИЛЬНО:  
System.out.println("Вы выиграли!");
System.out.println("загаданное слово было : " + secretWord);
```

**6. Комментарии**

Комментарий пытается исправить плохой нейминг. Лучше просто назови метод так, как принято называть фабричные методы
```java
public static GameState startGame(String secretWord) { // фабрика
```
Когда в проекте много каментов, это плохо- пользы от них практически нет, они только забивают пространство и мешают читать код.  
В идеале, комментариев вообще не должно быть, код должен объяснять сам себя через правильный нейминг и лаконичный код.  
*Мартин, "ЧК", гл.4*  

**7. class Config**

- Не используй секцию static. Это не дает ничего, кроме сложного для чтения кода 
```java
public class Config {
  private static final Properties properties = new Properties();

  static {
    //...
    properties.load(input);
  }
}

//ПРАВИЛЬНО:
public class Config {
  private static final Properties properties = getProperties();

  private static Properties getProperties() {
    //...
    Properties properties = new Properties();
    properties.load(input);
    return properties;
  }
}
```

- Соблюдай требования утилитных классов

Это утилитный класс, он содержит только статические методы. 
Утилитные классы должны иметь приватный конструктор и быть `final`. 
Не должно быть возможности унаследоваться от утилиты или сделать ее экземпляр.  
*Блох, "Java. Эффективное программирование", изд.3, гл.2.4*

**8. class ChoiceWord**

- Нарушение конвенции кода. Вспомогательный приватный метод стоит выше публичного, в интересах которого он существует.

- Этот *публичный* метод не нужен, он усложняет правила пользования классом, но не дает ничего полезного. 
К тому же, класс становится неиммутабельным
```java
public void reloadDictionary(Path path)
```

Если нужно заново получить `ChoiceWord` со всеми словами, просто пересоздай объект
```java
//БЫЛО:
choiceWord.reloadDictionary(Config.getPath("dict.ru.path"));

//СТАНЕТ:
choiceWord = new ChoiceWord(Config.getPath("dict.ru.path"));
```

- Текст в exception всегда должен быть на английском языке
```java
throw new IllegalStateException("Словарь пуст: " + path);
```
Исключение это не просто телеграмма, которая летит сквозь слои. 
У exception особое назначение- если исключение вылетит и не будет перехвачено внутри программы, то аварийно прекратит выполнение программы.
Тогда на экране будет распечатано сообщение эксепшена, и это сообщение должно быть понятно сисадмину в любой точке планеты. 
А значит, сообщение должно быть на английском. 
Интерпритация исключения и перевод его на локальный язык должно происходить там, где это соответствует архитектуре программы или не происходить вовсе, если исключение не планируется перехватывать.

- Реакция на ошибки

Если по указанному пути не будет найден файл со словами, то программа аварийно вылетит и напечатает непонятные для юзера сообщения в консоль
```java
Exception in thread "main" java.lang.RuntimeException: Не удалось прочитать словарь: words.txt
	at ChoiceWord.loadWords(ChoiceWord.java:47)  <-- ЧТО ЮЗЕР ДОЛЖЕН ДЕЛАТЬ С ЭТОЙ ИНФОРМАЦИЕЙ?
	at ChoiceWord.reloadDictionary(ChoiceWord.java:27)
```
Если ты все равно уже предусматриваешь какую-то реакцию на отсутствие файла со словами, то реакция должна быть корректной.  
Нужно сказать юзеру, что файл со словами по указанному пути открыть не удалось и поэтому работа программы будет завершена. 

Причем, путь нужно указать не относительный, как у тебя(`Не удалось прочитать словарь: words.txt`).  
А абсолютный, чтобы юзер знал, где конкретно ему нужно искать файл: `Не удалось прочитать словарь: c:/..../words.txt`.

После этого корректно завершить работу программы без вываливания красных эксепшенов в консоль.

**9. class Painter**

+ 👍 Отдельный класс для хранения картинок это хорошо, это разгружает классы с логикой от изображений.

- Используй многострочные строки
```java
"    +---+\n" +
"    |   |\n" +
"    |\n" +
"    |\n" +
"    |\n" +
"    =========",

//ПРАВИЛЬНО:
   """
    +---+
    |   |
    |
    |
    |
    =========
"""
```
Многострочные строки(текстовые блоки) имеются в java начиная с 13 версии.


- Неправильная реакция на ошибки.

Есть три основных вида реакции на ошибки: игнорирование, исправление, падение.  
Здесь выбрано исправление- при получении некорректного номера картинки, метод подгоняет номер под последний корректный
```java
public String getPicture(int countMistakes) {
  int index = Math.min(countMistakes, hangman.length - 1);
  return hangman[index];
}
```

Правильная реакция должна быть другой, нужно сделать падение:
```java
public String getPicture(int countMistakes) {
  return hangman[countMistakes];  <-- КИНЕТ ИСКЛЮЧЕНИЕ, ЕСЛИ КАРТИНКИ С ТАКИМ НОМЕРОМ НЕТ В МАССИВЕ
}
```
Потому что запрос картинки с неправильным номером означает, что в алгоритме игры есть баг.

Этот баг нужно не маскировать- это приведет только к тому, что игра будет происходить некорректно.  
Этот баг нужно максимально быстро выявить и устранить.

- Почему класс знает про какие-то ошибки ввода букв, даже на уровне нейминга?   
Единственное, что должен знать этот класс, это то, что он хранит картинки и должен их выдавать по номеру
```java
public String getPicture(int countMistakes)

//ПРАВИЛЬНО:
public String getPicture(int pictureNumber)
```

**10. class View**, заявка на слой представления

+ 👍 Класс, через который идет всё общение с юзером, или хотя бы только *распечатка* всех сообщений для юзера- это хорошо.

- Плохо то, что не вся распечатка идет через этот класс. 
Другие классы печатают и напрямую в консоль, потоэтому этот класс не имеет особого смысла.

- Ацкий ад. Это вообще не читается
```java
StringBuilder log = new StringBuilder();
log.append("Слово: ").append(gameState.getMaskWord()).append("\n");
log.append("Ошибки ").append("(").append(countMistakes).append("): ").append(formatterList(gameState.getUsedLetters())).append("\n");
log.append(painter.getPicture(countMistakes));
System.out.println(log);

//ПРАВИЛЬНО:
String hangmanPicture = painter.getPicture(countMistakes);

System.out.println("Слово: " + gameState.getMaskWord());
System.out.printf("Ошибки (%d): %s  \n", countMistakes, gameState.getUsedLetters());
System.out.println(hangmanPicture);
```

**11. class GameState**

- Позиционирование

Что это такое, мне неясно.  
Класс хранит не только состояния игры(например, слово и маску).  
Класс также хранит общеигровую логику, например процесс угадывания буквы
```java
public void processGuess(char letter) {
  //30 строк кода игровой логики  
}
```

Так что это, может быть это движок игры? То есть класс, в котором происходит общеигровая логика без взаимодействия с представлением?  
Нет, здесь среди прочего происходит взаимодействие с представлением.  
Причем, печать производится не через View, а напрямую в консоль:
```java
if (usedLetters.contains(letter)) {
  System.out.println("Вы уже использовали эту букву!");
}
```

Я сейчас не вижу в этом классе какой-то осмысленной сущности.
Это просто контейнер с методами в стиле процедурного программирования.

- Это фабричный метод, он не стартует игру. Он создает экземпляр этого класса
```java
public static GameState startGame(String secretWord) {    
  return new GameState(secretWord);
}

//ПРАВИЛЬНО:
public static GameState create(String secretWord) {...}
```
Другие стандартные названия фабричных методов: `of()`, `valueOf()`.

- Раз в программе есть словари с разными языками, то валидация должна быть с учетом языка текущего словаря
```java
private boolean isLetterValid(char letter) {
  return Character.isLetter(letter); <-- ВСЕ БУКВЫ ПОСЧИТАЕТ КОРРЕКТНЫМИ: й, w, 魚
}
```

- Вместо переменных для хранения состояния игры(флагов), используй вспомогательные методы. 
Енам для описания состояний тоже не нужен- он только усложняет код без всякой пользы 
```java
enum Status {
  IN_PROGRESS,
  WIN,
  LOSE
}

boolean isOpenSecretWord = true;
//...
if (c == '_') {
  isOpenSecretWord = false;
}

if (isOpenSecretWord) {
   this.gameStatus = Status.WIN;
}

public Status getGameStatus() {
  return gameStatus;
}

//ПРАВИЛЬНО:
```java
public boolean isWin() {
  //вернуть true если вся маска открыта
}

public boolean isLose() {
  return countMistakes >= MAX_MISTAKES;
}

public boolean isGameOver() {
  return isWin() || isLose();  
}
```
Преимущество: используя вспомогательные методы вместо переменных-флагов, не приходится переживать за актуальность данных, которые хранят флаги. 
Код становится проще и надежнее.

- Создавай вспомогательные методы, делай программу более простой и понятной
```java
if (secretWord.toLowerCase().contains(String.valueOf(letter))) {
  for (int i = 0; i < secretWord.length(); i++) {
    if (Character.toLowerCase(secretWord.charAt(i)) == letter) {
       maskWord[i] = secretWord.charAt(i);
    }
  }
}

//ПРАВИЛЬНО:
if (isSecretWordLetter(letter)) {
  openLetter(letter);
}
```

- Не используй префиксную форму инкремента(`++i`), используй только постфиксную форму(`i++`). 
Иначе приходится мучительно вспоминать, в чем между ними разница, при этом увеличивается вероятность совершить ошибку
```java
if (++countMistakes == MAX_MISTAKES) {...} 

//ПРАВИЛЬНО:
countMistakes++;
if (countMistakes == MAX_MISTAKES) {...} 
```

**12. class GameController**

- Нарушение конвенции кода. 

Конструктор должен стоять первым среди методов. 
Если читатель твоего кода не увидит конструктор первым методом, то подумает, что в у класса есть только конструктор по умолчанию.

- Нарушение SRP.

Класс такой же невнятный, как и `GameState`.

Что это? Здесь происходит начальное конфигурирование игры: выбор языка словаря и диалог "играть или выйти?".
Но кроме этого, здесь выполняется еще и общеигровая логика в `gameCycle()`.

Если этот класс предназначен для начального конфигурирования игры, то он не должен называться контроллером- от контроллера мы ожидаем немного другое.
И не должен содержать общеигровой логики.  
Ответственность такого класса должна состоять только в конфигурировании игры и ее запуске. 
При этом класс не должен управлять самим процессом игры.

Если же этот класс контроллер, то он должен только управлять процессом игры, но не конфигурировать сам себя путем выбора словаря и диалога "Играть или выйти?".  
В этом случае, выбор словаря и "Играть или выйти?" должны происходить выше, например, в слое "Main".  
*Мартин "ЧК", "Отделение конструирования системы от ее использования"*

Для понимания того, какие методы должен содержать этот класс, сделай такое упражнение:  
Создай несколько отдельных Main'ов.  
Один майн пусть запускает игру через выбор словаря и "играть или выйти?".  
Второй майн пусть запускает игру с английским словарем и без "играть или выйти?".  
Третий майн- то же самое, но с русским словарем.

- Нарушение DRY.

Куча методов, которые делают одно и то же: просят ввести число в заданном диапазоне.  
Раз уж все равно в программе заявлено ООП, то используй приемы ООП.  
Подробнее напишу ниже.

 **13. Диалоги**

Для рутинных действий по запросу-получению от юзера нужных данных, можно сделать универсальные классы-диалоги.

Делаем общий диалог для получения от юзера нужной информации:
```java
public interface Dialog<T> {
  T input();
}
```

Делаем реализацию для получения числа в заданных пределах:
```java
public class IntegerMinMaxDialog implements Dialog<Integer> {
  private final String title;
  private final String error;
  private final int min;
  private final int max;

  public IntegerMinMaxDialog(String title, String error, int min, int max) {...}

  @Override
  public Integer input() {
    Scanner scanner = new Scanner(System.in);
    while (true) {
     System.out.println(title);
     // Получить от юзера ответ
     // Если ответ это число в диапазоне min-max,
     // то вернуть это число через return
     System.out.println(error);
    }
  }
}
```

Использование:
```java
public class Main {
  private final static int RUSSIAN_DICTIONARY = 1;
  private final static int ENGLISH_DICTIONARY = 2;

  public static void main(String[] args) {
    String title = """
        Выберите язык словаря:
        %d. Русский
        %d. English""".formatted(RUSSIAN_DICTIONARY, ENGLISH_DICTIONARY);
    String error = "Такой опции нет! Попробуйте ещё раз.";

    Dialog<Integer> dialog = new IntegerMinMaxDialog(title, error, RUSSIAN_DICTIONARY, ENGLISH_DICTIONARY);
    int index = dialog.input();
    
    Language language = Language.values()[index-RUSSIAN_DICTIONARY];
    System.out.println("ВЫБРАН ЯЗЫК: " + language);
  }
}
```

Результат:
```
Выберите язык словаря:
1. Русский
2. English
0
Такой опции нет! Попробуйте ещё раз.
Выберите язык словаря:
1. Русский
2. English
ыцц
Такой опции нет! Попробуйте ещё раз.
Выберите язык словаря:
1. Русский
2. English
2
ВЫБРАН ЯЗЫК: EN
```

Более подробно в моем стриме [начиная с 6:28](https://t.me/zhukovsd_it_chat/53243/176204)

## АРХИТЕКТУРА

Несмотря на то, что в проекте несколько классов, он написан в процедурном стиле.  
Программа в ООП стиле должна быть декомпозирована по правилам ООП и использовать ООП подход для реализации типичных задач.  
Здесь не видно ООП декомпозиции. 
Само по себе разделение программы на *какие-то* классы, без правильной декомпозиции, еще не делает стиль написания программы объектно-ориентированным.

**Объекты**

Есть классы, которые можно посчитать объектно-оринтированными: ChoiceWord, Painter и в некоторой степени View.
Но эти классы настолько простые и очевидные, что вполне могут существовать и в программе, написанной в процедурном стиле.

Остальные классы здесь с точки зрения ООП не являются объектами, а являются контейнерами процедур. 
И в этом смысле ничем не отличаются от модулей в процедурных языках.

**Контроллеры и представления**

Среди названий классов здесь встречаются термины: View, Controller.  
Кажется, такие названия даны без понимания того, что такое контроллеры и представления.  
Здесь контроллер- не контроллер, потому что общается с юзером напрямую, а не через вью.

Вью похоже на вью, но проблема в том, что здесь не всё общение с юзером происходит через него. 
Значит, нет понимание того, для чего в ООП программе используется View.

Еще есть класс, который по своей внутренней сути не является ни моделью, ни вью- `GameState`.
Это такой же контроллер, как и GameController, но почему-то он в своем названии не подписан как контроллер.

И вообще, почему эти классы называются "View" и "Controller"? Это заявка на архитектуру MVC?  
Если так, то реализация программы в целом не соответствует архитектуре MVC.

Если под этими названиями понимается нечто иное, то лучше не использовать эти термины всуе. 
Потому что они ассоциируются с архитектурами, где к классам с такими названиями предъявляются определенные требования.

**Приёмы**

Приемы программирования здесь чаще всего процедурные.  
Про получение данных от юзеров я уже писал, теперь рассмотрим работу с языками словарей.

Здесь есть енам с языками, это вполне себе объектно-ориентированно:
```java
enum Language {
  RU,
  EN
}
```
Только нужно этот енам сделать публичным и вынести в отдельный java-файл.

Но используются языки в процедурном стиле.  
Класс GameController зависит от конкретного набора языков и при добавлении новых языков, это класс нужно будет переписывать
```java
public class GameController {
  //...
  private void setupLanguage() {
    if (currentLanguage == Language.RU) {
      choiceWord.reloadDictionary(Config.getPath("dict.ru.path"));
    } else {
      choiceWord.reloadDictionary(Config.getPath("dict.en.path"));
    }
  }
}

//ПРИ ДОБАВЛЕНИИ НОВОГО ЯЗЫКА:
... else {
    choiceWord.reloadDictionary(Config.getPath("dict.fr.path"));
  }
```

Объектно-ориентированный подход состоит в другом:  
Выбор языка влияет на набор зависимостей, а значит, это должно происходить таким образом, 
чтобы не приводило к необходимости изменять классы при добавлении новых языков.

Выбор языка и формирование зависимостей нужно проводить не в `GameController`, а выше.  
Например, в слое `Main`: 
```java
public class Main {
  //...

  public static void main(String[] args) {
    Language language = inputLanguage();
    Dictionary dictionary = DictionaryFactory.get(language);
    LetterValidator letterValidator = LetterValidatorFactory.get(language);

    String word = dictionary.get();
    Game game = new Game(word, letterValidator, ...);
    game.start();
  }
  
  private static Language inputLanguage() {...}
}
```

## ВЫВОД

Сравнить различия между ООП и процедурным стилями:  
Стрим Сергея [Крестики-нолики в процедурном стиле](https://www.youtube.com/watch?v=PPikj1qHxrA)  
Мой стрим [Крестики-нолики в ООП стиле](https://t.me/zhukovsd_it_chat/53243/187097)

Подробное объяснение, как делать эту программу в процедурном и ООП стилях, есть у Сергея в расширенных материалах.  
В том числе, там рассматривается, как делать разные уровни сложности в игре- во многом эта задача схожа с выбором языка для словаря.

n.142(260)  
#ревью #виселица 