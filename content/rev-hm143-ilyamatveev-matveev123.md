https://github.com/matveev123/Hangman  
[Ilya Matveev]

Игра в процедурном стиле, выполнена в нескольких классах.  
В целом, удручающе.

## НЕДОСТАТКИ РЕАЛИЗАЦИИ

1. Игра не началась. Она начнется, когда я введу "Y"
```
Игра началась!
Чтобы начать игру введите "Y", чтобы выйти из игры введите  "N"
```

2. В чем смысл играть в отгадывание слов, если в начале игры его подсказывать?
```
Подсказка:розеола
*******
```

3. В этом наборе палочек трудно признать изображение виселицы, где висельника вешают по частям
```
Введите букву
у
Ошибок:2
   \
    О
    | /
```

4. Нет списка введенных букв

## ХОРОШО

+ 👍 Игра запускается
+ 👍 Можно ввести только одиночную букву русского алфавита

## ЗАМЕЧАНИЯ

Хотя эта реализация получилась написанной не в ООП стиле, подозреваю, что планировалось писать именно этом стиле.  
Поэтому критика классов будет с точки зрения ООП.

**1. Нейминг**

- Константы должны быть final static
```java
final String LIST_OF_WORDS 

//ПРАВИЛЬНО:
final static String LIST_OF_WORDS 
```

- Хуже венгерской ноттации только венгерская ноттация, которая обманывает. 
Здесь в названии фигурирует слово "List", но эта переменная не является листом
```java
final String LIST_OF_WORDS = Paths.get("src", "resources", "dictionary.txt").toString();

//ПРАВИЛЬНО:
final String filepath = Paths.get("src", "resources", "dictionary.txt").toString();
```

- Почему именно "fr"? Это даже не сокращение от BufferedReader
```java
BufferedReader fr
```

- Название должно как можно лучше объяснять суть явления
```java
class Data

//ЛУЧШЕ:
class Dictionary
```

- Избыточно. Словарь, как понятие, сам по себе означает список слов. Список слов можно назвать просто "слова"
```java
public class Data {
  List<String> dictionaryFromListOfWords = new ArrayList<>();
  //...
}

//ЛУЧШЕ:
public class Data {
  List<String> words = new ArrayList<>();
  //...
}
```

- Не называй поля "переменная1", "переменная2", это совсем плохой тон. 
Всегда можно найти более осмысленные названия
```java
String message1 = "", message2 = "";
```

- Название обманывает. 

Этот метод ничего не печатает, он возвращает картинку виселицы в виде строки 
```java
public String draw(int count)
```

- Переменные нужно писать стилем camelCase. 
Стиль маленькой змеи вообще нигде в яве не применяется
```java
StringBuilder str_build

//ПРАВИЛЬНО:
StringBuilder stringBuilder
```

- Название обманывает. 

Этот метод ничего не ищет, потому что он ничего не возвращает. 
Если метод "find", то он обязан что-то найти и вернуть
```java
void findPositionOfLetter()
```

- Не сокращай названия без необходимости. 

Чтобы понять, что означает слово "patt", нужно изучить вызывающий этот метод код.  
Только тогда становится понятным, что имеется ввиду "pattern".  
Если кажется, что это и так очевидно, то нет, не очевидно
```java
public String checkPrevInputErrorLetter(int flag, String patt, String input)
```

- В названия не нужно вставлять частицы "Of", "The" и т.д. 
Если конечно там "Of" не используется в контексте valueOf()
```java
String secret, maskOfSecret;
int countOfError;

//ПРАВИЛЬНО:
String mask;
int errorCount;
```
*Oracle Java code conventions, part."Naming conventions"*  
*Мартин, "Чистый код", гл.2*  
*Ютуб, Немчинский "Как называть переменные, методы и классы?"*

**2. Нарушение конвенции кода**. 

В любой ситуации выделяй тело блока скобочками, даже если тело состоит из одной строки. 
Исключение- метод equals(), там можно после if не выделять блоки скобочками
```java
if (patt.contains(input))
  return patt;

//ПРАВИЛЬНО:
if (patt.contains(input)) {
  return patt;
}
```

**3. В одной стоке можно объявить только одну переменную**
```java
public String message1 = "", message2 = "";

//ПРАВИЛЬНО:
public String message1 = "";
public String message2 = "";
```
То же самое:
```java
public String secret, maskOfSecret, specificLetters = "Ёё";
```

**3. Если в блоке if есть return**(break, continue, throw, exit и т.д.), то else не пишется.
 
В этом случае неважно, будет else или нет, так как программа будет работать одинаково, а код без `else` будет выглядеть читабельней
```java
if (matcher.find()) {
  resultOfValid = matcher.group();
  break;
} else {
  System.out.println(message.message2);
}

//ПРАВИЛЬНО:
if (matcher.find()) {
  resultOfValid = matcher.group();
  break;
} 
System.out.println(message.message2);
```

**4. Нарушение DRY**

- Магические буквы, числа, слова. Вводи константы 
```java
if (resultOfValid.equals("Y") || resultOfValid.equals("y")) {...}
if (resultOfValid.equals("N") || resultOfValid.equals("n")) {...}

//ПРАВИЛЬНО:
private final static String START = "Y";
private final static String QUIT = "N";

if (resultOfValid.equalsIgnoreCase(START)) {...}
if (resultOfValid.equalsIgnoreCase(QUIT)) {...}
```

- Если к одной и той же магической штуке обращаются два разных класса, то делай ее константой в одном из этих двух классов. 
Либо перенеси ее в третий класс- эти данные должны быть синхронизированы между собой
```java
public class Game {
  public void startMenu() throws InterruptedException {
    if (resultOfValid.equals("Y") || resultOfValid.equals("y")) {...}  <-- Y
    if (resultOfValid.equals("N") || resultOfValid.equals("n")) {...}  <-- N
  }
}

public class Message {
  public void outputMessages(int state) {
      message2 = "Неверный ввод \n Пожалуйста, введите символ(пр. \"Y\",\"y\",\"N\",\"n\") ";  <-- Y, N
      System.out.println("Хотите перезапустить игру введите \"Y\" или \"y\"? иначе \"N\" или \"n\" она будет завершена");  <-- Y, N
      System.out.println("Хотите перезапустить игру введите \"Y\" или \"y\"? иначе \"N\" или \"n\" она будет завершена");  <-- Y, N
      System.out.println("Чтобы начать игру введите \"Y\", чтобы выйти из игры введите  \"N\"");  <-- Y, N
  }
}
```

*Фаулер, "Рефакторинг", гл.8, "Замена магического числа символической константой"*  
*refactoring.guru "Замена магического числа символьной константой"*  

**5. class Data**

- Сложные правила пользования классом.

Сейчас для использования класса нужно провести две операции: прочитать слова из файла(в память объекта) и получить случайное слово из объекта
```java
public class Data {
  public void readData() {...}
  public String takeRandomWord() {...}
}
```
Это приводит к лишним сложностям и неоднозначностям при работе с классом.  
Например, что будет, если предварительно не вызвать `readData()` и попытаться получить случайное слово через `takeRandomWord()`?  
Что будет, если два раза подряд вызвать чтение файла?
```java
Data data = new Data();
data.readData();
data.readData();
```
Загрузку слов из файла нужно проводить в конструкторе.  
В классе оставить только один публичный метод `takeRandomWord()`.

- Путь к файлу класс должен получать в конструктор либо в метод чтения файла. 

Иначе класс становится неуниверсальным.
Допустим, в программе будет несколько разных текстовых файлов- на разных языках или по разным темам. 
Тогда вместо нескольких специализированных классов Словаря, где имя файла словаря будет жестко прописано в каждом из классов, 
можно будет использовать один универсальный класс Словарь.

- Реакция на ошибки

В проекте предусмотрено, что может возникнуть исключение при чтении файла со словами.  
В этом случае просто перехватывает исключение и распечатывает его
```java
try (BufferedReader fr = new BufferedReader(new FileReader(LIST_OF_WORDS))) {
  //...
} catch (IOException ex) {
  System.out.println(ex.getMessage());
}
```

Это приводит только к тому, что в случае отсутствия файла словаря, программа аварийно закрывается:
```java
src\resources\dictionary.txt_ (Не удается найти указанный файл)
Exception in thread "main" java.lang.IllegalArgumentException: bound must be positive  <-- ЧТО ЮЗЕР ДОЛЖЕН ДЕЛАТЬ С ЭТОЙ ИНФОРМАЦИЕЙ?
	at java.base/java.util.Random.nextInt(Random.java:551)
```
Здесь должна быть другая реакция на отсутствие файла со словами.  
Нужно сказать юзеру, что файл со словами по указанному пути открыть не удалось и поэтому работа программы будет завершена. 

Причем, путь нужно указать не относительный, как у тебя(`src\resources\dictionary.txt_`).  
А абсолютный, чтобы юзер знал, где конкретно ему нужно искать файл: `c:/..../dictionary.txt_`.

После этого корректно завершить работу программы без вываливания красных эксепшенов в консоль.

- Не стримись засунуть несколько инструкций в одну строку, особенно, когда используются неадекватно длинные названия перменных. 
Приоритет- понятность кода, а не экономия строк
```java
return dictionaryFromListOfWords.get((random.nextInt(dictionaryFromListOfWords.size())));

//ПРАВИЛЬНО:
int max = dictionaryFromListOfWords.size();
int wordIndex = random.nextInt(max);
return dictionaryFromListOfWords.get(wordIndex);
```
*Фаулер, "Рефакторинг", гл.6, "Введение поясняющей переменной"*

**6. class Message**

- Вводящий в заблуждение нейминг.

Это не "Сообщение", это "Принтер сообщений". Класс не хранит сообщения, он их печатает.  

Хотя, немножечко ещё и хранит, конечно. В том смысле, что предоставляет клиентскому коду доступ к своим полям. 
Но это только еще один довод в пользу того, что класс плохо спроектирован: класс должен или печатать сообщения, или их возвращать.

Единственный публичный метод класса заявлен именно как метод вывода(печати):
```java
public void outputMessages(int state) {...}
```

- Нарушение инкапсуляции
```java
public String message1 = "", message2 = "";
```
Все поля должны быть приватными, а доступ к ним- только через геттеры и сеттеры
Для прямого использования потомками, некоторые поля могут быть `protected`.  
*Блох "Java. Эффективное программирование", изд.3, гл.4.2*

- Не парься так с этими двойными скобочками
```java
System.out.println("Хотите перезапустить игру введите \"Y\" или \"y\"? иначе \"N\" или \"n\" она будет завершена");

//ПРАВИЛЬНО:
System.out.println("Хотите перезапустить игру введите 'Y' или 'y'? иначе 'N' или 'n' она будет завершена");
```

- Метод делает настолько разное, что вообще не ясно, что это такое
```java
public String message1 = "", message2 = "";

public void outputMessages(int state) {
  if (state == 2) {
    message1 = "Введите букву";
    message2 = "Неверный ввод! \n Пожалуйста, введите букву(пр. \"а\",\"б\",\"г\") ";
  } else {
    //еще что-то устанавливает в message1 и message2
  }
  if (state == 3) {
    System.out.println("Поздравляем! Вы выйграли ");
    System.out.println("Хотите перезапустить игру введите \"Y\" или \"y\"? иначе \"N\" или \"n\" она будет завершена");
  }
  if (state == 4) {
   System.out.println("Вы проиграли!");
   System.out.println("Хотите перезапустить игру введите \"Y\" или \"y\"? иначе \"N\" или \"n\" она будет завершена");
  }
  if (state == 0) {
    //еще что-то печатает
  }
}
```
По входящему ключу меод или устанавливает значения полей класса, или печатает сообщения.
Значит, метод уже делает два дела, а значит, его нужно разделить на два.

Далее, метод печатает разное, опять, же писходя из значния ключа.  
Значит его снова нужно разделить на несколько, каждый из которых будет печатать свое:
```java
public void printОсмысленноеНазваниеMessage() {
  System.out.println("Поздравляем! Вы выйграли ");
  System.out.println("Хотите перезапустить игру введите \"Y\" или \"y\"? иначе \"N\" или \"n\" она будет завершена");
}

public void printДругоеОсмысленноеНазваниеMessage() {
   System.out.println("Вы проиграли!");
   System.out.println("Хотите перезапустить игру введите \"Y\" или \"y\"? иначе \"N\" или \"n\" она будет завершена");
}
```
Один миетод должен делать одно дело.  
*"ЧК", гл.3, "Правило одной операции"*

- Нарушение DRY. Дублирование теста
```java
if (state == 3) {
  //...
  System.out.println("Хотите перезапустить игру введите \"Y\" или \"y\"? иначе \"N\" или \"n\" она будет завершена");
}
if (state == 4) {
  //...
  System.out.println("Хотите перезапустить игру введите \"Y\" или \"y\"? иначе \"N\" или \"n\" она будет завершена");
}
```

**7. class Input**

Что делает этот метод и как он работает, мне вообще непонятно. Ясно одно- он НЕ вводит валидацию
```java
public String inputValidation(String inputFromUser, String changeablePattern, int state) {...}
```
Метод непонятно зачем принимает кучу аргументов, алгоритм его работы избыточно сложный, название не соответствует тому, что он делает.
Конечно, разобраться с тем, что это такое, можно, но на это уйдет минут пять.

Если на поняимание одного метода нужно уделить столько времени, значит с методом что-то сильно не так.

Плохое название метода усугубляется плохим названием класса- из него мы можем понять, что это это ввод. 
Но не можем понять, ввод чего.

**8. class Human**

+ 👍 Хранить картинки хангмана в отдельном классе это хорошо, это разгружает классы с логикой от графики.

- Вот это совсем не читается. Нет задачи экономить строчки, главная задача- писать понятный код, который при необходимости можно легко изменить
```java
return "   \\\n    О\n" + "  \\ | /\n" + "    |\n" + "   / \\";

//ПРАВИЛЬНО:
return """
   -----   
   |   |   
   |   O   
   |       
   |       
  ------- 
  """;
```

- Не делай, как проще, делай, как надо.

Сейчас картинки распечатываются таким образом, что в них трудно узнать изображение висельника
```
   \
    О
    | /
```
Не думаю, что ты печатешь именно так потому, что это красиво- потому что это не красиво.  
Скорее всего, ты печатаешь так, потому что так проще хранить картинки в одной плоскости
```java
return "   \\\n    О\n" + "  \\ | /\n" + "    |\n" + "   / \\";
```
То есть, UI был пренесен в жертву техническим сложностям.

Это неправильный подход, пользователь программы не должен оказаться крайним.  
В коде храни картинку в двух плоскостях, а не в одной.

- В любом switch-case должен быть default. В данном случае default должен бросать исключение с сообщением, что такого номера картинки не существует.

- Выдавать картинки виселицы через switch-case или if-elseif - индусский код. 

Картинки нужно хранить в статическом массиве и выдавать по номеру.  
Например, так
```java
private static final String[] PICTURES = {
    """
    -----   
    |       
    |       
    |       
    |       
    ------- 
    """,
   """
     -----   
     |   |   
     |   O   
     |       
     |       
     ------- 
     """
   ,
   // more pics
};

public void get(int numPicture) {  
  return PICTURES[numPicture];
}
```

**9. class Symbol**

- Нарушение инкапсуляции.

Посторонние классы взаимодействуют с этими полями напрямую, минуя дозволенные классом методы. 
Поэтому `class Symbol` вообще не контролирует свое внутреннее устройство от произвола посторонних классов
```java
public class Symbol {
  public String secret, maskOfSecret, specificLetters = "Ёё";
  public char charToGuess;
  public int[] countOfDuplicates = new int[10];
  //...
}
```

- Процедурный стиль.

С точки зрения ООП, класс не представляет из себя какой-то внятной сущности.  
Это хранилище публичных полей и сборник методов в стиле процедурного программирования.  
Критиковать здесь *с точки зрения ООП* нечего, потому что это не объект в понимании ООП.

- Создавай вспомогательные методы, делай программу более простой и понятной
```java
if (this.maskOfSecret.contains(String.valueOf(charToGuess)) || this.maskOfSecret.contains(this.specificLetters)) {...}

//ПРАВИЛЬНО:
if (isНазваниеКотороеВсеОбъясняет(...)) {...}
```

- Правило одной операции

Кждый метод должен делать одно дело.  
Этот метод обещает заменить букву, это одна операция
```java
public void replaceSymbol() {...}
```
На самом деле, метод делает несколько операций:  
Проверяет, вводили ли букву ранее.  
Выводит сообщение, если вводили букву ранее.  
Заменяет(открывает) букву.

Раздели метод на несколько, каждый из которых будет делать только одно.

- Абсолютно непонятный метод.

Даже комментарий не помогает понять что происходит в этом методе
```java
public String checkPrevInputErrorLetter(int flag, String patt, String input) {// здесь делаем!
```

Этот метод стоит показать целиком:
```java
public String checkPrevInputErrorLetter(int flag, String patt, String input) {// здесь делаем!
  if (flag >= 1) {//после добавления буквы к шаблону
    if (patt.contains(input))
      return patt;
    StringBuilder str_build = new StringBuilder(patt);
    str_build.insert(15, input);
    patt = str_build.toString();
  } else {//меняем первый раз шаблон
    StringBuilder str_build = new StringBuilder(patt);
    str_build.insert(10, "&&[^" + input + "]");
    patt = str_build.toString();
  }
  return patt;
}
```
Что здесь происходит, я даже не хочу разбираться.  
Непонятные аргументы("flag"), непонятный нейминг("patt"), магические числа, название метода которое не объясняет что делает метод а что он делает из кода совсем неясно, 
отсутствие скобок, комментарии которые оставляют больше вопросов чем дают ответов...

Флаги в методах это одельное зло. Никогда не используй флаги: *"ЧК", гл.3, "Аргументы-флаги"*.

**10. class Game**

- Это нечитаемо
```java
public String inputFromUser = "", letterToGuess = "", resultOfValid = "", patternToCheckDuplicates = PATTERN_OF_GUESS_CHECK, changeablePattern = "", test = "";
```

- Нарушение DRY, дублирование одинакового кода в разных методах
```java
public void startMenu() throws InterruptedException {
  if (resultOfValid.equals("Y") || resultOfValid.equals("y")) {...}
  if (resultOfValid.equals("N") || resultOfValid.equals("n")) {...}
}

int chooseUser(int countOfError) throws InterruptedException {
  if (resultOfValid.equals("Y") || resultOfValid.equals("y")) {...}
  if (resultOfValid.equals("N") || resultOfValid.equals("n")) {...}
}
```

- Большой божественный метод на 70 строк. 
Метод делает много разного, нужно разделить на несколько отдельных, каждый из которых будет делать что-то свое
```java
public void guessLetter() throws InterruptedException {...}
```
Такие методы плохи не потому, что они большие. Они большие потому, что плохие: *"ЧК", гл.3, "Компактность!"*. 

- Комментарии.

Комментарии в божественном методе(да и везде в этом проекте) в основном пытаются компенсировать плохой код
```java
patternToCheckDuplicates = symbol.checkPrevInputErrorLetter(countOfError, patternToCheckDuplicates, letterToGuess);// отказ засчитывания ошибки на дубликат символа от User!
```
Когда в проекте много каментов, это плохо- пользы от них практически нет, они только забивают пространство и мешают читать код.  
В идеале, комментариев вообще не должно быть, код должен объяснять сам себя через правильный нейминг и лаконичный код.  
*Мартин, "ЧК", гл.4* 

- Используй только постфиксную форму инкремента(i++). 
Иначе велика вероятность перепутать и получить баг из-за различий в работе постфиксной и префиксной формы
```java
System.out.println("Ошибок:" + ++countOfError);

//ПРАВИЛЬНО:
countOfError++;
System.out.println("Ошибок:" + countOfError);
```

## АРХИТЕКТУРА

Судя по структуре проекта, была явная попытка написать проект в объектно-ориентированном стиле.  
Но несмотря на то, что в проекте несколько классов, он написан в процедурном стиле.   
Программа в ООП стиле должна быть декомпозирована по правилам ООП и использовать ООП подход для реализации типичных задач.  
Здесь не видно ООП декомпозиции, а используемые приемы совершенно процедурные.

Я часто критикую реализации, что в них нет ООП, даже там, где он немножечко есть на уровне "уже не процедурный стиль, но еще не вполне объектно-ориенированный".  
В этой реализации ООП совсем-совсем-совсем нет.

Пишу это не с целью абъюза. А для того, чтобы было ясно, на каком этапе понимания программирования сейчас находишься и что нужно проработать.

## ВЫВОД
---
Я бы посоветовал для начала написать хангмана в процедурном стиле. 
Там отработать недостатки этого проекта, которые являются общими для всех стилей:
- Нейминг
- Методы, которые делают разное
- Сложные непонятные алгоритмы простых действий
- Дублирования кода

На этом этапе учебы, написание хорошего процедурного хангмана принесет тебе больше пользы, чем попытка переработать эту реализацию до уровня нормального ООП.  
Навыки процедурного программирования важны для дальнейшего освоения ООП.
---
Есть простейшие нарушения конвенции кода. 
Сначала нужно изучить *Oracle Java code conventions*.

Изучить основы ООП: зачем нужна инкапсуляция, геттеры и сеттеры.

Для развития навыков процедурной декомпозиции, посмотреть стрим Сергея [Крестики-нолики в процедурном стиле](https://www.youtube.com/watch?v=PPikj1qHxrA)

Сравнить различия между ООП и процедурным стилями:  
Стрим Сергея [Крестики-нолики в процедурном стиле](https://www.youtube.com/watch?v=PPikj1qHxrA)  
Мой стрим [Крестики-нолики в ООП стиле](https://t.me/zhukovsd_it_chat/53243/187097)

Подробное объяснение, как делать эту программу в процедурном и ООП стилях, есть у Сергея в расширенных материалах.

n.143(262)  
#ревью #виселица