https://github.com/CTY6A/hangman  
[Vadzim Stubeda]

Игра в процедурном стиле, выполнена в нескольких классах.

## НЕДОСТАТКИ РЕАЛИЗАЦИИ

1. Команды работают неправильно. 
Если заявлено использование двух команд, в данном случае 'y' и 'n', то любая другая команда должна считаться ошибочной
```
Would you like to play new game? (Y/N)
start
Thank you for playing! Goodbye!

Process finished with exit code 0
```

2. Весь интерфейс на английском, а угадываемые слова- на русском
```
Enter a letter: ц
Letter not in the word!
  +---+
  |   |
  O   |
 /|\  |
 / \  |
      |
=========
Word: _ а _ _ _ к а 
Mistakes (6/6): ф, ы, в, п, у, ц
------------------------------
Game over! You lost.
The word was: бабочка
```

## ХОРОШО

+ 👍 Игра запускается
+ 👍 Можно ввести только одиночную букву русского алфавита
+ 👍 Есть список введенных букв
+ 👍 Простой понятный алгоритм

## ЗАМЕЧАНИЯ

**1. Нейминг**

- Если метод что-то проверяет(check), то результат проверки он должен возвращать в виде `boolean`. 
Иначе, он не должен так называться
```
void checkGameState()
```

- Буква это один символ, строка- много символов
```
String letter;

//ПРАВИЛЬНО:
char letter;
```
*Oracle Java code conventions, part."Naming conventions"*  
*Мартин, "Чистый код", гл.2*  
*Ютуб, Немчинский "Как называть переменные, методы и классы?"*

**2. Нарушение DRY**, магические буквы, числа, слова. 

- Вводи константы 
```
System.out.println("Would you like to play new game? (Y/N)");
if (!scanner.nextLine().equalsIgnoreCase("Y")) {...}

if (letter.charAt(0) >= 'а' && letter.charAt(0) <= 'я' || letter.charAt(0) == 'ё') {...}
System.out.println("Please enter a Russian letter (а-я, ё).");

//ПРАВИЛЬНО:
private final static String START = "Y";
private final static String QUIT = "N";

private final static char FIRST_LETTER = "а";
private final static char LAST_LETTER = "я";
private final static char SPECIAL_LETTER = "ё";

System.out.printf("Would you like to play new game? (%s/%s) \n", START, QUIT);
if (!scanner.nextLine().equalsIgnoreCase(START)) {...}

if (letter.charAt(0) >= FIRST_LETTER && letter.charAt(0) <= LAST_LETTER || letter.charAt(0) == SPECIAL_LETTER) {...}
System.out.printf("Please enter a Russian letter (%c-%c, %c).  \n", FIRST_LETTER, LAST_LETTER, SPECIAL_LETTER);
```

- Если константы уже есть- пользуйся ими 
```
public static final int MAX_COUNT_MISTAKES = 6;

System.out.println("Rules: guess the word letter by letter. You have 6 attempts.");

//ПРАВИЛЬНО:
System.out.printf("Rules: guess the word letter by letter. You have %d attempts. \n", MAX_COUNT_MISTAKES);
```
*Фаулер, "Рефакторинг", гл.8, "Замена магического числа символической константой"*  
*refactoring.guru "Замена магического числа символьной константой"*

**3. Форматирование строк**

Если нужно печатать или создавать строку с более, чем одним подстановочным значением или значение вставляется внутрь сообщения, используй форматирование- тогда сразу будет виден весь шаблон
```
System.out.println("Mistakes (" + mistakeCounter + "/" + MAX_COUNT_MISTAKES + "): " +
   String.join(", ", wrongLetters.stream()
     .map(String::valueOf)
     .toArray(String[]::new)));

//ПРАВИЛЬНО:
String wrongLettersText = wrongLetters.stream()
     .map(String::valueOf)
     .toArray(String[]::new);

System.out.printf("Mistakes (%d/%d): %s  \n", mistakeCounter, MAX_COUNT_MISTAKES, wrongLettersText);
```

**4. Приоритет- читаемость кода**

Не экономь строки, не пиши в условии if'ов сложные конструкции.  
Код должен читаться легко, вводи поясняющие переменные и/или вспомогательные методы:
```
if (!scanner.nextLine().equalsIgnoreCase(START)) {...}

//ПРАВИЛЬНО:
String command = scanner.nextLine();
if (command.equalsIgnoreCase(START)) {...}

//ЕЩЕ ЛУЧШЕ:
String command = scanner.nextLine();
if (isStartCommand(command)) {...}

private boolean isStartCommand(String command) {
  return command.equalsIgnoreCase(START);
}
private boolean isQuitCommand(String command) {
  return command.equalsIgnoreCase(QUIT);
}
```

**5. Объявляй переменные там, где они используются**

Минимизируй область видимости локальных переменных
```
String letter;
while (true) {
  letter = scanner.nextLine().toLowerCase();
  //...
}

//ПРАВИЛЬНО:
while (true) {
  String line = scanner.nextLine().toLowerCase();
  //...
}
```
*Блох, "Java эффективное программирование" гл.9.1*

**6. Если в блоке if есть return**(break, continue, throw, exit и т.д.), то else не пишется.  
В этом случае неважно, будет else или нет, так как программа будет работать одинаково и код без `else` будет выглядеть читабельней
```
if (letter.charAt(0) >= 'а' && letter.charAt(0) <= 'я' || letter.charAt(0) == 'ё') {
  break;
} else {
  System.out.println("Please enter a Russian letter (а-я, ё).");
}

//ПРАВИЛЬНО:
if (letter.charAt(0) >= 'а' && letter.charAt(0) <= 'я' || letter.charAt(0) == 'ё') {
  break;
} 
System.out.println("Please enter a Russian letter (а-я, ё).");
```

**7. class Dictionary**

- Нарушение ООП дизайна.

Это поле не должно быть static
```
private static List<String> words;

//ПРАВИЛЬНО:
private List<String> words;
```
Статические поля в ООП программе нужно применять очень осторожно. 
Потому что сама идея классов состоит в том, что у класса может быть несколько экземпляров одновременно. 
И при этом все они должны корректно работать.

В данном случае если в проекте будет существовать несколько экземпляров класса одновременно, они будут работать некорректно. 
Потому что будут одновременно вносить изменения в одно и то же статическое поле.

И тут вопрос не в том, должно ли *конкретно в этом* проекте быть два экземпляра одного класса, или не должно. 
Если класс не утилитный, не синглтон и можно создать несколько его экземпляров, то по умолчанию мы признаем, что его экземпляров в проекте может быть более одного.

+ 👍 Хорошо, что путь к файлу класс получает в конструктор- поэтому класс может читать разные файлы. Например, файлы со словарями на разных языках.
```
public Dictionary(String filename)
```

+ 👍 Реакция на ошибки

Если по указанному адресу  не будет найден файл со словами, то программа выводит соответствующее сообщение и корректно завершается. Это хорошо 
```
Error reading dictionary file: src\main\resources\russian_words.txt_
```
Но путь к файлу нужно указать не относительный, как у тебя(`src\main\resources\russian_words.txt_`), а абсолютный. 
Чтобы юзер знал, где конкретно ему нужно искать файл: `c:/..../src/main/resources/russian_words.txt_`.


**8. enum ScaffoldState**, енам с картинками этапов повешения

- Используй многострочные строки
```
return "  +---+" + "\n" +
       "  |   |" + "\n" +
       "      |" + "\n" +
       "      |" + "\n" +
       "=========";

//ПРАВИЛЬНО:
return """
       +---+
       |   |
           |
           |
     =========";
     """
```
Многострочные строки(текстовые блоки) имеются в java начиная с 13 версии.

- Неправильное использование енамов
```
public enum ScaffoldState {
  ZERO {
    @Override
    public String toString() {
      return "  +---+" + "\n" +
          "  |   |" + "\n" +
          "      |" + "\n" +
          //остальная часть картинки 0
    }
  },
  ONE {
    @Override
    public String toString() {
      return "  +---+" + "\n" +
          "  |   |" + "\n" +
          "  O   |" + "\n" +
          //остальная часть картинки 1
    }
  //...
  }
}  
```

Здесь картинки виселицы используются клиентским кодом не персонально как константы. 
То есть не так, например: `System.out.println(ScaffoldState.ONE);`

А в виде массива:
```
System.out.println(ScaffoldState.values()[mistakeCounter]);
```
Более того, персонально использоваться как константы эти картинки и не планируются.  
А значит, выбранный способ их хранения в енаме- неправильный.

Если картинки все равно используются в виде массива, то почему бы их сразу не хранить в массиве? Например так
```
public final class HangmanPicture {
  <закрытый конструктор>

  private static final String[] PICTURES = {
      """
       +---+
       |   |
           |
           |
           |
     =========";
     """
      ,
      """
       +---+
       |   |
       0   |
           |
           |
     =========";
     """
   // more pics
  };

  public static String get(int pictureNumber) {  
    return PICTURES[pictureNumber]; 
  }
}
```
Преимущество: код проще и понятнее, добавление и удаление каринок происходит легче.

В этом случае работа с картинками тоже будет проще, красивее и правильнее:
```
//БЫЛО:
System.out.println(ScaffoldState.values()[mistakeCounter]);

//СТАЛО:
System.out.println(HangmanPicture.get(mistakeCounter));
```

**9. enum GameState**, текущий этап игры
```
public enum GameState {
  NOT_FINISHED,
  WON,
  LOST
}
```

Нет необходимости использовать енам с этапами игры для промежуточных вычислений- это делает программу более громоздкой, а алгоритм избыточным.  
Этапы игры нужно определять с помощью вспомогательных методов и тут же реагировать на эти этапы, а не кешировать их:
```
boolean gameWon = //логика определения выигрыша;

if (gameWon) {
  gameState = GameState.WON;
} 

if (gameState == GameState.WON) {
  System.out.println("Congratulations! You won!");
}

//ПРАВИЛЬНО:

if(isWon()) {
  System.out.println("Congratulations! You won!");  
}

private boolean isWon() {
  return //логика определения выигрыша;
}
```

**10. class Game**, основная игровая логика

+ 👍 Принимает словарь в конструктор, а не создает его самостоятельно, это хорошо
```
public Game(Dictionary dictionary)
```

- Диалог "Играть или выйти?" нужно вести не в этом классе, а на уровень выше. 

Потому что, если юзер выберет "выйти", то экземпляр этого класса создавался зря
```
System.out.println("Would you like to play new game? (Y/N)");
```

- Избыточно

Чтобы напечатать разделительную линию в 30 символов, каждый раз эта линия заново формируется через `repeat`.  
Сделай эту линию только один раз
```
public void printGameState() {
  //...  
  System.out.println("-".repeat(30)); 
}

//ПРАВИЛЬНО:
private final static String LINE = "-".repeat(30);

public void printGameState() {
  //...  
  System.out.println(LINE);
}
```

- Сделай вспомогательные методы для определения текущего состояния игры. Например, так:
```
while (!isGameOver()) {...}

boolean isWon() {...}

boolean isLose() {
  return mistakeCounter >= MAX_COUNT_MISTAKES;  
}

boolean isGameOver() {
  return  isWon() || isLose();
}
```

- Создавай вспомогательные методы, делай программу более простой и понятной
```
if (letter.charAt(0) >= 'а' && letter.charAt(0) <= 'я' || letter.charAt(0) == 'ё') {...}

//ПРАВИЛЬНО:
char symbol = line.charAt(0);
if (isRussianLetter(symbol)) {...}

private boolean isRussianLetter(char symbol) {
  char c = Character.toLowerCase(symbol);
  return c >= 'а' && c <= 'я' || c == 'ё';
}
```

- Буква должна быть char, потому что буква это один символ
```
private String inputLetter() {
  String letter;
  while (true) {
    //...
    if (letter.charAt(0) >= 'а' && letter.charAt(0) <= 'я' || letter.charAt(0) == 'ё') {
      break;
    } else {
      System.out.println("Please enter a Russian letter (а-я, ё).");
    }
  }
  return letter;
}

//ПРАВИЛЬНО:
private char inputRussianLetter() {
  while (true) {
    //...
    if (isRussianLetter(symbol)) {
      return symbol;
    } 
    System.out.printf("Please enter a Russian letter (%c-%c, %c).  \n", ...);
  }
}
```

**11. class Main**, содержит точку входа main

+ 👍 Только создает и запускает игру, это хорошо. 

+ 👍 Создает и инжектит в игру Словарь, это хорошо.

- Не нужно всегда стремиться несколько инструкций написать в одну строку.  
Приоритет- читаемость:
```
public static void main(String[] args) {
   Dictionary dictionary = new Dictionary("russian_words.txt");

   (new Game(dictionary)).gameLoop();
}

//ПРАВИЛЬНО:
public static void main(String[] args) {
   Dictionary dictionary = new Dictionary("russian_words.txt");

   Game game = new Game(dictionary);
   game.gameLoop();
}
```

## АРХИТЕКТУРА

Не знаю, как планировалось, но игра сделана в процедурном стиле. 
Наличие нескольких классов само по себе не делает программу объектно-ориентированной.  

Текущее разбиение на классы может быть и при процедурном стиле. 
В декомпозиции классов сейчас не видно специфики идей ООП.

## ВЫВОД

В целом, норм.

Сравнить различия между ООП и процедурным стилями:  
Стрим Сергея [Крестики-нолики в процедурном стиле](https://www.youtube.com/watch?v=PPikj1qHxrA)  
Мой стрим [Крестики-нолики в ООП стиле](https://t.me/zhukovsd_it_chat/53243/187097)

Подробное объяснение, как делать эту программу в процедурном и ООП стилях, есть у Сергея в расширенных материалах.

n.139(254)  
#ревью #виселица