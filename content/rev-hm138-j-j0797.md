https://github.com/j0797/hangman-clean/blob/main/src/Hangman.java  
[J.]

Игра в процедурном стиле, выполнена в одном классе.

## НЕДОСТАТКИ РЕАЛИЗАЦИИ

1. Команды работают неправильно.

Если заявлено использование двух команд, в данном случае '1' и '2', то любая другая команда должна считаться ошибочной
```
1 - начать новую игру
2 - выйти из игры
Выберите действие: 
123абырвалг
Загружено 420 слов из файла

Игра началась! У вас 6 попыток
```

2. Можно ввести не одну букву, а набор букв и это будет считаться допустимым вводом
```
Введите букву русского алфавита: ывап
Буквы 'ы' нет в этом слове
```

3. Неправильная последовательность в порядке отображения введенных букв: букву 'ц' я ввел второй, но в распечатке она стала первой. 
При этом введенная третьей буква 'к' в распечатке стала на третью позицию
```
Введите букву русского алфавита: й
Использованные буквы: й 

Введите букву русского алфавита: ц
Использованные буквы: ц й 

Введите букву русского алфавита: к
Использованные буквы: ц й к 
```
Введенные буквы должны размещаться по алфавиту(й, к, ц) либо в порядке ввода(й, ц, к).

## ХОРОШО

+ 👍 Игра запускается
+ 👍 Можно ввести только буквы русского алфавита
+ 👍 Есть список введенных букв

## ЗАМЕЧАНИЯ

**0. Давай правильную ссылку на репозиторий**
```
https://github.com/j0797/hangman-clean/blob/main/src/Hangman.java

//ПРАВИЛЬНО:
https://github.com/j0797/hangman-clean
```

**1. Нейминг**

- Избыточно. 

Уточнять, что слова валидные, здесь нужно только в том случае, если бы где-то был список невалидных слов. 
И тогда префикс "valid" в `validWords` был бы уточнением по сравнению с `invalidWords`.  
Но здесь в программе есть только список валидных слов
```
List<String> validWords = new ArrayList<>();

//ПРАВИЛЬНО:
List<String> words = new ArrayList<>();
```

- Название метода должно объяснять, что делает метод. А не то, *для каких целей метод используется клиентом*- это не всегда одно и то же
```
private static char readMenuChoice(Scanner scanner) {
  String input = scanner.next().toLowerCase();
  return input.charAt(0);
}

//ПРАВИЛЬНО:
private static char inputChar(Scanner scanner)
```

- Для одной концепции используй один термин. 

Оба эти метода делают одно и то же- распечатывают информацию.
Оба они должны быть или "draw" или "display"
```
void displayMainMenu()
void drawHangman(int wrongAttempts)
```

- Название должно объяснять, что делает метод
```
char readPlayerGuess(Scanner scanner)

//ПРАВИЛЬНО:
char inputRussianLetter(Scanner scanner)
```
*Oracle Java code conventions, part."Naming conventions"*  
*Мартин, "Чистый код", гл.2*  
*Ютуб, Немчинский "Как называть переменные, методы и классы?"*

**2. Нарушение DRY**, магические буквы, числа, слова. Вводи константы 
```
case '1':  //...
case '2':  //...
System.out.println("Неверный выбор. Пожалуйста, введите '1' или '2'");

System.out.println("1 - начать новую игру");
System.out.println("2 - выйти из игры");


//ПРАВИЛЬНО:
private final static char START = '1';
private final static char QUIT = '2';

case START:  //...
case QUIT:  //...
System.out.printf("Неверный выбор. Пожалуйста, введите '%c' или '%c'  \n", START, QUIT);

System.out.println(START + " - начать новую игру");
System.out.println(QUIT + " - выйти из игры");
```
*Фаулер, "Рефакторинг", гл.8, "Замена магического числа символической константой"*  
*refactoring.guru "Замена магического числа символьной константой"*

**3. Если нужно печатать или создавать строку** с более, чем одним подстановочным значением или значение вставляется внутрь сообщения, используй форматированный вывод- тогда сразу будет виден весь шаблон
```
System.out.println("Предупреждение: Строка " + lineNumber + " содержит некорректные символы: " + line);

//ПРАВИЛЬНО:
System.out.printf("Предупреждение: Строка %d содержит некорректные символы: %s  \n", lineNumber, line);
```

**4. Печать картинок**

- В любом switch-case должен быть `default`. 
В данном случае `default` должен бросать исключение с сообщением, что такого номера картинки не существует.

- Печать картинки виселицы через switch-case или if-elseif - индусский код
```
switch (wrongAttempts) {
  case 0:
    System.out.println("  _______");
    System.out.println("  |     |");
    //остальная часть картинки 1
    break;
  case 1:
    System.out.println("  _______");
    System.out.println("  |     |");
    //остальная часть картинки 2
    break;
  //...
}  
```
Картинки нужно хранить в статическом массиве и печатать по номеру.  
Например, так
```
private static final String[] PICTURES = {
    """
    -----   
    |       
    |       
    |       
    |       
    ------- 
    """,
   """
     -----   
     |   |   
     |   O   
     |       
     |       
     ------- 
     """
   ,
   // more pics
};

public void printPicture(int numPicture) {  
  System.out.println(PICTURES[numPicture]);
}
```
Желательно картинки и метод распечатки вынести в отдельный класс, чтобы в основном классе не смешивать логику и графику.  
Процедурный стиль дозволяет делать проекты, состоящие из нескольких классов.

**5. Реакция на ошибки** 

- Если по указанному адресу не будет найден файл со словами, то программа напечатает соответствующее сообщение и... снова выведет стартовое меню
```
Ошибка: Файл со словами не найден: resources/words.txt_
Не удалось загрузить слова для игры

=== ИГРА ВИСЕЛИЦА ===
1 - начать новую игру
2 - выйти из игры
Выберите действие: 
```
Это не имеет практического смысла- при попытке снова начать игру, произойдет то же самое.

Здесь должна быть другая реакция на отсутствие файла со словами.  
Нужно сказать юзеру, что файл со словами по указанному пути открыть не удалось и поэтому работа программы будет завершена. 

Причем, путь нужно указать не относительный, как у тебя(`resources/words.txt_`). 
А абсолютный, чтобы юзер знал, где конкретно ему нужно искать файл: `c:/..../resources/words.txt_`.

После этого корректно завершить работу программы.

**6. Использование изменяемых полей класса при процедурном стиле программирования**

Сейчас в классе нет изменяемых полей, а есть только константы.  
Но программе кроме констант нужны еще и изменяемые данные. 
Причем эти данные нужны не локально, не только в рамках метода. 
А для разных частей алгоритма игры, то есть, для многих методов.

Это отгадываемое слово и его маска, перечень использованных букв, счетчик ошибок и прочее.

Сейчас в рамках класса нет таких "глобальных" полей.
Эти данные сейчас сделаны переменными методов и они перекидываются из метода в метод через входящие аргументы.

Это приводит к тому, что сигнатура таких методов сильно разбухает- приходится принимать во входящие кучу "общих" данных
```
private static GameStateUpdateResult processPlayerGuess(char letter, String secretWord, String maskedWord, Set<Character> usedLetters, int wrongAttemptsCount) 
```
Код становится более сложным и хуже читаем по сравнению с тем, если бы данные принимались не в аргументы метода, а брались из окружения класса.  
Условный пример:
```
//ПЛОХО:
private void openLetter(String word, StringBuilder mask, char letter) {
  //открывает букву letter в маске,
  //если эта буква присутствует в слове
}

//ХОРОШО:
private String word;
private StringBuilder mask;

private void openLetter(char letter) {
  //открывает букву letter в маске,
  //если эта буква присутствует в слове
}
```

Статические изменяемые поля не позволяют корректно работать двум одновременно запущенным экземплярам одного и того же класса.  
Если это беспокоит, то выход есть- пиши в процедурном стиле, но создавай объекты, где изменяемые поля класса будут не статическими.  
Типа такого, это все еще не противоречит процедурному стилю:
```
public class Game {

  private String word;
  private StringBuilder mask;

  public static void main(String[] args) {
    Game game = new Game();
    game.start();
  }

  private void start() {
    word = "привет";
    mask = toMask(word);
    //...
  }
}
```

Хотя, на мой взгляд, в рамках простой процедурной программы, которая состоит из одного класса только со статическими методами, 
изменяемые поля можно делать просто `static`. 
Например, так:
```
public class Hangman {
  private static final int MAX_ATTEMPTS = 6;  <-- НЕИЗМЕНЯЕМОЕ ПОЛЕ
  private static String word;  <-- ИЗМЕНЯЕМОЕ ПОЛЕ
  //...
}
```
Многие дальнейшие проблемы реализации вытекают именно из этого пункта.

**7. Внутренний класс**

Использование внутреннего класса смотрится негармонично для такой мега-процедурной реализации, где все методы статические
```
public class Hangman {

  private static final int MAX_ATTEMPTS = 6;
  private static final String WORDS_FILE_PATH = "resources/words.txt";

  public static void main(String[] args) {...}  <-- ТОЧКА ВХОДА

  //тут статические методы

  private static class GameStateUpdateResult {...} <-- внутренний класс

 //тут тоже статические методы  
}
```

Как я писал выше, программа в процедурном стиле действительно может состоять из нескольких классов.

Но здесь использование внутреннего класса вызывает вопросы:  
Почему используется внутренний класс при подчеркнуто процедурном стиле `class Hangman`, в котором нет даже состояния, то есть изменяемых полей класса?  
Нужно определиться: или в трусах, или с крестиком.

Моё предложение: *в этой реализации* придерживайся чистоты МАХРОВОГО процедурного стиля, раз уж начал.  
Для этого убрать `class GameStateUpdateResult`, а общие для алгоритма изменяемые данные сделать `private static не-final`:
```
//БЫЛО:
public class Hangman {

  private static final int MAX_ATTEMPTS = 6;
  private static final String WORDS_FILE_PATH = "resources/words.txt";

  private static class GameStateUpdateResult {
    String maskedWord;
    int wrongAttemptsCount; 
    //...
  }
}

//СТАНЕТ:
public class Hangman {

  private static final int MAX_ATTEMPTS = 6;
  private static final String WORDS_FILE_PATH = "resources/words.txt";
  private static String maskedWord;
  private static int wrongAttemptsCount; 
  //...
}
```

**8. Промежуточные переменные vs вспомогательные методы**

Здесь лучше не использвать промежуточные переменные для хранения текущего этапа игры- выиграл/проиграл/в_игре. 
Лучше использовать вспомогательные методы, которые будут определять текущий этап:
```
boolean isRoundActive = true;
boolean isGameWon = false;

while (isRoundActive) {
  isRoundActive = result.isRoundActive;
  isGameWon = result.isGameWon;

  //...
  if (isWordGuessed(maskedWord)) {
    isRoundActive = false;
    isGameWon = true;
  }
}

//ЛУЧШЕ:
while (!isGameOver()) {
  //...
  if(isWin()) {
    printWinMessage();
  } else if(isLose()) {
    printLoseMessage();
  }
}

boolean isWin() {
  return isWordGuessed(maskedWord);
}

boolean isLose() {...}

boolean isGameOver() {
  return  isWin() || isLose();
}
```
Преимущества: состояния только проверяются, а не проверяются *и* кешируются; алгоритм проще и понятнее.

## ВЫВОД

В целом, замечаний не так много.  
Для развития навыков процедурной декомпозиции, посмотреть стрим Сергея [Крестики-нолики в процедурном стиле](https://www.youtube.com/watch?v=PPikj1qHxrA)

n.138(253)  
#ревью #виселица