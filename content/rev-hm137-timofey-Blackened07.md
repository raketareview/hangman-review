https://github.com/Blackened07/HangmanGame  
[Тимофей]

Игра в процедурном стиле, выполнена в нескольких классах.

## НЕДОСТАТКИ РЕАЛИЗАЦИИ

1. Не юзерфрендли интерфейс

Не нужно ставить юзеру необоснованных ограничений.  
Он должен имеь возможность вводить команды в любом регистре. 
Твоя задача- подстраиваться под удобство юзера, а не наоборот
```
Для начала игры введите заглавную букву 'Н'
```

2. Просили ввести букву, юзер ввел букву
```
Введите букву: 
q
Вы ввели неверную команду
```

3. Кроме процесса угадывания слова, юзер еще должен угадать, что от него вообще хотят
```
Введите букву: 
Г
Вы ввели неверную команду
```

4. Тут юзер уже бьется в истерике
```
Введите букву: 
ё
Вы ввели неверную команду
```

5. Нет списка введенных букв

## ХОРОШО

+ 👍 Игра запускается
+ 👍 Можно ввести только одиночную букву русского алфавита

## ЗАМЕЧАНИЯ

**1. Нейминг**

- Старайся не называть классы названием во множественном числе
```
class Words

//ПРАВИЛЬНО:
WordFileReader, WordStorage
```

- Название метода должно быть глаголом в повелительном наклонении
```
void wordsReader()

//ПРАВИЛЬНО:
void read()

//НО ТАМ ПО КОНТЕКСТУ ЛУЧШЕ:
void loadWords()
```

- Названия пакетов нужно писать стилем lower_snake
```
package Game;

//ПРАВИЛЬНО:
package game;
```

- UPPER_SNAKE только для констант, а это не константа
```
private final String PATH = "resources/russian_Words.txt";

//ПРАВИЛЬНО ТАК:
private final static String PATH = "resources/russian_Words.txt";
```

- Здесь даже намека нет на константу
```
private void picsReader() {
  String SEPARATOR = "#";
  //...
}

//ПРАВИЛЬНО:
private void picsReader() {
  String separator = "#";
  //...
}
```

- Избыточный контекст 
```
RandomWordGenerator.chooseTheRandomWord()

//ПРАВИЛЬНО:
RandomWordGenerator.get()
```

- При прочих равных, в названия не нужно вставлять частицы "Of", "The" и т.д. 
Если конечно там "Of" не используется в контексте `valueOf()`
```
int numberOfWords = words.getWords().size();

//ПРАВИЛЬНО:
int wordNumbers = words.getWords().size();
```

- Не сокращай без надобности- не заставляй читателя кода разгадывать ребус
```
void printHung(GamePics gamePics, int count)

//ПРАВИЛЬНО:
void printHangman(GamePics gamePics, int count)
```

- Название вводит в заблуждение. Метод возвращает оду картинку(pic) а не несколько (pics)
```
String getPicsByIndex(int index)

//ПРАВИЛЬНО:
String getPictureByIndex(int index)
```

- Это не стоит экономии четырех букв
```
private final List<String> pics;

//ПРАВИЛЬНО:
private final List<String> pictures;
```

- Название метода должно быть глаголом
```
boolean introducedCharsChecker(String input)

//ПРАВИЛЬНО ТАК:
boolean introduceБлабла(String input)

//ИЛИ ТАК:
boolean checkБлабла(String input)
```

- Хуже венгерской ноттации только венгерская ноттация, которая обманывает. 
Это не список char'ов, это список String'ов
```
List<String> chars
```
*Oracle Java code conventions, part."Naming conventions"*  
*Мартин, "Чистый код", гл.2*  
*Ютуб, Немчинский "Как называть переменные, методы и классы?"*

**2. Нарушение конвенции кода**

В любой ситуации выделяй тело блока скобочками, даже если тело состоит из одной строки. 
Исключение- метод equals(), там можно после if не выделять блоки скобочками
```
if (i != chars.size() - 1) {
  System.out.print(chars.get(i));
} else System.out.print(chars.get(i) + "\n");

//ПРАВИЛЬНО:
if (i != chars.size() - 1) {
  System.out.print(chars.get(i));
} else {
  System.out.print(chars.get(i) + "\n");
}
```

**3. Нарушение DRY**, магические буквы, числа, слова. Вводи константы. 

Если к одной и той же магической штуке обращаются два разных класса, то делай ее константой в одном из этих двух классов. 
Либо перенеси ее в третий класс- эти данные должны быть синхронизированы между собой
```
public interface Printable {
  String FAIL_RESTART_GAME_MESSAGE = "Игра не началась, нечего перезапускать. Введите 'Н', что бы начать игру!";  <-- ВОТ
  String FAIL_START_GAME_MESSAGE = "Игра в процессе и её можно только перезапустить! Введите 'Р', что бы перезапустить игру!";
  String START_GAME_AFTER_GAME_MESSAGE = "Если хотите продолжить игру, введите 'Н'\nЕсли хотите выйти, введите 'В'";
  //...
}

public class Main {
    private final static String introduce = """
            Для начала игры введите заглавную букву 'Н'  <-- ВОТ
            Для перезапуска игры введите заглавную букву 'Р'
            Для выхода из игры введите заглавную букву 'В'
            """;
}

public interface GameScanner extends Printable {
  String START_GAME_COMMAND = "Н";  <-- ВОТ
  String RESTART_GAME_COMMAND = "Р";
  String EXIT_GAME_COMMAND = "В";
  //...
}
```
*Фаулер, "Рефакторинг", гл.8, "Замена магического числа символической константой"*  
*refactoring.guru "Замена магического числа символьной константой"*  

**4. Если нужно печатать или создавать строку** с более, чем одним подстановочным значением или значение вставляется внутрь сообщения, используй форматированный вывод- тогда сразу будет виден весь шаблон
```
print(WIN_MESSAGE + "\n" + ENCODED_WORD_MESSAGE + randomWord.toUpperCase() + "! " + START_GAME_AFTER_GAME_MESSAGE);

//ПРАВИЛЬНО:
println(WIN_MESSAGE);
printf("%s%s! %s \n", ENCODED_WORD_MESSAGE, randomWord.toUpperCase(), START_GAME_AFTER_GAME_MESSAGE);
```
Да, этот шаблон выглядит ужасно. Но то, что у тебя без шаблона- выглядит еще ужаснее.

**5. class Words**

- Путь к файлу класс должен получать в конструктор. 

Иначе класс становится неуниверсальным.
Допустим, в программе будет несколько разных текстовых файлов- на разных языках или по разным темам. 
Тогда вместо нескольких специализированных классов файлового чтения, где имя файла словаря будет жестко прописано в каждом из классов, 
можно будет использовать один универсальный класс.

- Этот метод не должен быть protected, он должен быть публичный
```
protected Set<String> getWords() {
  return words;
}
```
Потому что это тот метод, который предназначен для пользования клиентом. А клиент не обязан находиться в одном пакете с классом `Words`.

- Реакция на ошибки

Если по указанному адресу не будет найден файл со словами, то программа аварийно вылетит и напечатает непонятные(для юзера) сообщения в консоль
```
Exception in thread "main" java.lang.RuntimeException: java.io.FileNotFoundException: resources\russian_Words.txt_ (Не удается найти указанный файл)  <-- ОТНОСИТЕЛЬНО ПОНЯТНО ЮЗЕРУ
  at Game.Words.wordsReader(Words.java:28)  <-- НЕПОНЯТНО ЮЗЕРУ
```
Здесь должна быть другая реакция на отсутствие файла со словами.  
Нужно сказать юзеру, что файл со словами по указанному пути открыть не удалось и поэтому работа программы будет завершена. 

Причем, путь нужно указать не относительный, как у тебя(`resources\russian_Words.txt_`). 
А абсолютный, чтобы юзер знал, где конкретно ему нужно искать файл: `c:/..../resources\russian_Words.txt_`.

После этого корректно завершить работу программы без вываливания красных эксепшенов в консоль.

**6. class RandomWordGenerator**

- В этом классе плохо его название и то, как он позиционируется через это название
```
public class RandomWordGenerator {
  //...
  public String chooseTheRandomWord()
}

//ЛУЧШЕ:
public class Dictionary {
  //...
  public String getRandom()
}
```

- Нарушение Low Coupling

Нет никакой необходимости, чтобы этот класс знал про класс Words и тем самым имел зависимость на этот класс
```
public class RandomWordGenerator {
  private final Words words;

  //...
  public RandomWordGenerator(Words words) {
    this.words = words;
  }
}

//ПРАВИЛЬНО:
public class RandomWordGenerator {
  //...
  public RandomWordGenerator(Set<String> words) {
    this.words = words;
  }
}
```

Словарь(а это по факту словарь) может работать не с коллекцией строк, а со специфическими классами. 
Но для этого должны быть основания.  
Например- возможность получать слова из разных загрузчиков слов:
```
public class Dictionary {
  private final WordReader reader;

  //...
  public Dictionary(WordReader reader) {
    this.reader = reader;
  }

  public String getRandom() {...}
}

public interface WordReader {
  Set<String> read();  
}

public class FileWordReader implements WordReader {...}
public class HttpWordReader implements WordReader {...}
```

**7. interface Printable**

- Не используй интерфейсы для хранения констант, это антипаттерн
```
String USER_INPUT_START_WAITING_MESSAGE = "Введите команду: ";
String START_GAME_MESSAGE = "Игра началась";
```
Перенеси константы в те классы, которые их используют. 
Или в отдельный класс с константами.

*Блох, "Java эффективное программирование", гл.4.8*  
```
"Антишаблон интерфейса констант — не используйте его." - Блох
```

Хранение текстовых констант здесь имеет практическое следствие. 
Из-за этого `interface` зависит от языка игры. А значит, нельзя использовать `interface Printable` c UI на других язках.
Вернее можно, конечно, но через костыли.

- Ошибка ООП дизайна при использовании интерфейсов

Интерфейс должен говорить что нужно делать", а не "как делать".
Использование дефолтных методов *здесь* прямо противоречит этой идее. 

Потому что не просто реализует какое-то абстрактное действие с данными.  
А выполняет конкретное действие, связанное с представлением данных- распечатывает конкретно в консоль
```
default void printReplaceSymbols(List<String> chars) {
  for (int i = 0; i < chars.size(); i++) {
    if (i != chars.size() - 1) {
      System.out.print(chars.get(i));  <-- КОНКРЕТНО В КОНСОЛЬ
    } else System.out.print(chars.get(i) + "\n");
  }
}
```
Здесь это имеет практические последствия- этот интерфейс нельзя будет использовать в других визуальных средах. 
Например, в Андроиде, там это просто не скомпилируется из-за `System.out.print()`.

- Неправильная сигнатура в методах интерфейса

Даже если не делать этот метод дефолтным, его сигнатура все равно никуда не годится
```
default void printHung(GamePics gamePics, int count) {...}

//ПРАВИЛЬНО:
void printHangman(int errorNumber);
```
Потому что `GamePics` хранит картинки в виде псевдографики- строк. 
А значит, они актуальны только для конкретной визуальной среды. 
Например, консоли или матричного принтера- cнова зависимость interface от представления.

В других средах, например в Андроиде или Виндовс-интерфейсе Swing, изображения висельника могут быть пиксельной графикой или анимацией.

**8. interface GameScanner extends Printable**

- Те же замечания. Это не интерфейс, это карго культ.

- Этот код вообще не читается

Нарушение конвенции кода + адовы условия в if'ах
```
if (userTextInput.isEmpty()) throw new InvalidCommandException(NOTHING_INPUT_MESSAGE);
else if (userTextInput.equals(userTextInput.replaceAll(NON_RUS_PATTERN, "")))
  throw new InvalidCommandException(USER_INPUT_ERROR_MESSAGE);
else if (userTextInput.length() != 1) throw new InvalidCommandException(MORE_THAN_ONE_CHAR_INPUT_MESSAGE);
else if (userTextInput.equals(START_GAME_COMMAND) || userTextInput.equals(RESTART_GAME_COMMAND) || userTextInput.equals(EXIT_GAME_COMMAND)) {...}

//ПРАВИЛЬНО:
if (isEmpty(userTextInput)) {
  throw new InvalidCommandException(NOTHING_INPUT_MESSAGE);
} else if (isНазваниеКотороеВсеОбъясняет()) {
  //...  
} 
```
Вводи вспомогательные методы, придерживайся конвенции кода.

**9. class GamePics**

Нарушение DRY.

По сути, класс выполняет ту же задачу, что и `class Words`- производит чтение текстового файла.
Замечания к нему те же.

Переосмысли эти два класса, выяви общее между ними- чтение текста из файла, вынеси это в отдельный класс.

**10. class GameProcess implements GameScanner**

- Побочный эффект. 

Судя по названию, метод должен только проверять игру(что бы это ни значило).  
Но кроме этого метод вносит неявные изменения в поле класса `List<String> replacedChars`
```
private boolean gameWordChecker(String input, List<String> chars) {
  for (String s : chars) {
    if (input.equals(s)) {
      replaceChars(s);  <-- ПОБОЧНЫЙ ЭФФЕКТ
      return true;
    }
  }
  return false;
}
```
Метод нужно разделить на несколько, каждый из которых будет выполнять только одну задачу.  
*Мартин "Чистый код", гл.3, "Избавьтесь от побочных эффектов"*  
*Фаулер "Рефакторинг", гл.6, "Извлечение метода"*  

- Здесь нет смысла возвращать значение

Потому что в случае `false` все равно бросит исключение. 
А значит, вместо обработки булевого результата, придется перехватывать исключение
```
private boolean introducedCharsChecker(String input) {
  boolean check = false;
  //...
  else {
    check = true;
    throw new RuntimeException(REPEAT_MESSAGE.toUpperCase());
  }
  //...
  return check;
}

//ПРАВИЛЬНО:
private void introducedCharsChecker(String input) {
  //...
  else {
    throw new RuntimeException(REPEAT_MESSAGE.toUpperCase());
  }
}
```

- В целом, весь код в классе плохо читается из-за системного несоблюдения конвенции кода и недостаточного количества вспомогательных методов.

## АРХИТЕКТУРА

Несмотря на то, что в проекте несколько классов, он написан в процедурном стиле.  
Программа в ООП стиле должна быть декомпозирована по правилам ООП и использовать ООП подход для реализации типичных задач.  
Здесь не видно ООП декомпозиции.

Код тяжело читается из-за плохого нейминга, сайд эффектов и спагетти-кода.

Интерфейсы здесь по сути таковыми не являются, а являются карго культом на ООП.

## ВЫВОД

Есть простейшие нарушения конвенции кода. 
Первым делом, нужно изучить *Oracle Java code conventions"*

Не видно понимания ООП на базовом уровне- зачем нужны интерфейсы и как их нужно использовать.

Если хочешь разобраться, как делать эту задачу в ООП стиле, можешь обратиться к Сергею за расширенными материалами к Виселице.

Сравнить различия между ООП и процедурным стилями:  
Стрим Сергея [Крестики-нолики в процедурном стиле](https://www.youtube.com/watch?v=PPikj1qHxrA)  
Мой стрим [Крестики-нолики в ООП стиле](https://t.me/zhukovsd_it_chat/53243/187097) 

n.137(252)  
#ревью #виселица  