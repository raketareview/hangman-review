https://github.com/makarikhin-n/Hangman/  
[распознается графически]

Игра в процедурном стиле, выполнена в нескольких классах.

## НЕДОСТАТКИ РЕАЛИЗАЦИИ

1. Так нужно ввести 0 и 1 или 1 и 2?
```
1 - Начать игру
2 - Выйти из игры
Напишите нужную команду (1,2) сюда: 
Необходимо ввести цифру 0 или 1. Попробуйте еще раз ...
```

2. Игра не запускается
```
Напишите нужную команду (1,2) сюда: 1
Exception in thread "main" java.lang.RuntimeException: Файл 'other/singular.txt' не найден в ресурсах
```

3. Это буква кириллицы
```
Введите сюда букву: ё

Переключитесь на кириллицу и повторите попытку ...
```

4. Можно ввести не одну букву, а набор букв и это будет считаться допустимым вводом

5. Почему вместо виселицы рисуются пустоты?
```
Напишите нужную команду (1,2) сюда: 1
- - - - - H A N G M A N - - - - -
О Ш И Б О К : 0 ИЗ 7
С Л О В О : * * * * * * 
О Ш И Б К И : []





Введите сюда букву: й ц у к 

- - - - - H A N G M A N - - - - -
О Ш И Б О К : 1 ИЗ 7
С Л О В О : * * * * * * 
О Ш И Б К И : [Й]





=========
```

6. Вешаеся безногий 
```
 +---+
 |   |
 O   |
/|\  |
     |
     |
=========
Введите сюда букву: г
ИГРА ОКОНЧЕНА. ВЫ ПРОИГРАЛИ :(
```

## ХОРОШО

+ 👍 Есть список введенных букв

## ЗАМЕЧАНИЯ

**0. Мусор в репе**

В репозитории кроме классов *.java лежат еще *.class. 
Разберись, какие классы/папки нужно комитить, а какие нет.

**1. Нейминг**

- Придерживайся единообразия
```
private static List<Character> allErrors = new ArrayList<>();
private static Set<Character> usedLetters = new HashSet<>();

//ПРАВИЛЬНО:
private static List<Character> errorLetters = new ArrayList<>();
private static Set<Character> usedLetters = new HashSet<>();
```

- Название должно объяснять суть явления. Просто "Helper" не объясняет ничего
```
class Helper

//ПРАВИЛЬНО:
BoardHelper, UserHelper etc.
```

- Не нужно в названиях писать частицы типа "The", "Of" и подобные им. Если конечно, "Of" не используется в контексте "valueOf()" 
```
boolean checkTheCurrentState(...)

//ПРАВИЛЬНО:
boolean checkCurrentState(...)
```
*Oracle Java code conventions, part."Naming conventions"*  
*Мартин, "Чистый код", гл.2*  
*Ютуб, Немчинский "Как называть переменные, методы и классы?"*

**2. Нарушение коненции кода**

- Если ты придерживаешься конвенции Google и считаешь `Scanner scanner` не константой, из-за того, что этот класс не immutable, тогда должен объявлять его ниже констант:
```
private static final Scanner scanner = new Scanner(System.in);
private static final int MAX_NUM_ERRORS = 7;

//ПРАВИЛЬНО:
private static final int MAX_NUM_ERRORS = 7;
private static final Scanner scanner = new Scanner(System.in);
```

- Если придерживаешься конвенции Oracle, то сканнер здесь это константа.
А значит, должен писаться UPPER_SNAKE:
```
private static final Scanner scanner = new Scanner(System.in);
private static final int MAX_NUM_ERRORS = 7;

//ПРАВИЛЬНО:
private static final Scanner SCANNER = new Scanner(System.in);
private static final int MAX_NUM_ERRORS = 7;
```

### class Menu

**3. Нарушение DRY**, магические буквы, числа, слова. Вводи константы 

```
System.out.println("1 - Начать игру");
System.out.println("2 - Выйти из игры");
System.out.print("Напишите нужную команду (1,2) сюда: ");

case "1": //
case "2": //
System.out.println("Необходимо ввести цифру 0 или 1. Попробуйте еще раз ...");

//ПРАВИЛЬНО:
private final static String START = "1";
private final static String QUIT = "2";

System.out.println(START + " - Начать игру");
System.out.println(QUIT + " - Выйти из игры");
System.out.prinf("Напишите нужную команду (%s,%s) сюда: ", START, QUIT);

case START: //
case QUIT: //
System.out.printf("Необходимо ввести цифру %s или %s. Попробуйте еще раз ...  \n", START, QUIT);
```

Здесь мы видим, почему нужно использовать константы- в одном месте игра требует ввести команды 1/2, а в другом- 0/1. 
Хотя имеются ввиду одни и те же команды для выполнения действия "Старт"/"Стоп". Просто эти тексты не синхронизированы между собой.

*Фаулер, "Рефакторинг", гл.8, "Замена магического числа символической константой"*  
*refactoring.guru "Замена магического числа символьной константой"*  

**4. Объясняй свою магию**

Объясняй загадочные действия
```
while (true) {
  System.out.print("\033[H\033[2J");
  //...
}  

//ПРАВИЛЬНО ТАК:
private final static String COMMAND_CLEAR_SCREEN = "\033[H\033[2J";

while (true) {
  System.out.print(COMMAND_CLEAR_SCREEN);
  //...
}

//ИЛИ ТАК:
while (true) {
  clearScreen();
  //...
}
```

**5. Нарушение инкасуляции**. Публичным тут должен быть только метод `startGame()`.

**6. Класс делает лишнее**

Раз этот класс называется меню, значит он должен выполнить только те действия, которые касаются функционирования меню.  
Но не должен сам управлять процессом игры
```
public class Menu {

  public void startGame() {
    <печатает тест меню>

    switch (scanner.nextLine()) {
      case "1":
        startGameLoop();
        break;
      //...
    }
  }

  public void startGameLoop() {
    //управление процессом игры
  }
}

//ПРАВИЛЬНО:
public class Menu {

  public void startGame() {
    <печатает тест меню>

    switch (scanner.nextLine()) {
      case "1":
        Game game = new Game();
        game.start();
        break;
      //...
    }
  }
}
```

### class Helper

**7. Неправильно использование статических изменяемых полей**

Если классы содержат не только статические методы и его экземпляры создаются через `new`, то изменяемые поля в них не должны быть статическими.  
Потому что если создать два таких объекта, они при работе будут изменять значения в одних и тех же статических полях, что приведет к багам
```
public class Helper {
  private static List<Character> allErrors = new ArrayList<>();
  private static Set<Character> usedLetters = new HashSet<>();
  //...
  public List<Character> getRandomWord() {...}
}

//ПРАВИЛЬНО:
public class Helper {
  private List<Character> allErrors = new ArrayList<>();
  private Set<Character> usedLetters = new HashSet<>();
  //...
  public List<Character> getRandomWord() {...}
}
```
И тут вопрос не в том, должно ли *конкретно в этом* проекте быть два экземпляра одного класса, или не должно.  
Если класс имеет не статические методы и его объект создается через `new`, то по умолчанию мы признаем, 
что его экземпляров в проекте может быть более одного.

**8. Реакция на ошибки** 

- Если по указанному адресу не будет найден файл со словами, то программа аварийно вылетит и напечатает непонятные для юзера сообщения в консоль
```
Напишите нужную команду (1,2) сюда: 1
Exception in thread "main" java.lang.RuntimeException: Файл 'other/singular.txt' не найден в ресурсах  <-- ОТНОСИТЕЛЬНО ПОНЯТНО ЮЗЕРУ
  at Helper.getRandomWord(Helper.java:29)  <-- НЕПОНЯТНО ЮЗЕРУ
```
Здесь должна быть другая реакция на отсутствие файла со словами.  
Нужно сказать юзеру, что файл со словами по указанному пути открыть не удалось и поэтому работа программы будет завершена. 

Причем, путь нужно указать не относительный, как у тебя(`other/singular.txt`). 
А абсолютный, чтобы юзер знал, где конкретно ему нужно искать файл: `c:/..../other/singular.txt`.

После этого корректно завершить работу программы без вываливания красных эксепшенов в консоль.


**9. Печать картинок хангмана**

Печать картинок сейчас слишкомс сложная и непонятная. Самое плохое здесь- из кода мы не можем представить, как в итоге эти картинки распечатаются.
Отладка и изменение такого кода будет сложной
```
public void showHangman(int errorCounter) {
  String[] partHangman = new String[7];
  Arrays.fill(partHangman, "");

  if (errorCounter >= 1) {
    partHangman[6] = "=========";
  }
  if (errorCounter >= 2) {
    partHangman[0] = "     +";
    partHangman[1] = "     |";
    //остальной индусский код
  }
  //...
}
```

Картинки нужно хранить в статическом массиве и печатать по номеру.  
Например, так
```
public class HangmanRenderer {
  private static final String[] PICTURES = {
    """
    -----   
    |       
    |       
    |       
    |       
    ------- 
    """,
   """
     -----   
     |   |   
     |   O   
     |       
     |       
     ------- 
     """
   ,
   // more pics
  };

  public void render(int numPicture) {  
    System.out.println(PICTURES[numPicture]);
  }
}
```
Желательно картинки и метод распечатки вынести в отдельный класс, чтобы в основном классе не смешивать логику и графику.

**10. Магические числа**
```
if (1040 <= letter && letter <= 1071) {...}
```
1040 год- битва при Данданакане. Или имелось ввиду что-то иное? 

**11. Если в блоке if есть return**(break, continue, throw, exit и т.д.), то else не пишется.  
В этом случае неважно, будет else или нет, так как программа будет работать одинаково и код без `else` будет выглядеть читабельней
```
if (allErrors.contains(letter)) {
  //...
  return true;
} else if (usedLetters.contains(letter)) {
  //...
  return true;
} else {
  return false;
}

//ПРАВИЛЬНО:
if (allErrors.contains(letter)) {
  //...
  return true;
} 
if (usedLetters.contains(letter)) {
  //...
  return true;
} 
return false;
```

**12. Объявляй переменные в тех блоках, где они используются**
```
List<String> dict = new ArrayList<>();

try (InputStream inputStream = getClass().getClassLoader().getResourceAsStream("other/singular.txt")) {
  dict = List.of(temp.split("\n"));
  //...
}

//ПРАВИЛЬНО:
try (InputStream inputStream = getClass().getClassLoader().getResourceAsStream("other/singular.txt")) {
  List<String> dict = List.of(temp.split("\n"));
  //...
}
```

**13. Создавай вспомогательные методы**, делай программу более простой и понятной

Божественный метод на миллион строк, делает много разного. 
Находи отдельные смысловые блоки кода и выноси их во вспомогательные методы, каждый из которых будет делать что-то своё
```
public int giveAnswer(List<Character> word, List<Character> wordMask, int errorCounter) {
  //...

  for (int index = 0; index < word.size(); index++) {
    if (letter == word.get(index)) {

      usedLetters.add(letter);

      wordMask.set(index, letter);
      word.set(index, maskForGuessed);

      isError = false;
    }
  }

  //...
}

//ПРАВИЛЬНО:
public int giveAnswer(List<Character> word, List<Character> wordMask, int errorCounter) {
  //...

  if(isWordLetter(letter)) {
    openLetter(letter);
  } else {
    addErrorLetter(letter);
    errorCount++;
  }
  addUsedLetter(letter);

  //...
}
```
*Мартин "ЧК", гл.3*  
*Фаулер "Рефакторинг", гл.6, "Выделение метода"*

**14. Побочный эффект**

Судя по названию, метод должен только проверить состояние (игры).  
Но кроме этого метод делает много разного, в том числе устанавливает глобальный флаг конца игры, печатает информационные сообщения etc
```
boolean checkTheCurrentState(int errorCounter, int needToGuess, List<Character> word)
```
Метод нужно разделить на несколько, каждый из которых будет выполнять только одну задачу.  
*Мартин "Чистый код", гл.3, "Избавьтесь от побочных эффектов"*  
*Фаулер "Рефакторинг", гл.6, "Извлечение метода"*  

**15. class Main**, содержит точку входа main

👍 Только запускает игру, в данном случае через меню, это хорошо.

## ВЫВОД

Код читается тяжело из-за больших методов и побочных эффектов.

Для развития навыков процедурной декомпозиции посмотреть стрим Сергея [Крестики-нолики в процедурном стиле](https://www.youtube.com/watch?v=PPikj1qHxrA)  
Сравнить с ООП стилем: мой стрим [Крестики-нолики в ООП стиле](https://t.me/zhukovsd_it_chat/53243/187097)

n.135(249)  
#ревью #виселица 