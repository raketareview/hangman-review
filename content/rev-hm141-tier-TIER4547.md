https://github.com/TIER4547/Hangman_OOP  
[TIER]

Игра в смешанном стиле: процедурный/ООП.

## НЕДОСТАТКИ РЕАЛИЗАЦИИ

1. На старте пишет слово, которое нужно отгадать
```
вентилятор
**********
```

2. Почему текст печатается с разными отступами слева, это красиво?
```
Нажмите 1 если хотите начать игру, 2 если нет
1
    Игра началась!
   Вам нужно угадать загаданное слово по буквам!
   Количество попыток ограничено!

вентилятор
**********
```

3. Неправильная последовательность в порядке отображения введенных букв: букву 'ц' я ввел второй, но в распечатке она стала первой. 
При этом введенная третьей буква 'к' в распечатке стала на третью позицию
```
й
Использованные буквы: [й]

ц
Использованные буквы: [ц, й]

к
Использованные буквы: [ц, й, к]
```
Введенные буквы должны размещаться по алфавиту(й, к, ц) либо в порядке ввода(й, ц, к).

4. При проигрыше не открывает отгадываемое слово
```
Вы проиграли!
Использованные буквы: [у, г, е, ц, ш, й, к, н]
******н***ц**
```

## ХОРОШО

+ 👍 Игра запускается
+ 👍 Можно ввести только одиночную букву русского алфавита
+ 👍 Есть список введенных букв

## ЗАМЕЧАНИЯ

Реализация заявлена как ООП, поэтому критика к классам будет с точки зрения ООП.

**1. Нейминг**

- Избыточно. Не дублируй имя класса в названии его методов
```java
Hangman.printHangman(...);

//ПРАВИЛЬНО:
Hangman.print(...);
```

- UPPER_SNAKE только для констант
```java
public static int TRIES = 6;

//ПРАВИЛЬНО ТАК:
public final static int TRIES = 6;
```

- Если метод называется "проверка"(check), то он должен выполнить проверку и вернуть результат проверки в виде boolean.
Сейчас название метода не соответствует тому, что он делает
```java
private void checkForUsedCharacters(String guessedCharacter) 
```

- Хуже венгерской ноттации только венгерская ноттация, которая обманывает.
Это не перменная типа `Character`, это массив примитивного типа `char`
```java
char[] character = word.toCharArray();
```

- В дополнение к предыдущему замечанию. Этот сет хранит не объекты типа `Character`, а Стринги.
Но даже, если бы хранил чарактеры, то так этот сет нельзя было бы называть- это была бы венгерская ноттация
```java
Set<String> usedCharacters = new HashSet<>();

//ПРАВИЛЬНО:
Set<String> usedLetters = new HashSet<>();
```

- "Character" не может быть строкой, потому что так называется класс данных, который хранит один символ. 
Даже "Letter" не может быть строкой, потому что буква тоже состоит из одного символа 
```java
String guessCharacter = scanner.next().toLowerCase();

//ПРАВИЛЬНО:
String userInput = scanner.next().toLowerCase();
```

- Название метода должно быть глаголом в повелительном наклонении. 
Это название не соотвествует требованиям
```java
void guessedWrongCharacter(String guessedCharacter)
```

*Oracle Java code conventions, part."Naming conventions"*  
*Мартин, "Чистый код", гл.2*  
*Ютуб, Немчинский "Как называть переменные, методы и классы?"*  

**2. class Dictionary**

- Путь к файлу класс должен получать в конструктор. 

Иначе класс становится неуниверсальным.
Допустим, в программе будет несколько разных текстовых файлов- на разных языках или по разным темам. 
Тогда вместо нескольких специализированных классов Словаря, где имя файла словаря будет жестко прописано в каждом из классов, 
можно будет использовать один универсальный класс Словарь.

- Реакция на ошибки.

Если по указанному адресу в `class Dictionary` не будет найден файл со словами, то программа аварийно вылетит и напечатает непонятные для юзера сообщения в консоль
```java
Exception in thread "main" java.lang.NullPointerException
	at java.base/java.io.Reader.<init>(Reader.java:280)
	at java.base/java.io.InputStreamReader.<init>(InputStreamReader.java:74)
```
Здесь должна быть другая реакция на отсутствие файла со словами.  
Нужно сказать юзеру, что файл со словами по указанному пути открыть не удалось и поэтому работа программы будет завершена. 

Нужно написать путь, по каторому не удалось прочитать файл.  
Причем, путь нужно указать абсолютный, чтобы юзер знал, где конкретно ему нужно искать файл: `c:/.../nounsDictionary.txt`.

После этого корректно завершить работу программы без вываливания красных эксепшенов в консоль.

**3. class Hangman**, рендерер картинок виселицы

- В любом switch-case должен быть default. 
В данном случае default должен бросать исключение с сообщением, что такого номера картинки не существует.

- Печать картинки виселицы через switch-case или if-elseif - индусский код.  
Картинки нужно хранить в статическом массиве и печатать по номеру.  
Например, так
```java
public class HangmanRenderer {
  private static final String[] PICTURES = {
    """
    -----   
    |       
    |       
    |       
    |       
    ------- 
    """,
   """
     -----   
     |   |   
     |   O   
     |       
     |       
     ------- 
     """
   ,
   // more pics
  };

  public void render(int numPicture) {  
    System.out.println(PICTURES[numPicture]);
  }
}
```

- Закомментированный код- антипаттерн "Лодочный якорь". 
Этот код никогда уже не понадобится
```java
//                System.out.println(GAME_STATE_LOSE);
//                tries = false;
//                startGameRound = false;
```
Не превращай программу в свалку. 
Если что-то хочешь сохранить на память, сделай коммит в гите с соответствующим комментарием.  
Правильное использование комментариев: *"Чистый код", гл.4*

**4. class Constant**

- Константный класс, как разновидность утилитного класса, должен быть `final` и иметь приватный конструктор- не должно быть возможности унаследоваться от утилиты или сделать ее экземпляр. 
*Блох, "Java. Эффективное программирование", изд.3, гл.2.4*

- Константы должны быть `public static final`
```
public static int TRIES = 6;  <-- ЭТО СТАТИЧЕСКОЕ ПОЛЕ, НО НЕ КОНСТАНТА
```

**5. class Game**

+ 👍 Класс принимает в конструктор достаточное количество зависимостей, это хорошо
```java
public Game(Dictionary dictionary, Hangman hangman) {...}
```

- Нарушение паттерна GRASP Creator

Но нужно ли принимать в коснтруктор *именно эти* объекты?
```java
Dictionary dictionary, Hangman hangman
```
Это не интерфейсы, поэтому нельзя в `Game` передать разные их реализации. 
Эти объекты не параметризируются при создании, а значит их вполне мог бы внутри себя создать сам `Game`.

Например, имело бы смысл передвать в конструктор `Game` экземпляр `Dictionary` в том случае, если бы он параметризировался при создании. 
То есть, если бы он не содержал в себе жестко прописанный путь к файлу, а получал путь к файлу в конструктор.  
Тогда на уровне `main` можно было бы создавать `Dictionary` с конкретным файлом-словарем(напр. легкие слова, сложные слова) и инжектить его в конструктор Game.

Сейчас передача в конструктор `Game` этих зависимостей не имеет практического смысла и нарушает паттерн "Creator".

- Нарушение инкапсуляции. Публичным должен быть только `startGame()`.

- Расточительно. 

Если регулярное выражение используется многократно, как здесь, то нужно его откомпилировать.  
Компиляция регулярного выражения в объект Pattern с помощью `Pattern.compile()` позволяет повысить производительность при многократном использовании
```java
if (guessCharacter.matches("[А-Яа-яЁё]+")) {...}
```
*Блох "Java. Эффективное программирование", изд.3, гл.2.6*

- Несоответствие названия тому, что делает метод.

Этот метод обещает открыть буквы. На самом деле, тут происходит вся логика игрового процесса. 
В том числе открыти букв, но не только.
```java
void openCharacters(String word) {...}
```
Еще этот метод получает от юзера букву, проверяет ее, проверяет состояние игры, пишет информацию о раунде и т.д.  
Метод нужно назвать адекватно тому, что он делает.

- Вместо переменных для хранения состояния игры(флагов), используй вспомогательные методы
```java
private boolean gameNotFinished = true;

while (gameNotFinished) {
  //...
  if (Arrays.equals(maskCharacters, character)) {
    System.out.println("Поздравляем! Вы выиграли! угаданное слово: " + word);
    gameNotFinished = false;
  }
}

//ПРАВИЛЬНО:
while (!isGameOver()) {
  //...
  if (isWin()) {
    System.out.println("Поздравляем! Вы выиграли! угаданное слово: " + word);
  } else (ifLose()) {
    //печать сообщения при ошибке
  }
}

boolean isWin() {...}

boolean isLose() {...}

boolean isGameOver() {
  return  isWin() || isLose();
}
```
Преимущество: используя вспомогательные методы вместо переменных-флагов, не приходится переживать за актуальность данных, которые хранят переменные. 
Код становится проще и надежнее.

- Создавай вспомогательные методы, делай программу более простой и понятной
```java
if (guessCharacter.matches("[А-Яа-яЁё]+")) {
  break;
}

//ПРАИВЛЬНО:
if (isRussianLetter(letter)) {
  break;
}
```

- Отдельные смысловые блоки в больших методах тоже выноси во вспомогательные методы, например:
```java
private char inputRussianLetter() {
  System.out.println("Введите букву: ");  
  while(true) {
    //получить от юзера строку
    //проверить, что строка состоит из одного символа
    //проверить, что это символ русского языка
    //если все проверки ок- вернуть это символ 
    //если не ок- ничего не делать, вечный цикл все начнет сначала
  }
}
```
*Фаулер "Рефакторинг", гл.6, "Выделение метода"*

- Метод должен делать только одно дело, а этот метод делает разное. 

Название не объясняет, что делает метод. 
Наверное потому, что трудно это сформулировать- настолько он аморфный
```java
void checkIfWordContainsCharacter(String word, char[] character, String guessedCharacter) 
```
Определить, что конкретно делает метод и разделить его на несколько, каждый из которых будет делать только свое.  
*"ЧК", гл.3, "Правило одной операции"*

- Нарушение DRY. Если текст встречается несколько раз в коде, то его нужно сделать константой
```
"Вы уже вводили данную букву. Попробуйте ввести другую"
```

**6. class Main**, содержит точку входа main

+ 👍 Только создает и запускает игру, это хорошо.

+ 👍 Тут происходит диалог "Играть или выйти", это хорошо.

- Нарушение DRY, магические буквы, числа, слова. Вводи константы 
```java
System.out.println("Нажмите 1 если хотите начать игру, 2 если нет");
if (console.equals("1")) {...}
if (console.equals("2")) {...}
System.out.println("Нажмите 1 если хотите начать игру, 2 если нет");

//ПРАВИЛЬНО:
private final static String START = "1";
private final static String QUIT = "2";
private final static String TITTLE = "Нажмите '%s' если хотите начать игру, '%s' если нет".formatted(START, QUIT);

System.out.println(TITTLE);
if (console.equals(START)) {...}
if (console.equals(QUIT)) {...}
System.out.println(TITLE);
```
*Фаулер, "Рефакторинг", гл.8, "Замена магического числа символической константой"*  
*refactoring.guru "Замена магического числа символьной константой"*  

## АРХИТЕКТУРА

Код программы плохо читается из-за плохого нейминга в `Game`.
Методы в `Game` недостаточно хорошо разделены по ответственностям.
Это взаимная проблема: для плохого метода трудно придумать хорошее название, потому что бывает трудно сформулировать, что такой метод делает.

**Программа заявлена как ООП, но не является им *в полной мере*.**

С одной стороны, программа поделена на классы, которые являют собой отдельные сущности: словарь, рендерер виселицы, основная игровая логика.

С другой стороны, это слишком очевидные классы, которые вполне могут существовать при процедурном стиле программирования. 
Сам факт разделения программы на классы еще не делает программу объектно-ориентированной.  
Здесь не выявлены и не сделаны в виде классов другие сущности(минимум один), сам факт выявления которых говорил бы о понимании идей объектно-ориентированной декомпозиции.

Неясны намерения, по которым в конструктор `Game` передается то, что передается. 
Поэтому сейчас это выглядит как карго-культ на ООП.

Я бы сказал, что данный момент программа имеет черты как объектно-ориентированного, так и процедурного стиля программирования. 
Хотя ближе все-таки к ООП.

## ВЫВОД

Для развития навыков процедурной декомпозиции, посмотреть стрим Сергея [Крестики-нолики в процедурном стиле](https://www.youtube.com/watch?v=PPikj1qHxrA)

Сравнить различия между ООП и процедурным стилями:  
Стрим Сергея [Крестики-нолики в процедурном стиле](https://www.youtube.com/watch?v=PPikj1qHxrA)  
Мой стрим [Крестики-нолики в ООП стиле](https://t.me/zhukovsd_it_chat/53243/187097)

Посмотреть ролики Немчинского про SOLID.

Подробное объяснение, как делать эту программу в процедурном и ООП стилях, есть у Сергея в расширенных материалах.

n.141(259)  
#ревью #виселица