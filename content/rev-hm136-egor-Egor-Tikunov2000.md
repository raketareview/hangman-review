https://github.com/Egor-Tikunov2000/HangmanGame  
[Егор]

Игра в процедурном стиле, выполнена в нескольких классах.

Проект на C#, ревью на правах общей эрудиции.

## НЕДОСТАТКИ РЕАЛИЗАЦИИ

1. Можно ввести не одну букву, а набор букв и это будет считаться допустимым вводом
```
Введите букву: ыфвфвфвфвф
Увы, буквы 'Ы' нет в слове. Потеряна попытка.
```

2. Можно ввести не только русскую букву, но и буквы других алфавитов
```
Неверные буквы: A, I, Q, Й
```

3. Вешается одноногий
```
    +---+
    |   |
    O   |
   /|\  |
   /    |
        |
  =========
Введите букву: а
Увы, буквы 'А' нет в слове. Потеряна попытка.
Вы проиграли. Game Over.
```

## ХОРОШО

+ 👍 Игра запускается
+ 👍 Есть список введенных букв

## ЗАМЕЧАНИЯ

**1. Нейминг**

- Кастомный класс WordList может так называться только в том случае, если он наследуется от стандартного класса List. 
Этот класс не наследуется, нужно придумать синоним
```
class WordList
```

- Название класса должно быть существительным
```
class StartGame

//ПРАВИЛЬНО:
class Game
```

- Название должно объяснять суть явления. Этот класс не печатает виселицу, он хранит и выдает картинки виселицы
```
class PrintGallow

//ПРАВИЛЬНО:
class GallowPictureStorage
```

- Избыточно
```
LetterFeedback.GetFeedbackMessage(char letter)
StartGame.StartGallowGame()

//ПРАВИЛЬНО:
LetterFeedback.GetMessage(char letter);
Game.Start()
```

- Название не соответствует тому, что делает метод. Метод не делает команду букв, метод делает LetterFeedback
```
LetterFeedback MakeLetterCommand(char letter) 

//ПРАВИЛЬНО:
LetterFeedback CreateLetterFeedback(char letter)
```

- Этот метод проверяет на проигрыш, а не конец (игры). Конец игры- когда ты выиграл или проиграл
```
public bool IsOver()
{
  return _incorrectLetter >= _maxIncorrectLetters;
}

//ПРАВИЛЬНО:
public bool IsLose()
```

*.NET Общие соглашения об именовании*  
*Правила и соглашения об именовании идентификаторов C#*  
*Мартин, "Чистый код", гл.2*  
*Ютуб, Немчинский "Как называть переменные, методы и классы?"*

**2. Комментарии**

Комментарии здесь в основном не несут полезной нагрузки, а констатируют очевидное
```
/// <summary>
/// Проверяет, выиграл ли игрок.
/// </summary>
/// <returns>True, если игрок угадал все буквы</returns>
public bool IsWin() {...}
```
Когда в проекте много каментов, это плохо- пользы от них практически нет, они только забивают пространство и мешают читать код.
В идеале, комментариев вообще не должно быть, код должен объяснять сам себя через правильный нейминг и лаконичный код.  
*Мартин, "ЧК", гл.4* 

**3. class WordList**

- Путь к файлу класс должен получать в конструктор

Сейчас класс хранит в себе путь к файлу в виде захардкодженой строки. 
Это делает класс неуниверсальным.

Допустим, в программе будет несколько разных текстовых файлов- на разных языках или по разным темам. 
Тогда вместо нескольких специализированных классов Словаря, где имя файла словаря будет жестко прописано в каждом из классов, можно будет использовать один универсальный класс словарь.

- Реакция на ошибки

Если по указанному адресу не будет найден файл со словами, то программа аварийно вылетит и напечатает непонятные для юзера сообщения:
```
System.TypeInitializationException: "The type initializer for 'Hangman_Game.WordList' threw an exception."
```
Здесь должна быть другая реакция на отсутствие файла со словами.  
Нужно сказать юзеру, что файл со словами по указанному пути открыть не удалось и поэтому работа программы будет завершена. 

Путь к файлу нужно указывать абсолютный, чтобы юзер знал, где конкретно ему нужно искать файл: `c:/..../russian_nouns.txt`.
После этого корректно завершить работу программы без аварий.

**4. class PrintGallow**

- Класс называется "Печатать", но на самом деле он ничего не печатает, а возвращает картинку по номеру ошибки
```
string GetArt(int errors)
```

- Реакция на ошибки

Есть три основных варианта реакции на ошибку: игнорирование, исправление, прерывание работы.  
Здесь выбрано исправление- если приходит запрос с некорректным номером картинки, возвращается картинка с ближайшим корректным номером:
```
public string GetArt(int errors)
{
  if (errors < 0)
  {
    errors = 0;
  }
  if (errors >= hangmanState.Length)
  {
    errors = hangmanState.Length - 1; 
  }
  return hangmanState[errors];
}
```
В данном случае это неправильный способ реакции, здесь нужно быстроее падение:
```
public string GetArt(int errors)
{
  return hangmanState[errors];  <-- ЕСЛИ ERRORS НЕКОРРЕКТЕН, ВЫЛЕТИТ ИСКЛЮЧЕНИЕ
}
```
Потому что при таком исправлении, если придет баг, то игра будет работать некорректно. 
Например, счетчик ошибок будет увеличиваться сверх предела, а рисоваться будет одна и та же картинка.  
Поэтому в таких случаях нужно бросать исключение, чтобы сразу выявить и исправить этот баг.

**5. Классы в файле LetterFeddback.cs**

В этом файле находится `abstract class LetterFeedback` и его наследники.

Их назначение- возвращать конкретное информационное сообщение 
```
public abstract class LetterFeedback
{
  public abstract string GetFeedbackMessage(char letter);    
}

public class CorrectLetter : LetterFeedback
{
  public override string GetFeedbackMessage(char letter)
  {
    return $"Отлично! Буква '{letter}' есть в слове.";
  }
}
```
Это похоже на антипаттерн "Дробление"- здесь эти классы являются просто оберткой над конкретным сообщением.

**6. class WordsChecker**

- Тут мы видим, как используются ранее рассмотренные Фидбеки
```
public LetterFeedback MakeLetterCommand(char letter) 
{
  letter = char.ToUpper(letter);
  if (!char.IsLetter(letter))
  {
    return new InvalidLetter(); 
  }
  if (_guessedLetters.Contains(letter))
  {
    return new AlreadyLetter(); 
  }
  //...
}
```
Если хочешь хранить сообщения в отдельном классе(само по себе желание норм), создай отдельный класс для всех сообщений.  
Плюсы такого подхода- можно сделать классы с сообщениями на разных языках и юзать их через полиморфизм:
```
public abstract class MessageCenter
{
  public abstract string GetCorrectLetterMessage(char letter);
  public abstract string GetIncorrectLetterMessage(char letter);
}

public abstract class RusMessageCenter : MessageCenter
{
  public override string GetCorrectLetterMessage(char letter)
  {
    return $"Отлично! Буква '{letter}' есть в слове.";
  }
  public override string GetIncorrectLetterMessage(char letter) {
    return $"Увы, буквы '{letter}' нет в слове. Потеряна попытка.";
  }
}

public abstract class EngMessageCenter : MessageCenter
{
  public override string GetCorrectLetterMessage(char letter)
  {
    return $"Excellent! The letter '{letter}' is in the word.";
  }
  public override string GetIncorrectLetterMessage(char letter)
  {
    return $"Sorry, the letter '{letter}' isn't in the word. Lost attempt.";
  }
}
```
И тогда алгоритм будет проще, понятнее и универсальнее, а проект избавится от кучи лишних классов:
```
public string GetLetterFeedbackMessage(char letter) 
{
  letter = char.ToUpper(letter);
  if (!char.IsLetter(letter))
  {
    return _messageCenter.GetInvalidLetterMessage(letter); 
  }
  if (_guessedLetters.Contains(letter))
  {
    return _messageCenter.GetAlreadyLetterMessage(letter); 
  }
  //...
}
```
Ближайшая аналогия- твой класс `PrintGallow`, который возвращает строку-картинку, а не объект `GallowFeedback`.  
По сути, возврат сообщения и возврат картинки это задачи одного порядка и должны решаться одинаковым образом.

- Метод делает много всякого, но одно он не делает точно- не создает букву
```
GuessResult MakeLetter(char letter)
```

- Побочные эффекты.

Судя по сигнатуре, метод должен делать возращать информационные сообщения
Но кроме этого метод вносит изменения в список букв, увеличивает счетчик ошибок
```
public LetterFeedback MakeLetterCommand(char letter) 
{
   //... 
   if (_guessedLetters.Contains(letter))
   {
     return new AlreadyLetter(); <-- ВОЗВРАТ ИНФОРМАЦИОННОГО СООБЩЕНИЯ
   }

   _guessedLetters.Add(letter); <-- ПОБОЧНЫЙ ЭФФЕКТ

    if (_wordToGame.Contains(letter))
    {
      return new CorrectLetter(); <-- ВОЗВРАТ ИНФОРМАЦИОННОГО СООБЩЕНИЯ
    }
    //...
    _incorrectLetter++; <-- ПОБОЧНЫЙ ЭФФЕКТ
    //...
}
```
Метод нужно разделить на несколько, каждый из которых будет выполнять только одну задачу.  
*Мартин "Чистый код", гл.3, "Избавьтесь от побочных эффектов"*  
*Фаулер "Рефакторинг", гл.6, "Извлечение метода"*  

**7. class StartGame**, основная игровая логика

- Вводи вспомогательные методы, делай программу проще и понятнее
```
while (!_wordsChecker.IsWin() && !_wordsChecker.IsOver()) {...}

//ПРАВИЛЬНО:
while (!IsGameOver()) {...}

private boolean isGameOver() {
  return _wordsChecker.IsWin() || _wordsChecker.IsLose();
}
```

- Название не соответствует тому, что делает метод

Метод не выполняет процесс угадывания. Метод распечатывает информационное сообщение про букву
```
private void ProcessGuess(char letter)
{
  LetterFeedback feedback = _wordsChecker.MakeLetterCommand(letter);
  Console.WriteLine(feedback.GetFeedbackMessage(letter));
}
```
Да, где-то в недрах `MakeLetterCommand(letter)` в виде побочных эффектов выполняется еще какой-то алгоритм.
Но на уровне кода `ProcessGuess(char letter)` читатель об этом не сможет догадаться.

Пишу это замечание здесь, а не в "Нейминге", чтобы подчеркнуть, что плохие названия в этой реализации это не исключение, а правило.
Ни одному названию здесь заранее нельзя доверять, методы делают не то, что обещают.

Критиковать названия каждого такого метода я не буду- их много. 
Тебе нужно пересмотреть свой подход к придумыванию названий.

**8. class Program**, содержит точку входа main

+ 👍 Только создает и запускает Игру(`class StartGame`), это хорошо.

## АРХИТЕКТУРА

Несмотря на то, что в проекте несколько классов, он написан в процедурном стиле.  
Программа в ООП стиле должна быть декомпозирована по правилам ООП и использовать ООП подход для реализации типичных задач.  
Здесь не видно ООП декомпозиции.

Код тяжело читается из-за плохого нейминга, сайд эффектов и спагетти-кода.

## ВЫВОД

Если хочешь разобраться, как делать эту задачу в ООП стиле, можешь обратиться к Сергею за расширенными материалами к Виселице.

Сравнить различия между ООП и процедурным стилями:  
Стрим Сергея [Крестики-нолики в процедурном стиле](https://www.youtube.com/watch?v=PPikj1qHxrA)  
Мой стрим [Крестики-нолики в ООП стиле](https://t.me/zhukovsd_it_chat/53243/187097)  
C# и Java очень похожи, так что понять получится.

n.136(250)  
#ревью #виселица #csharp 